/* 「SQL 写与优化」深度版案例（16-20） */

export const DEEP_SQL_4 = [
({
  t: '临时表滥用 · 物化代价', sub: '10 分钟写完', d: '优化临时表使用：识别物化代价与替代',
  ly: 'doc', pal: 'orange', bg: 'grid',
  facts: ['输入：含临时表逻辑', '输出：优化方案', '提速 15 倍'],
  inp: '粘贴查询逻辑', out: ['结构：物化代价识别', '关键：流式替代', '配套：必要场景保留'],
  stats: [['耗时', '1.5s→100ms'], ['临时表', '消除'], ['保留', '场景声明']],
  quote: '临时表不是免费的中间站：写进去、读出来都是成本——先问「这步中间结果真的需要落地吗」',
  fb: ['开发', '把三步临时表拍平成一条查询后，报表快了一个数量级'], risk: '把复杂查询拆成「一串临时表」看似清晰，实则每一步都在付落地的税',
  ctx: '一个报表查询用了三步临时表（先筛、再聚、后关联），每步都要落地读写，1.5 秒。分析发现中间结果都很小，完全可以在一条查询里用子查询或窗口函数完成。拍平后 100 毫秒。同时明确临时表的正当使用场景（跨语句复用的大中间结果），避免矫枉过正。',
  raw: `报表查询结构：
第一步：筛选结果 → 临时表 1
第二步：临时表 1 聚合 → 临时表 2
第三步：临时表 2 关联维度 → 结果

问题：
· 每步临时表都要
  写存储再读出来
· 三步之间优化器
  无法整体优化
· 1.5 秒

分析：
中间结果都很小
（几千行），
没有落地必要`,
  steps: [
    ['算代价', '物化成本盘点', '2 分钟'],
    ['审中间', '中间结果大小评估', '2 分钟'],
    ['做拍平', '子查询与窗口替代', '3 分钟'],
    ['验等价', '结果一致性', '2 分钟'],
    ['定边界', '临时表正当场景', '2 分钟'],
    ['立规矩', '评审检查项', '1 分钟'],
  ],
  full: `【优化 · 临时表（物化代价）】

一、物化代价（先算账）
临时表的每一步：
· 计算结果写入
　（内存或磁盘）
· 后续步骤再读出
· 优化器看不到
　「全局形状」，
　只能按部就班
三步临时表 =
三次「写+读」的税，
外加全局优化机会
的丧失

二、中间结果评估
关键问题：
中间结果有多大？
· 小（几千几万行）：
　落地毫无必要，
　派生表/窗口函数
　在内存中流转即可
· 大且被多次复用：
　落地反而划算
　（算一次，用多次）
本例：
中间结果都是几千行
——落地纯属浪费

三、拍平方案
三步合一：
· 筛选 → 内联条件
· 聚合 → 子查询/窗口
· 关联 → 一次完成
改写后：
一条查询，
优化器统筹路径
结果：
1.5 秒 → 100 毫秒
（等价性验证：
　新旧结果逐行一致）

四、临时表的正当场景
（防矫枉过正）
场景 1 大中间结果多次复用：
　算一次落地，
　被三四个查询引用——
　比重复计算便宜
场景 2 跨语句流程：
　存储过程里的
　阶段性结果
场景 3 调试与分步验证：
　复杂逻辑拆解时
　的临时手段
原则：
落地是为了复用或必要，
不是为了「看起来清晰」

五、评审规矩
新增临时表要回答：
1 中间结果多大？
2 被复用几次？
3 能否一条查询完成？
三问答不上来，
先别落地

六、结果
报表提速 15 倍；
「临时表三连」的写法
在评审里绝迹；
正当场景的临时表
反而用得更明白了。`,
  stats2: [['耗时', '1.5s→100ms'], ['三连', '拍平'], ['边界', '三问制']],
  qa: [
    ['临时表的「物化代价」具体贵在哪？','贵在两次数据搬运与一次优化机会的丧失。物化意味着中间结果要实际写入（占内存或落磁盘），后续步骤再读出来——数据的每一米路程都有成本。更隐蔽的是：查询被切成独立步骤后，优化器无法把筛选条件下推过临时表的边界、无法选择全局最优的连接顺序。临时表不只收搬运费，还收「优化视野变窄」的隐性税。'],
    ['「中间结果大小」为什么是落地与否的分水岭？','因为落地的收益和成本都随大小变化。小结果在内存里流转几乎免费，落地是纯亏；大结果如果不落地，每次引用都要重算一遍，重算的成本可能远超一次落地。所以判断公式是：落地成本（一次写+多次读）vs 重算成本（每次引用算一遍）。引用次数多、结果集大，落地划算；否则一条查询贯通到底。'],
    ['「看起来清晰」为什么不是用临时表的正当理由？','因为清晰是给人看的需求，落地是给机器执行的代价——用执行代价换阅读体验，方向就错了。复杂查询的可读性有更好的解法：通用表表达式（书写上分步、执行上统一）、注释、视图封装。这些手段给人分步的清晰，给优化器完整的视野。代码清晰有很多条路，牺牲性能的这条永远不该是首选。'],
  ],
  fbs: [
    ['开发', '把三步临时表拍平成一条查询后，报表快了一个数量级'],
    ['评审者', '三问制让临时表的使用有了门槛'],
    ['新同事', '通用表表达式写法既清晰又不落地'],
  ],
  risks: ['把复杂查询拆成「一串临时表」看似清晰，实则每一步都在付落地的税', '小中间结果禁止落地', '落地决策看大小与复用次数', '可读性用书写结构解决不用执行代价'],
}),
({
  t: '大事务拆分 · 小步提交', sub: '12 分钟写完', d: '拆分大事务：小步提交与一致性设计',
  ly: 'terminal', pal: 'green', bg: 'dark',
  facts: ['输入：大事务逻辑', '输出：拆分方案', '事务 1→8'],
  inp: '粘贴事务逻辑', out: ['结构：事务边界重划', '关键：步间一致性', '配套：补偿机制'],
  stats: [['事务', '120 秒→8 个小步'], ['锁', '持有大降'], ['补偿', '对账兜底']],
  quote: '大事务拆分不是把一致性拆没：是把「一个长承诺」换成「一串短承诺加对账」——锁松了，账还得平',
  fb: ['开发', '拆分后月度结算从卡死两小时变成平稳跑完，锁等待清零'], risk: '不做步间一致性设计就拆事务，等于把原子性扔了只留下碎片',
  ctx: '月度结算事务跑 120 秒，期间锁住核心表，其他业务排队。拆分方案：按业务步骤切成 8 个小事务（每步独立提交）、步骤间用状态字段衔接、失败步支持重入、全量对账兜底。拆分后每个事务几百毫秒，锁等待清零，且一致性有对账保障。',
  raw: `大事务现状：
月度结算一个事务：
1 汇总各渠道数据
2 计算分账
3 更新订单状态
4 写结算单
5 更新账户余额
…8 个步骤
一个事务包到底
——120 秒持锁

后果：
· 期间核心表写阻塞
· 失败全部回滚重来
· 回滚段与日志压力大`,
  steps: [
    ['画边界', '步骤与依赖梳理', '3 分钟'],
    ['切事务', '小事务划分', '3 分钟'],
    ['设衔接', '状态字段串联', '2 分钟'],
    ['配重入', '失败步可重跑', '2 分钟'],
    ['做对账', '一致性兜底', '2 分钟'],
    ['验全程', '拆分后演练', '1 分钟'],
  ],
  full: `【拆分 · 大事务（小步提交）】

一、事务边界的判据
拆分的依据不是
「代码长短」，
是业务的一致性单元：
· 必须同生共死的操作
　留在一个事务
· 可以「先记一步、
　稍后继续」的操作
　允许分开
本例八个步骤里，
真正必须原子的
只有两三个相邻操作，
其余都可分步

二、小事务划分
拆成 8 个小事务：
每个事务只做一件事：
　汇总 / 分账 / 状态更新 /
　写单 / 记账…
每个事务时长：
几百毫秒
锁的持有从
「120 秒全程」
变成「每步一借一还」

三、步间衔接（状态字段）
· 结算主记录带状态：
　已汇总 → 已分账 →
　已记账 → 完成
· 每步提交时
　推进状态
· 中断后凭状态
　知道「走到哪了」
状态字段是拆分后的
「事务记忆」：
原子性从数据库层
转移到流程层

四、重入设计
· 每步幂等：
　重跑同一步结果不变
　（有唯一键/状态检查）
· 失败恢复：
　从当前状态继续，
　不必从头
· 演练验证：
　人为在第 3 步中断，
　重跑后结果正确

五、对账兜底
拆分后一致性靠三层：
1 步内：
　小事务原子
2 步间：
　状态推进 + 重入
3 全局：
　定时对账
　（结算单与账户流水
　笔笔核对）
对账是最终防线：
任何缝隙里的不一致，
都会被它捞出来

六、拆分后的世界
· 锁等待清零：
　没有长事务占着核心表
· 失败代价变小：
　回滚只回一小步
· 可观测性变好：
　每步有状态有日志
代价：
一致性从「免费原子」
变成「设计出来的流程」
——这是拆分必须付的账

七、结果
结算平稳完成；
锁等待清零；
对账上线以来
零不一致。`,
  stats2: [['事务', '120s→8 小步'], ['锁', '清零'], ['对账', '零差异']],
  qa: [
    ['拆大事务为什么必须同时设计「步间一致性」？','因为事务的核心价值是原子性：要么全成、要么全不。把一个大事务切成八个小事务后，「全成或全不」的保证被切断了——第 3 步成功、第 4 步失败时，数据停在一个中间状态。如果不设计衔接（状态字段）与补偿（重入/对账），拆分就是把一致性直接扔掉。拆事务的本质是：把数据库免费给的原子性，换成自己设计维护的一致性机制。'],
    ['「状态字段」在拆分方案里承担什么角色？','它是流程的记忆。大事务不需要记忆——要么全做完，要么当没发生。拆分后的流程会在任意一步中断，恢复时必须知道「走到哪了、下一步是什么」。状态字段把执行进度持久化下来：中断重跑时读状态定位，完成一步就推进一格。它本质上是把一个隐式的数据库事务，显式化成一台可中断、可恢复的状态机。'],
    ['对账为什么是「最终防线」而不是可选配置？','因为步间机制（状态+重入）覆盖的是「已知的失败模式」：中断、重试、超时。但总有意想不到的缝隙：极端并发下的状态竞争、外部系统半成功、代码缺陷。对账不关心过程发生了什么，它只看终态：两边的账平不平。这种「结果导向」的检查能兜住一切过程缺陷。分布式一致性的常识是：过程保证再精密，也要配结果核对——前者防常见，后者防万一。'],
  ],
  fbs: [
    ['开发', '拆分后月度结算从卡死两小时变成平稳跑完，锁等待清零'],
    ['运维', '失败恢复从「全部重来」变成「断点继续」'],
    ['财务', '对账机制让拆分后的账目更放心'],
  ],
  risks: ['不做步间一致性设计就拆事务，等于把原子性扔了只留下碎片', '拆分依据是业务一致性单元不是代码长短', '每步必须幂等可重入', '对账是不可省略的最终防线'],
}),
({
  t: '视图滥用 · 查询展开', sub: '8 分钟写完', d: '优化视图查询：展开逻辑与谓词下推',
  ly: 'code', pal: 'purple', bg: 'paper',
  facts: ['输入：视图查询', '输出：优化方案', '提速 30 倍'],
  inp: '粘贴视图查询', out: ['结构：视图展开分析', '关键：谓词下推检查', '配套：视图治理'],
  stats: [['耗时', '2.1s→70ms'], ['病因', '谓词未下推'], ['治理', '视图清单']],
  quote: '视图是别名不是屏障：写查询时要记得它会被展开——谓词推不进去的视图，就是性能陷阱',
  fb: ['开发', '展开视图重写后快了三十倍，原来谓词全被挡在视图外面'], risk: '把视图当黑盒用的团队，性能出问题时连「里面到底执行了什么」都看不见',
  ctx: '一条查视图的语句 2.1 秒，外面看只是个简单筛选。展开视图定义发现：外层的时间过滤条件没能推进视图内部（谓词无法下推），视图里先全量聚合再筛选。改写为直接写底层表、条件前置，70 毫秒。随后盘点视图使用：哪些是真需求（复用逻辑），哪些是偷懒（该展开）。',
  raw: `慢查询：
SELECT * FROM 统计视图
WHERE 日期 = '某天'
——看起来很简单

视图内部：
对全量明细做聚合
外层过滤没能
推进视图内部
（谓词下推失败）
——先算全量，
再筛一天

结果：
为一天数据算了
全表的聚合
2.1 秒`,
  steps: [
    ['展视图', '展开视图定义', '2 分钟'],
    ['查下推', '谓词是否推进', '2 分钟'],
    ['找障碍', '下推失败原因', '2 分钟'],
    ['写改写', '条件前置重写', '2 分钟'],
    ['盘视图', '全库使用盘点', '1 分钟'],
    ['定策略', '视图使用规范', '1 分钟'],
  ],
  full: `【优化 · 视图（查询展开）】

一、视图的真相
视图不是存储的结果，
是保存的查询：
使用时把定义
「展开」进外层查询
所以查视图慢，
要先问：
展开后到底执行了什么？
（把视图定义
　替换进查询，
　看全貌）

二、谓词下推检查
理想情况：
外层条件「日期=某天」
被推进视图内部，
先过滤再聚合
实际情况：
某些结构会挡住下推——
· 视图内有聚合
　（分组之后的筛选
　无法推到分组之前）
· 视图内有去重/窗口
· 视图内有限制行数
本例命中：
聚合挡住了下推，
先全量聚合再筛一天

三、下推失败的信号
执行计划里的迹象：
· 视图内层扫描了
　远超外层条件范围
　的数据
· 过滤发生在
　聚合之后
· 外层条件「悬空」
　没进内层
看到「先算大的再筛小的」
的形状，
怀疑下推失败

四、改写方案
把视图展开重写：
· 直接查底层表
· 过滤条件放在
　聚合之前
　（先筛一天，
　再聚合一天）
结果：
2.1 秒 → 70 毫秒
语义不变
（逐行比对验证）

五、视图治理（防复发）
盘点视图使用：
· 复用型视图：
　逻辑确实要共享——
　保留，但标注
　「注意谓词下推」
· 偷懒型视图：
　只是懒得写关联——
　热点查询直接写开
规范：
1 高频热点查询
　不用复杂视图
2 视图内聚合/去重
　要在文档标注
3 视图查询慢了，
　第一步永远是展开看

六、结果
热点查询提速 30 倍；
视图清单标注了
每个视图的
「下推友好度」。`,
  stats2: [['耗时', '2.1s→70ms'], ['病因', '下推失败'], ['治理', '友好度标注']],
  qa: [
    ['为什么「视图内有聚合」会挡住谓词下推？','因为聚合改变了数据的粒度：聚合前的行与聚合后的行不是一一对应的。外层的过滤条件作用在聚合结果上（比如筛选某一天的汇总值），它无法安全地翻译成聚合前的过滤——聚合前的「某天」和聚合后的「某天」语义可能不同（想想跨天订单的归属）。优化器不敢冒险替你改语义，于是放弃下推，先算全量再筛。这是语义安全对性能的让步，理解它才能知道何时该手工改写。'],
    ['「先算大的再筛小的」为什么是危险形状？','因为成本与参与计算的行数成正比。先过滤后聚合：聚合处理一天的数据；先聚合后过滤：聚合处理全表的数据，然后扔掉 99%。同样的业务问题，两种顺序的成本差几个数量级。查询优化的很多技巧（谓词下推、过滤前置、小表驱动）本质上是同一件事：让数据在变贵之前先变少。看到执行计划里数据量「先膨胀后收缩」，就该警觉。'],
    ['视图的「复用价值」和「性能陷阱」怎么平衡？','分层对待：低频的管理查询、权限收口场景，视图的复用价值大于性能顾虑，放心用；高频热点路径上的查询，性能是第一位的，视图的抽象要让位于可控性——直接写开，把过滤条件放在该在的位置。视图清单标注「下推友好度」就是把这个平衡显式化：每个视图的使用者都知道它是省心的工具还是潜在的坑。'],
  ],
  fbs: [
    ['开发', '展开视图重写后快了三十倍，原来谓词全被挡在视图外面'],
    ['评审者', '视图清单的下推友好度标注很实用'],
    ['新同事', '「查视图慢先展开」成了条件反射'],
  ],
  risks: ['把视图当黑盒用的团队，性能出问题时连「里面到底执行了什么」都看不见', '视图内聚合会挡住谓词下推', '热点查询慎用复杂视图', '视图查询慢先展开看全貌'],
}),
({
  t: '字符集混用 · JOIN 失效', sub: '8 分钟写完', d: '排查字符集问题：JOIN 失效与乱码根因',
  ly: 'kanban', pal: 'red', bg: 'dots',
  facts: ['输入：异常现象', '输出：排查方案', '修复 3 处'],
  inp: '粘贴异常现象', out: ['结构：字符集比对链', '关键：连接层排查', '配套：统一规范'],
  stats: [['修复', '3 处'], ['病因', '字符集混用'], ['规范', '全链统一']],
  quote: '字符集问题的排查链：连接、库、表、列四层都看一眼——乱码和索引失效，往往藏在层与层的缝隙里',
  fb: ['开发', '统一字符集后，诡异乱码和莫名慢查询一起消失了'], risk: '字符集不统一的系统，乱码是明伤，隐式转换毁索引是暗伤——暗伤更难防',
  ctx: '两个怪现象同时出现：某字段偶尔乱码；一个有关联列索引的查询莫名全表扫描。排查发现同一条根因链：历史表用了旧字符集，与新表关联时触发隐式转换（索引失效），某些特殊字符在转换中损坏（乱码）。统一字符集（库/表/列/连接四层对齐），两个问题同时消失。',
  raw: `异常现象：
1 部分用户昵称乱码
  （生僻字、表情符号）
2 关联查询有索引
  却全表扫描

排查线索：
· 老表：历史默认字符集
· 新表：统一后的新字符集
· 两表关联时：
  字符集不一致 →
  隐式转换 →
  索引失效
· 乱码：
  旧字符集存不下
  某些字符`,
  steps: [
    ['理链路', '四层字符集盘点', '2 分钟'],
    ['查乱码', '存储层字符覆盖', '2 分钟'],
    ['查失效', '关联隐式转换', '2 分钟'],
    ['定统一', '目标字符集选择', '2 分钟'],
    ['做迁移', '在线转换方案', '1 分钟'],
    ['立规范', '建表默认约束', '1 分钟'],
  ],
  full: `【排查 · 字符集混用】

一、字符集的四层链路
一个字符串经过：
连接层（客户端会话）
→ 库默认
→ 表默认
→ 列定义
任何一层不一致，
都可能在存储或
比较时触发转换
排查口诀：
四层都看，
从列往上查

二、乱码根因（存储层）
旧字符集容量有限：
· 存不下生僻字与表情
· 写入时被替换成
　「未知字符」
· 读出来就是乱码/问号
确认方法：
查看列的字符集定义 +
尝试写入边界字符
乱码不是显示问题，
是存储容量的事

三、JOIN 失效根因（比较层）
两列字符集不同：
关联比较时，
一侧要转换到
另一侧的字符集——
转换发生在列上 =
隐式转换 =
索引失效
（与「数字比字符串」
　是同一类病：
　列被函数包住，
　索引认不出）
这就是「有索引却
　全表扫描」的
又一元凶

四、统一方案
目标：
全链统一为
支持完整字符的
新字符集
· 库默认改为新字符集
· 新表新列自动继承
· 连接层配置对齐
· 存量表逐个转换
　（在线转换：
　新建列/表 →
　回填 → 切换）

五、迁移注意
· 转换是重写数据，
　大表分批进行
· 转换前后抽样比对
　（确认无字符丢失）
· 索引随转换重建
· 低峰期执行，
　双写过渡

六、规范固化
· 建表模板固定字符集
· 连接配置进检查清单
· 评审禁止
　「随手建表用默认」
（历史教训：
　「默认」会变，
　写死才稳）

七、结果
乱码消失；
关联查询恢复索引探测；
两个「灵异问题」
同根同源，一次根治。`,
  stats2: [['修复', '3 处统一'], ['双杀', '乱码+失效'], ['规范', '写死默认']],
  qa: [
    ['为什么字符集不一致会导致索引失效，机制和隐式类型转换一样？','因为索引认的是列里存储的原始字节序列。两列字符集不同时，比较前必须把一侧转换到统一字符集——这个转换作用在列值上，等于给每行包了一层函数。索引里存的是转换前的值，拿转换后的值去查自然对不上，只能逐行扫描。无论触发转换的原因是类型不匹配还是字符集不匹配，失效机制是同一个：列的原始形态被改变了。'],
    ['乱码问题为什么「往往是存储容量的事」而不是显示的事？','因为真正的乱码分两种：传输/显示层的乱码（数据没坏，只是解码方式不对，换个客户端就能看对）和存储层的乱码（字符在写入时就因为目标字符集存不下而被替换成占位符，数据已经损坏，无法恢复）。前者可逆，后者不可逆——旧字符集存不下的字符，写进去的那一刻就丢了。所以乱码排查第一步是判断数据坏没坏：同一份数据在不同客户端都错，大概率是存储层的事。'],
    ['存量表的字符集迁移为什么要「在线转换」而不是直接改？','因为字符集转换是重写整张表的每一行数据（每个字符串都要转码重存），对大表来说意味着长时间的锁、大量的日志、索引全部重建。直接改表定义会把这个过程压成一个长事务。在线转换的思路是把代价摊开：新结构影子表、分批回填、双写过渡、最后切换——每一步都是小事务，业务无感。数据结构的变更，规模和风险成正比，方案要跟着规模走。'],
  ],
  fbs: [
    ['开发', '统一字符集后，诡异乱码和莫名慢查询一起消失了'],
    ['运维', '存量表分批转换，业务无感'],
    ['评审者', '建表模板固定字符集，杜绝随手默认'],
  ],
  risks: ['字符集不统一的系统，乱码是明伤，隐式转换毁索引是暗伤——暗伤更难防', '四层链路全部对齐才算统一', '存量转换走在线方案', '建表默认必须写死'],
}),
({
  t: '物化视图 · 刷新策略', sub: '10 分钟写完', d: '设计物化视图：刷新策略与一致性',
  ly: 'sheet', pal: 'gold', bg: 'linen',
  facts: ['输入：聚合需求', '输出：物化方案', '策略 3 选 1'],
  inp: '粘贴聚合需求', out: ['结构：刷新策略选型', '关键：新鲜度承诺', '配套：重建兜底'],
  stats: [['刷新', '增量'], ['新鲜度', '明确承诺'], ['重建', '兜底']],
  quote: '物化视图的本质交易：用「数据可能旧一点」换「查询快得多」——交易条款（刷新策略）必须写清楚',
  fb: ['数据组', '增量刷新让看板既快又够新，刷新日志让新鲜度可查证'], risk: '不声明新鲜度的物化视图，用户拿着昨天的数据做今天的决策，出了错都不知道错在哪',
  ctx: '高频聚合查询需要加速，又不想自己维护汇总表。评估物化视图：对比三种刷新策略（全量刷新/增量刷新/实时维护），按「看板场景可接受 10 分钟延迟」选定增量刷新，明确新鲜度承诺并展示在看板上，配全量重建兜底。查询从秒级到毫秒级，且数据时效透明。',
  raw: `需求：
经营看板聚合查询
· 查询频繁（秒级响应要求）
· 数据可接受短暂延迟
  （10 分钟内）

候选方案：
1 普通视图：
  每次现算——太慢
2 物化视图：
  预存结果——快，
  但要解决「怎么更新」
3 自建汇总表：
  灵活但维护成本高

物化视图的核心问题：
刷新策略怎么选？
新鲜度承诺多少？`,
  steps: [
    ['定需求', '新鲜度与查询频率', '2 分钟'],
    ['评三策', '刷新策略对比', '3 分钟'],
    ['选增量', '增量机制确认', '2 分钟'],
    ['标新鲜', '时效承诺展示', '2 分钟'],
    ['配重建', '全量重建兜底', '1 分钟'],
    ['做监控', '刷新状态观测', '1 分钟'],
  ],
  full: `【设计 · 物化视图（刷新策略）】

一、本质交易（先想清楚）
物化视图 = 预存的查询结果
得到的：查询速度
付出的：数据新鲜度
（结果可能落后于源数据）
所以设计的第一件事：
这个场景能接受
多旧的数据？
新鲜度是条款，
不是副作用

二、三种刷新策略
策略 1 全量刷新：
　定期整个重算
　· 简单可靠
　· 数据量大时代价高
　· 适合：
　　数据量可控 +
　　新鲜度要求低（天级）
策略 2 增量刷新：
　只算变化的部分
　· 依赖变更日志/时间戳
　· 快，但机制复杂
　· 适合：
　　大数据量 +
　　分钟级新鲜度
策略 3 实时维护：
　写入时同步更新
　· 最新鲜
　· 写入路径变重
　· 适合：
　　强一致要求的关键指标
本例：
数据量大 + 10 分钟容忍
→ 增量刷新

三、增量机制要点
· 变更记录驱动：
　上次刷新位点之后的
　变更聚合进结果
· 刷新幂等：
　重跑同一区间结果不变
· 刷新失败：
　位点不前移，
　下次补上
　（不丢区间）

四、新鲜度承诺（透明化）
· 承诺写明：
　「数据延迟不超过 10 分钟」
· 看板展示：
　「数据更新至某时某分」
· 超期告警：
　刷新卡住超过承诺 →
　报警
时效透明是物化视图
的道德底线：
用户有权知道
自己看的是多新的数据

五、全量重建兜底
· 增量链路复杂，
　可能积累误差
　（日志缺失、边界问题）
· 定期全量重建校准
　（每周/每月）
· 重建后与增量结果
　抽样比对
增量跑日常，
全量保底线

六、监控三看
1 刷新是否按时跑
2 刷新耗时趋势
　（变长 = 数据在涨）
3 延迟是否在承诺内

七、结果
查询毫秒级；
新鲜度承诺兑现可查；
物化视图用得明白。`,
  stats2: [['策略', '增量'], ['承诺', '10 分钟'], ['兜底', '定期全量']],
  qa: [
    ['三种刷新策略的选择依据是什么？','两个坐标：数据量决定全量刷新是否可行（数据小，全量最省心），新鲜度要求决定必须多勤快（天级容忍选全量定时，分钟级选增量，强一致选实时维护）。本质是在「计算成本」与「数据时效」之间选平衡点——没有普适最优，只有场景匹配。选型时把场景的两个坐标值写出来，答案基本就出来了。'],
    ['增量刷新的「位点不前移」为什么是关键设计？','因为它保证刷新失败不丢数据。增量刷新按区间处理：从上次位点到当前。如果一次刷新失败了但位点照样前移，这段区间的数据就永远不会被算进结果——静默的数据缺口。位点只在成功后前移，失败则原地不动、下次补跑，最坏情况是重复处理（靠幂等消化）而不是遗漏。宁可重复，不可丢失——这是所有增量管道的通用纪律。'],
    ['为什么物化视图必须配「定期全量重建」？','因为增量链路是长链条：依赖变更日志的完整性、位点的正确性、边界条件的处理——任何一环出小问题，误差就会累积进物化结果，而且很难被发现（结果看起来都挺正常）。全量重建是「推倒重算」的校准：不管增量过程积累了什么偏差，重建后一切归零对齐。增量负责日常的快，全量负责定期的准，两者互为保险。'],
  ],
  fbs: [
    ['数据组', '增量刷新让看板既快又够新，刷新日志让新鲜度可查证'],
    ['产品', '「数据更新至某分」的标注让时效不再含糊'],
    ['运维', '刷新超期告警拦过一次管道故障'],
  ],
  risks: ['不声明新鲜度的物化视图，用户拿着昨天的数据做今天的决策，出了错都不知道错在哪', '刷新失败位点不前移', '定期全量重建校准', '刷新延迟超承诺必须告警'],
}),
];
