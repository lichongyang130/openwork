/* 「SQL 写与优化」深度版案例（6-10） */

export const DEEP_SQL_2 = [
({
  t: '批量插入 · 分片提交', sub: '8 分钟写完', d: '优化批量写入：分批提交与错误隔离',
  ly: 'terminal', pal: 'cyan', bg: 'grid',
  facts: ['输入：批量写入', '输出：优化方案', '提速 50 倍'],
  inp: '粘贴写入逻辑', out: ['结构：分片批量提交', '关键：单片失败隔离', '配套：进度可恢复'],
  stats: [['插入', '50 万条'], ['批次', '每批 2000'], ['提速', '50 倍']],
  quote: '批量写入的两难：一条条插太慢，一口气全插太险——分片提交是速度与安全的交点',
  fb: ['开发', '50 万条数据从四十分钟缩到不到一分钟，而且中间断了能接着来'], risk: '单条循环插入是性能灾难，超大批单事务是锁灾难——两个极端都通向事故',
  ctx: '导入 50 万条数据，逐条插入要 40 分钟。改成批量：每 2000 条合成一条批量插入、每批独立事务（一批失败不影响其他批）、失败批次记录后重试、进度留档支持断点续跑。全程 50 秒，且中途断网也能接着跑。',
  raw: `写入任务：
导入 50 万条记录

逐条插入的问题：
· 每条一次网络往返
· 每条一次事务开销
· 40 分钟起步

超大单事务的问题：
· 长事务占锁
· 失败全部回滚重来
· 日志压力陡增

目标：
快（批量）
稳（失败隔离）
可恢复（断点续跑）`,
  steps: [
    ['定批大小', '批量与事务权衡', '2 分钟'],
    ['写批量', '批量插入语句', '2 分钟'],
    ['配事务', '每批独立提交', '2 分钟'],
    ['隔失败', '失败批次记录重试', '2 分钟'],
    ['留进度', '断点续跑支持', '1 分钟'],
    ['测压', '批次大小调优', '1 分钟'],
  ],
  full: `【优化 · 批量插入（分片提交）】

一、批大小的选择
· 每批 2000 条
　（经验起点，实测微调）
· 权衡维度：
　批量越大 →
　往返与事务开销越省；
　但单批失败代价越大、
　内存与日志压力越高
· 实测方法：
　1000/2000/5000 各跑一轮，
　看吞吐与失败率
批大小不是信仰，
是测量出来的参数

二、批量语句
· 多值插入合并为
　一条语句
　（一次往返插 2000 行）
· 或驱动层批量接口
　（效果类似）
· 语句长度控制：
　避免超长的单条语句
　触发限制

三、每批独立事务（关键）
· 一批一个事务，
　提交后再开下一批
· 好处：
　锁持有时间短、
　失败只回滚一批、
　日志可分段
· 对比单大事务：
　长事务的锁与回滚段
　压力都被化解
原则：
批量是语句的事，
小事务是提交策略的事，
两者不矛盾

四、失败隔离与重试
· 单批失败：
　记录批次范围与原因，
　继续下一批
· 失败清单收尾重试
　（可能是脏数据批：
　逐条定位问题行）
· 脏数据行单独报告，
　不阻塞整体
（批量的容错设计：
　整体进度不因局部
　问题停摆）

五、断点续跑
· 进度档案：
　已完成批次号
· 重跑跳过已完成批
· 幂等保证：
　目标表有唯一键，
　重复批走冲突处理
　（跳过或更新）

六、结果
40 分钟 → 50 秒；
中途断网重跑，
从断点继续；
三批脏数据被隔离
定位，其余无损导入。`,
  stats2: [['耗时', '40 分→50 秒'], ['隔离', '批级'], ['续跑', '断点']],
  qa: [
    ['「每批独立事务」相比一个大事务好在哪？','好在把风险与资源都切小了。大事务意味着：锁持有到最后一次插入（阻塞其他写入）、失败时全部回滚（几十分钟白干）、回滚段与日志持续膨胀。分批提交后：锁是短暂借用、失败只损失一批、日志可分段落盘。本质上是用「多次小承诺」替代「一次大赌注」——每个承诺都可验证、可回退，整体反而更稳。'],
    ['批大小为什么必须实测而不是抄经验值？','因为它同时被太多变量影响：行宽（每行多大）、索引数量（每插入一行要维护几个索引）、网络往返成本、数据库的日志配置。别人的 5000 在你的表结构下可能触发语句长度限制，你的 1000 在万兆内网下可能太保守。经验值只是起点，真实环境跑一轮吞吐曲线才是答案——调优的参数，要长在自已的系统上。'],
    ['「脏数据批逐条定位」的设计意图？','批量插入的失败有个特点：一批 2000 条里可能只有 1 条有问题，但失败的是整批。如果直接把整批标记失败，1999 条好数据也进不去；如果逐条重试整批，又浪费。标准做法是：失败批进入「逐条模式」重新插入——好数据照常入库，坏数据被精确定位并报告。批量要效率，定位要精度，两全的方案就是分层处理。'],
  ],
  fbs: [
    ['开发', '50 万条数据从四十分钟缩到不到一分钟，而且中间断了能接着来'],
    ['数据组', '脏数据被逐条定位，修正后二次导入'],
    ['运维', '短事务让导入期间库压力平稳'],
  ],
  risks: ['单条循环插入是性能灾难，超大批单事务是锁灾难——两个极端都通向事故', '批大小要实测不要照抄', '失败批要隔离重试不阻塞整体', '断点续跑依赖幂等键'],
}),
({
  t: '去重查询 · 窗口函数', sub: '10 分钟写完', d: '编写去重查询：窗口函数取每组最新',
  ly: 'code', pal: 'orange', bg: 'paper',
  facts: ['输入：去重需求', '输出：优化后 SQL', '提速 20 倍'],
  inp: '粘贴去重需求', out: ['结构：窗口函数编号', '关键：分区排序键', '配套：索引配合'],
  stats: [['耗时', '900ms→45ms'], ['写法', '窗口编号'], ['并列', '显式处理']],
  quote: '「每组取最新」的去重题，窗口函数是最直白的解法：先编号，再取第一——把排序的语义写进查询里',
  fb: ['开发', '窗口函数版不仅快了二十倍，读代码的人一眼就懂在干什么'], risk: '用自关联做去重，数据量一大就是平方级噩梦——而且没人读得懂它在干什么',
  ctx: '需求：每个用户只取最新一条记录。老写法是自关联（不存在更新的记录），数据量上来后 900 毫秒且极难读。改用窗口函数：按用户分区、按时间倒序编号，取编号为 1 的行。配合 (用户, 时间) 索引，45 毫秒，语义一目了然。还顺手处理了时间并列的决胜键。',
  raw: `需求：
用户行为表，
每个用户取最新一条

旧写法（自关联）：
SELECT * FROM t a
WHERE NOT EXISTS (
  SELECT 1 FROM t b
  WHERE b.user_id = a.user_id
  AND b.time > a.time
)
——数据量大后 900 毫秒，
且没人能一眼读懂

窗口函数思路：
按用户分区
按时间倒序编号
取编号 = 1`,
  steps: [
    ['选函数', '窗口编号函数', '2 分钟'],
    ['定分区', '分区键与排序键', '2 分钟'],
    ['写查询', '编号后过滤', '2 分钟'],
    ['配索引', '分区排序联合索引', '2 分钟'],
    ['决并列', '决胜键处理', '1 分钟'],
    ['验计划', '执行计划确认', '1 分钟'],
  ],
  full: `【优化 · 去重查询（窗口函数）】

一、窗口函数写法
SELECT * FROM (
  SELECT *,
    ROW_NUMBER() OVER (
      PARTITION BY user_id
      ORDER BY time DESC, id DESC
    ) AS 序号
  FROM t
) 派生
WHERE 序号 = 1
语义直译：
「按用户分组，
　组内按时间倒序编号，
　取每组 1 号」
——查询即规格，
读一遍就懂

二、三个函数的选择
· 行号函数（严格编号）：
　并列也分先后——
　去重取一的标准选择
· 排名函数（并列同名次）：
　会返回多条并列第一，
　不符合「只取一条」
· 稠密排名：
　用于名次场景，
　此处不适用
选择依据：
需求要「恰好一条」，
所以用严格编号

三、排序键与并列决胜
ORDER BY time DESC, id DESC
· 主排序：时间倒序
· 决胜键：id 倒序
　（时间完全相同时，
　用 id 定胜负）
没有决胜键的排序
是不确定排序：
每次执行结果可能不同
——去重结果必须稳定

四、索引配合
联合索引 (user_id, time, id)：
· 分区列在前：
　同一用户的数据相邻
· 排序列跟上：
　组内已有序，
　编号顺势完成
执行计划验证：
索引有序扫描，
无额外排序

五、与旧写法对比
自关联版：
· 语义绕（不存在更优者）
· 性能平方级
　（每行都要比一圈）
窗口版：
· 一次扫描 + 编号
· 线性成本
· 可读性碾压
性能：900 毫秒 → 45 毫秒

六、适用与边界
· 适合：
　每组取前 N 条
　（把条件改成 序号 ≤ N）
· 超大数据集注意：
　窗口函数需要物化
　编号结果，
　先用过滤缩小范围
　再开窗

七、结果
查询提速 20 倍；
「每组取最新」类需求
统一收敛到这一模板。`,
  stats2: [['耗时', '900ms→45ms'], ['语义', '查询即规格'], ['并列', '决胜键']],
  qa: [
    ['窗口函数版相比自关联版的本质优势是什么？','是算法复杂度的代差。自关联的逻辑是「对每一行，检查是否存在更好的行」——每行都要和同组其他行比一圈，整体是平方级；窗口函数是「排好序，依次编号」——一次有序扫描，线性完成。数据量小的时候两者都够用，数据量上来后差距是数量级的。去重查询的选型，本质是在选算法。'],
    ['为什么必须显式加「决胜键」？','因为排序不稳定性会传染给结果。如果多条记录时间完全相同，「按时间倒序」没有规定它们的先后——数据库可以任意安排，而且不同次执行可能安排不同。今天取的是这条，明天可能变成那条。加上决胜键（如主键倒序），排序成为全序，去重结果才具备确定性。可复现的查询，排序必须无歧义。'],
    ['「取前 N 条」为什么只是把条件改成「序号 ≤ N」？','因为窗口函数把「组内排序位置」显式物化成了列。取最新一条是序号=1，取最新三条是序号≤3，取最新三条且跳过第一条是序号在2到4之间——需求的变化只是过滤条件的变化，查询结构纹丝不动。这就是「把语义写进结构」的好处：能力随需求伸缩，而不需要换一种完全不同的写法。'],
  ],
  fbs: [
    ['开发', '窗口函数版不仅快了二十倍，读代码的人一眼就懂在干什么'],
    ['评审者', '决胜键的加入让结果可复现了'],
    ['新同事', '「每组取前 N」模板直接套用'],
  ],
  risks: ['用自关联做去重，数据量一大就是平方级噩梦——而且没人读得懂它在干什么', '排序必须带决胜键保证确定性', '选对编号/排名函数', '超大集合先过滤再开窗'],
}),
({
  t: '锁等待 · 事务瘦身', sub: '12 分钟写完', d: '排查锁等待：长事务识别与瘦身',
  ly: 'chat', pal: 'ink', bg: 'mesh',
  facts: ['输入：锁等待告警', '输出：排查与方案', '等待清零'],
  inp: '粘贴告警信息', out: ['结构：锁链定位', '关键：事务边界收缩', '配套：监控布防'],
  stats: [['等待', '47 次→0'], ['事务', '8 秒→80 毫秒'], ['监控', '长事务告警']],
  quote: '锁等待的元凶通常不是锁本身，而是「拿着锁做慢事」的长事务——瘦身事务，锁自然就松了',
  fb: ['开发', '把外部调用挪出事务后，锁等待告警一夜清零'], risk: '事务里包着外部接口调用的代码，等于拿着全楼的钥匙去楼下取快递',
  ctx: '数据库锁等待告警频发。排查锁链发现：一个事务里居然在调外部接口（几百毫秒到几秒），事务全程持锁。瘦身方案：事务边界收缩到只包数据库操作、外部调用移出事务、大事务拆小。同时布防长事务监控（超过 2 秒的事务告警）。治理后锁等待清零。',
  raw: `告警现象：
锁等待次数飙升，
高峰期大量请求排队

锁链排查：
等待者 → 持有者事务
发现持有者事务里：
1 更新订单行
2 调用外部风控接口
  （平均 600 毫秒，
  慢的时候 3 秒）
3 更新状态
4 提交
——第 1 步拿的锁，
要等第 4 步才放

事务时长：
平均 8 秒（高峰期）`,
  steps: [
    ['定锁链', '等待者持有者定位', '2 分钟'],
    ['审事务', '事务内动作盘点', '3 分钟'],
    ['移外调', '外部调用出事务', '3 分钟'],
    ['缩边界', '事务只包库操作', '2 分钟'],
    ['布监控', '长事务告警', '1 分钟'],
    ['验效果', '等待次数对比', '1 分钟'],
  ],
  full: `【排查 · 锁等待（事务瘦身）】

一、锁链定位（先找元凶）
从告警出发：
· 谁在等：
　等待的语句与资源
· 谁持有：
　顺着锁找到持有事务
· 持有者在干什么：
　查看该事务的
　已执行语句与时长
定位结果：
持有者事务里
藏着外部接口调用

二、事务盘点（病灶分析）
原事务流程：
1 更新订单行（拿锁）
2 调用风控接口
　（600 毫秒～3 秒，
　锁不放）
3 更新状态
4 提交（才放锁）
锁持有时长 =
整个事务时长——
网络一抖，
锁就跟着一抖

三、瘦身方案（核心）
原则：
事务里只做数据库的事
改造后流程：
1 调用风控接口
　（事务外，随便慢）
2 开启事务
3 更新订单行 + 状态
　（几十毫秒内完成）
4 提交
事务时长：
8 秒 → 80 毫秒
锁持有时间缩短百倍

四、通用瘦身清单
· 外部调用（接口、消息）
　一律出事务
· 大批量写入拆分
　（参考分片提交）
· 事务内不等人、不等锁
　以外的任何外部事件
· 「先查后改」时
　查询也尽量移出事务
　（只把「改」包进去）

五、监控布防（防复发）
· 长事务告警：
　超过 2 秒的事务
　实时告警
· 锁等待看板：
　等待次数与时长趋势
· 慢查询与长事务
　联合排查入口
纪律：
事务时长是核心指标，
和接口响应时间
一个待遇

六、结果
锁等待 47 次/天 → 0；
高峰期排队消失；
长事务监控拦住两次
「事务里加外部调用」
的回归。`,
  stats2: [['事务', '8s→80ms'], ['等待', '清零'], ['布防', '2 秒告警']],
  qa: [
    ['为什么锁的持有时间等于事务时长，而不是语句时长？','因为数据库的锁是跟着事务走的：事务内获取的锁，要到事务提交（或回滚）那一刻才统一释放。所以哪怕更新语句本身只要 5 毫秒，只要事务后面还挂着 3 秒的外部调用，这把锁就要陪跑 3 秒。理解这一点，事务瘦身的逻辑就通了：缩短锁持有的唯一办法，是缩短从「拿锁」到「提交」之间的全部时间——事务里装的每一件事，都在给锁计时。'],
    ['「外部调用出事务」的改造要注意什么一致性风险？','要注意调用顺序带来的语义变化。原逻辑是「改数据库→调外部」，改造后可能变成「调外部→改数据库」——如果外部调用成功而数据库写入失败，状态就不一致了。解法是按业务语义设计补偿：要么外部调用支持幂等重试，要么引入异步对账。事务瘦身不是简单搬代码，是把「强一致的事务边界」换成「明确设计的最终一致流程」——边界变了，一致性方案要跟着设计。'],
    ['长事务监控为什么值得当核心指标？','因为长事务是数据库问题的公共源头：它拉长锁持有期（锁等待）、拖住历史版本（存储膨胀）、阻塞结构变更、放大故障半径（回滚时间随事务时长增长）。几乎所有「数据库莫名其妙变慢」的现场，都能找到长事务的身影。给事务时长设告警，等于在源头装哨兵——它不针对某一种故障，它针对故障的温床。'],
  ],
  fbs: [
    ['开发', '把外部调用挪出事务后，锁等待告警一夜清零'],
    ['运维', '长事务看板成了每天必扫的页面'],
    ['评审者', '「事务里不许有外部调用」进了评审红线'],
  ],
  risks: ['事务里包着外部接口调用的代码，等于拿着全楼的钥匙去楼下取快递', '事务边界只包数据库操作', '瘦身后的一致性要重新设计', '长事务监控必须布防'],
}),
({
  t: 'NULL 陷阱 · 三值逻辑', sub: '8 分钟写完', d: '编写防 NULL 查询：三值逻辑与空值处理',
  ly: 'mind', pal: 'gold', bg: 'dots',
  facts: ['输入：含空值查询', '输出：修正后 SQL', '避坑 6 处'],
  inp: '粘贴查询语句', out: ['结构：空值语义显式化', '关键：三值逻辑规避', '配套：建模规范'],
  stats: [['陷阱', '6 类'], ['修正', '显式处理'], ['规范', '默认值制']],
  quote: 'NULL 不是 0，不是空字符串，是「不知道」——用「不知道」参与比较，得到的也是「不知道」',
  fb: ['开发', '一次统计口径事故后，全组的查询都开始显式处理空值'], risk: '把 NULL 当成普通值参与运算的查询，结果错得悄无声息——计数少一块，比较全失真',
  ctx: '统计报表数字对不上，排查发现是 NULL 的锅：不等于条件漏掉了空值行、聚合函数跳过空值、排序里空值位置不确定。系统梳理 6 类 NULL 陷阱（比较、聚合、分组、排序、连接、去重），逐个给出显式处理写法，并立建模规范：业务上「没有」的字段用默认值，真正「未知」才允许空。',
  raw: `事故：
「未支付订单」统计
与总数对不上

排查发现的陷阱：
1 WHERE 状态 != '已支付'
  ——状态为空的行
  被漏掉
2 SUM(金额) 跳过空值
  ——均值被抬高
3 排序中空值位置
  不确定
4 关联键有空值
  ——关联不上
5 去重时空值行为
6 分组时空值自成一类

根源：
NULL 是三值逻辑里的
「未知」，
不是普通的值`,
  steps: [
    ['讲逻辑', '三值逻辑原理', '2 分钟'],
    ['排陷阱', '六类场景盘点', '3 分钟'],
    ['给写法', '显式处理模板', '2 分钟'],
    ['定口径', '统计中的空值策略', '2 分钟'],
    ['立规范', '建模默认值制', '1 分钟'],
    ['进检查', '查询评审项', '1 分钟'],
  ],
  full: `【防坑 · NULL（三值逻辑）】

一、三值逻辑（原理）
NULL 的语义是「未知」：
· 任何与 NULL 的比较
　结果是「未知」，
　不是真也不是假
· 「未知」在过滤条件里
　等同于不通过
· 所以「不等于某值」
　捞不出空值行——
　「未知不等于已支付」
　还是「未知」

二、六类陷阱与写法
陷阱 1 不等于漏空值：
　错误：状态 != '已支付'
　修正：(状态 != '已支付'
　OR 状态 IS NULL)
　或：COALESCE(状态,'未知')
　!= '已支付'
陷阱 2 聚合跳过空值：
　求和与均值自动跳过空值
　——若空值应算 0，
　先 COALESCE 再聚合
陷阱 3 计数差异：
　计数(列) 不计空值，
　计数(*) 计行数——
　按需选择，写清楚
陷阱 4 关联键空值：
　空值键关联不上——
　先过滤或先填充
陷阱 5 去重：
　多个空值在去重里
　算一个——
　确认这是否符合预期
陷阱 6 分组：
　空值自成一组——
　报告中明确标注

三、空值判断的正确姿势
· 判断空：IS NULL
　（= NULL 永远不成立）
· 判非空：IS NOT NULL
· 填充：COALESCE(列, 默认)
· 条件填充：
　CASE WHEN 列 IS NULL
　THEN … 显式分支

四、统计口径策略
每个统计指标回答：
· 空值算不算进分母？
· 空值在求和里当什么？
口径写进指标文档，
查询按口径显式处理
（「未支付」的定义
　含不含空值？
　必须白纸黑字）

五、建模规范（治本）
· 业务语义是「没有」：
　用默认值
　（数量默认 0、
　标记默认否）
· 业务语义是「未知」：
　才允许空
　（如选填字段）
· 禁止用空值表达
　第三种业务状态
　（该加枚举值就加）
空值越少，
陷阱越少

六、结果
统计口径事故归零；
「显式处理空值」
成为查询评审固定项。`,
  stats2: [['陷阱', '6 类全解'], ['口径', '空值策略'], ['建模', '默认值制']],
  qa: [
    ['为什么「不等于某值」查不出空值行？','因为在三值逻辑里，空值参与比较的结果是「未知」，而过滤条件只保留「真」的行。「状态不等于已支付」对空值行求值是「未知不等于已支付」→「未知」→ 不保留。这是最反直觉的一点：不等于不是「等于的补集」，它的补集之外还有一块「未知」的灰色地带。凡是「不等于/大于/小于」类条件，都要问一句：空值行去哪了？'],
    ['「业务上没有用默认值，业务上未知才用空」这条规范解决什么？','解决空值的语义过载。当一个字段同时用空值表示「没有」「未设置」「数据缺失」三种含义时，任何统计都必须先猜它这次是哪种——猜错就是口径事故。默认值制把「没有」固化为具体的值（0、否、空集合），让空值只保留一种纯粹的语义：「此刻确实不知道」。语义越单一，处理规则越能统一写死，陷阱的生存空间就越小。'],
    ['聚合函数跳过空值会造成什么隐蔽错误？','最典型的是均值失真：求平均时跳过空值，等于悄悄改变了分母。100 个人里 60 个有评分、平均分 4.5——系统算出的均值是 4.5，但业务可能想要的是「含未评分按 0 算」的 2.7。两种口径都「合理」，但只有一种是业务要的。聚合里的空值处理没有默认正确答案，必须在指标定义时显式选择——这也是口径文档必须覆盖空值策略的原因。'],
  ],
  fbs: [
    ['开发', '一次统计口径事故后，全组的查询都开始显式处理空值'],
    ['数据组', '指标文档里的空值策略成了裁判依据'],
    ['新同事', '六类陷阱清单是现成的避坑地图'],
  ],
  risks: ['把 NULL 当成普通值参与运算的查询，结果错得悄无声息——计数少一块，比较全失真', '不等于条件必须考虑空值', '统计口径要写明空值策略', '建模时区分「没有」与「未知」'],
}),
({
  t: '全文搜索 · 索引选型', sub: '10 分钟写完', d: '编写搜索查询：全文索引选型与实现',
  ly: 'doc', pal: 'green', bg: 'paper',
  facts: ['输入：搜索需求', '输出：选型方案', '方案 3 选 1'],
  inp: '粘贴搜索需求', out: ['结构：需求分层选型', '关键：匹配语义定义', '配套：相关性排序'],
  stats: [['选型', '3 方案'], ['延迟', '200ms 内'], ['语义', '明确定义']],
  quote: '搜索选型的第一个问题不是「用什么引擎」，而是「用户搜什么、期待匹配到什么」——语义定了，技术选型自然收敛',
  fb: ['产品', '选型评估把「搜索到底要做到什么程度」第一次讲清楚了'], risk: '用模糊匹配硬扛搜索需求的系统，数据量一上来就是全表扫描的深渊',
  ctx: '商品搜索用模糊匹配实现，数据量上来后一次搜索要扫全表。按需求分层做选型：如果只需前缀与关键词匹配，数据库全文索引够用；如果要分词、纠错、相关性排序，上专业搜索引擎；折中方案是轻量索引+应用层组装。最终按「搜索是核心功能」定为专业引擎，并定义了匹配语义与排序规则。',
  raw: `搜索需求：
商品搜索
· 关键词匹配标题与描述
· 按相关度排序
· 响应 200 毫秒内
· 数据量千万级

现状：
模糊匹配 '%关键词%'
——无法用索引，
全表扫描

选型候选：
1 数据库全文索引
2 专业搜索引擎
3 轻量方案组合`,
  steps: [
    ['定语义', '匹配规则与排序', '3 分钟'],
    ['分需求', '核心与辅助功能', '2 分钟'],
    ['评三案', '各方案能力边界', '3 分钟'],
    ['算成本', '运维与复杂度', '2 分钟'],
    ['做验证', '候选方案压测', '1 分钟'],
    ['定演进', '分阶段路线', '1 分钟'],
  ],
  full: `【选型 · 全文搜索（索引选型）】

一、匹配语义先行（定需求）
搜索的本质是语义问题：
· 「苹果」要匹配
　「苹果手机」吗？
· 搜「无线耳机」要召回
　「蓝牙耳机」吗？
· 打错字要纠错吗？
· 排序按什么：
　相关度/销量/时间？
把答案写下来：
匹配字段、分词粒度、
同义扩展、纠错范围、
排序因子
——语义明确后，
选型才有判据

二、三方案能力边界
方案 1 数据库全文索引：
　适合：关键词匹配、
　中等数据量、
　已有数据库栈
　边界：分词弱、
　排序粗糙、
　中文支持看实现
方案 2 专业搜索引擎：
　适合：搜索是核心功能、
　需要分词/纠错/
　相关性调优、
　千万级以上
　边界：引入新组件、
　数据同步链路、
　运维成本
方案 3 轻量组合：
　适合：搜索是辅助功能、
　前缀匹配为主
　（前缀索引 +
　应用层过滤）
　边界：复杂查询乏力

三、决策矩阵
| 维度 | 全文索引 | 搜索引擎 |
| 匹配能力 | 中 | 强 |
| 排序能力 | 弱 | 强 |
| 运维成本 | 低 | 高 |
| 数据同步 | 无需 | 需要 |
本例结论：
搜索是核心功能 +
千万级 + 要求相关度
→ 专业搜索引擎
（判断依据全部留档）

四、同步链路设计
（选定搜索引擎后）
· 数据变更 → 增量同步
　到搜索索引
· 延迟要求声明：
　「上架后 N 秒内可搜到」
· 全量重建兜底：
　索引损坏可重建
搜索索引是副本，
一致性策略要设计

五、相关性与体验
· 排序因子组合：
　文本相关度 +
　业务权重（销量等）
· 无结果兜底：
　纠错建议、
　宽泛匹配提示
· 搜索日志留存：
　搜什么、点什么——
　优化依据

六、演进路线
阶段 1：
全文索引顶上
（先让搜索可用）
阶段 2：
搜索引擎替换
（平滑切换，双跑对比）
（选型不是一锤子：
　先用得起，再上得去）

七、结果
搜索延迟稳定 200 毫秒内；
「搜索要做到什么程度」
从此有文档可查。`,
  stats2: [['选型', '三案评估'], ['语义', '先行定义'], ['演进', '两阶段']],
  qa: [
    ['为什么「匹配语义」要先于技术选型？','因为不同语义对应完全不同的技术能力：只要精确关键词，数据库索引就够；要分词召回，需要倒排结构；要纠错，需要词形相似算法；要同义扩展，需要词典或向量。如果先选了技术再倒推语义，等于让工具决定产品能力。语义清单是把「用户期待」翻译成「能力需求」的过程——有了这张清单，选型才是匹配题而不是信仰题。'],
    ['「模糊匹配」为什么扛不住搜索场景？','因为「%关键词%」这种前后通配的匹配无法利用索引的有序性——索引像字典，只能帮你快速找到「以某词开头」的内容，而前缀通配等于要求翻遍每一页找「包含某词」的句子，只能全表扫描。数据量小的时候这是可接受的偷懒，数据量上来后每次搜索都是全表扫描。搜索场景的正解是换匹配机制（倒排索引），而不是给模糊匹配调参。'],
    ['搜索索引作为「副本」要设计什么？','两件事：同步与重建。同步决定新鲜度——商品上架后多久能被搜到，需要明确延迟承诺并监控；重建决定可靠性——索引损坏或逻辑变更时，能从源数据完整重建，所以源数据永远是事实来源，索引只是视图。很多搜索事故的根源是把索引当唯一存储：一旦损坏，数据无从恢复。副本的本分是可丢弃、可重建。'],
  ],
  fbs: [
    ['产品', '选型评估把「搜索到底要做到什么程度」第一次讲清楚了'],
    ['开发', '两阶段演进让搜索先上线后升级，节奏可控'],
    ['运维', '同步延迟监控让索引新鲜度有保障'],
  ],
  risks: ['用模糊匹配硬扛搜索需求的系统，数据量一上来就是全表扫描的深渊', '匹配语义必须先行定义', '索引是副本，同步与重建要设计', '选型依据与决策留档'],
}),
];
