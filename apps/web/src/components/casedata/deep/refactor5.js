/* 「重构建议」深度版案例（21-25） */

export const DEEP_REFACTOR_5 = [
({
  t: '接口参数 · 出入参收敛', sub: '10 分钟写完', d: '重构接口参数：出入参对象化与收敛',
  ly: 'sheet', pal: 'blue', bg: 'grid',
  facts: ['输入：长参数接口', '输出：收敛方案', '收敛 6 个'],
  inp: '粘贴接口现状', out: ['结构：参数对象化', '关键：出入参分离', '配套：校验前置'],
  stats: [['参数', '9→1 对象'], ['接口', '6 个收敛'], ['校验', '前置']],
  quote: '九个散参数的接口是一次传了九个口头约定：参数对象化的意义，是把约定变成有名字、有类型、有校验的结构',
  fb: ['开发', '参数对象加校验之后，「传错参数顺序」这种事故从根上消失了'], risk: '靠位置传参的长参数列表，每一次调用都是一次「希望我没传错顺序」的祈祷',
  ctx: '几个核心接口带着七八个散参数，调用处全靠记忆参数顺序，已经出过两次传错顺序的事故。收敛重构：参数对象化（散参数收成一个有名字的参数对象）、出入参分离（返回也是明确结构而不是混装）、校验前置在入口处一次性完成。六个接口收敛后，调用处自文档化。',
  raw: `接口现状：
下单接口九个散参数：
（用户号, 商品号, 数量,
 优惠券号, 地址号,
 备注, 来源, 扩展, 幂等键）

事故记录：
· 两次调用把
  「数量」和「商品号」
  传反了（类型相同，
  编译器不拦）
· 新增参数只能
  往最后追加，
  参数表越来越长

收敛方案：
散参数 → 参数对象
位置传参 → 命名传参
校验散落 → 入口统一`,
  steps: [
    ['盘参数', '各接口参数清点', '2 分钟'],
    ['定对象', '参数对象结构', '3 分钟'],
    ['分出入', '返回结构明确化', '2 分钟'],
    ['置校验', '入口统一校验', '2 分钟'],
    ['迁调用', '调用处逐步切换', '1 分钟'],
    ['立规范', '新接口参数规则', '1 分钟'],
  ],
  full: `【重构 · 接口参数（出入参收敛）】

一、散参数的三宗罪
1 位置即约定：
　第九个参数是什么，
　全凭记忆与默契
2 同类型互串：
　两个同类型参数
　传反了没人拦
　（已发生两次事故）
3 只增不减：
　新参数只能追加，
　列表无限膨胀
　（旧参数不敢删，
　不知道谁在用）

二、参数对象化
九个散参数收敛为
一个下单请求对象：
· 每个参数有名字
　（调用处写键值，
　顺序无关）
· 每个参数有类型
　（类型不同的字段
　不会互串；
　同类型字段靠键名区分）
· 可选参数自然表达
　（有就传，无就不传，
　不用占位）
调用处从「一串值」
变成「一份清单」，
自文档化

三、出入参分离
出口同样明确化：
· 返回结构定义清晰：
　单号、状态、时间戳…
　各有各的字段
· 不混装
　（不把错误信息
　塞进正常字段，
　不把多个含义
　挤进一个字段）
· 入参与出参
　各自独立的类型
　（不图省事共用，
　共用会把两个字段的
　演化绑死）

四、校验前置（入口安检）
参数对象在入口
一次性校验：
· 必填项、格式、范围
· 校验失败立即返回
　明确的字段级错误
· 校验通过后，
　内部逻辑不再重复检查
入口把关一次，
内部处处放心

五、迁移策略
· 新接口直接用对象形态
· 旧接口逐个收敛：
　新增参数对象版本，
　旧版本标记废弃，
　调用方限期迁移
· 迁移期两个版本并存，
　监控旧版本调用量
　归零后下线

六、新规范
· 参数超过三个 →
　必须对象化
· 布尔参数禁止裸传
　（「传 真」是什么含义？
　用命名参数或枚举）
· 参数对象有校验定义
　（不允许「裸对象」）

七、结果
六个接口收敛完成；
参数顺序事故归零；
新增参数不再
「往最后追加」。`,
  stats2: [['参数', '9→1 对象'], ['事故', '顺序类归零'], ['校验', '入口统一']],
  qa: [
    ['「同类型参数传反」为什么只有参数对象能根治？','因为位置传参的约束只有「位置」，而位置不携带语义：两个同类型参数在类型系统眼里完全等价，传反了类型检查无能为力。参数对象把约束从位置换成键名——「数量=3，商品号=1001」写错了键名在编译或静态检查时就会被发现。本质上是把弱约束（顺序）升级为强约束（命名），这类错误的生存空间被结构性地消灭了。'],
    ['入参对象和出参对象为什么不该图省事共用？','因为入参和出参的演化方向不同。入参跟随调用方的需求演化（新增可选参数、调整校验规则），出参跟随消费方的需求演化（新增展示字段、调整结构）。共用一个类型意味着任何一边的修改都牵动另一边——某天出参要加一个只读字段，却发现入参的校验逻辑也要跟着适配。入与出是两个契约，契约应该各自独立修订。'],
    ['校验为什么必须「入口一次」而不是「用到时再查」？','两个理由：一致性与责任清晰。散落校验的问题是同一参数在不同位置被不同标准检查（或者干脆有的地方忘了检查），行为不一致且漏检随机发生。入口统一校验建立了一个契约边界：校验通过的对象在系统内部是「可信的」，内部逻辑可以直接使用而无需自保。这让内部代码专注于业务逻辑，而不是层层设防。安检设在门口，而不是每间房各自查票。'],
  ],
  fbs: [
    ['开发', '参数对象加校验之后，「传错参数顺序」这种事故从根上消失了'],
    ['调用方', '键值传参自文档，不用翻定义数位置'],
    ['评审者', '「超过三个参数必须对象化」进了规范'],
  ],
  risks: ['靠位置传参的长参数列表，每一次调用都是一次「希望我没传错顺序」的祈祷', '入参出参类型独立演化', '校验入口统一不重复设防', '布尔参数禁止裸传'],
}),
({
  t: '重试层 · 统一封装', sub: '10 分钟写完', d: '重构重试逻辑：统一重试层封装',
  ly: 'terminal', pal: 'gold', bg: 'paper',
  facts: ['输入：散落重试', '输出：统一方案', '收敛 7 处'],
  inp: '粘贴重试现状', out: ['结构：统一重试组件', '关键：策略可配置', '配套：幂等前置'],
  stats: [['收敛', '7 处'], ['策略', '可配置'], ['前置', '幂等检查']],
  quote: '重试逻辑散落在七处的代价不是重复代码，是七种不一致：有的无限重试，有的重试不幂等，有的把不可重试的错也重试了',
  fb: ['开发', '统一重试层上线后，「重试把重复支付打出来」的隐患被一次性清掉了'], risk: '每处自己实现的重试逻辑，等于每个调用点都埋了一个「重试策略靠手感」的盲盒',
  ctx: '代码里有七处自己实现的重试：有的无限循环重试、有的失败立刻放弃、有的对不可重试的错误也在重试。统一封装重试层：可配置的策略（次数、退避、可重试错误清单）、使用前置条件检查（目标操作必须幂等）、统一观测（重试次数与成功率指标）。七处收敛为一处。',
  raw: `重试现状（七处各自为政）：
· 调用 A：无限重试
  （把下游打挂过）
· 调用 B：重试 1 次就放弃
  （抖动即失败）
· 调用 C：对 400 也在重试
  （重试改变不了参数错误）
· 调用 D：重试无间隔
  （风暴式冲击）
· 某处重试不幂等
  （重试出过重复数据）

统一重试层：
策略可配置
错误分可重试/不可重试
幂等前置检查
统一观测`,
  steps: [
    ['盘散点', '七处重试清点', '2 分钟'],
    ['定策略', '次数退避错误分类', '3 分钟'],
    ['写组件', '统一重试实现', '3 分钟'],
    ['查幂等', '目标操作幂等确认', '2 分钟'],
    ['迁调用', '七处逐个替换', '1 分钟'],
    ['配观测', '重试指标采集', '1 分钟'],
  ],
  full: `【重构 · 重试层（统一封装）】

一、散点的七种病
盘点发现的乱象：
1 无限重试：
　下游故障时被反复冲击，
　加重雪崩
2 一次就弃：
　网络抖动即报错，
　可用性差
3 盲目重试：
　参数错误（400 类）
　也在重试——
　重试一万次也不会对
4 零间隔：
　失败立刻重来，
　风暴式冲击
5 不幂等重试：
　重试造成重复操作
　（最危险的一种）
统一封装的前提：
承认「重试」是个专业活，
不是顺手写的循环

二、策略三要素
要素 1 次数与退避：
　默认重试 3 次，
　指数退避 + 抖动
　（拉开重试间隔，
　避免同步冲击）
要素 2 错误分类：
　可重试：超时、网络、
　限流、服务暂不可用
　不可重试：参数错、
　权限错、业务拒绝
　（不可重试立即失败，
　一次都不多试）
要素 3 总时长上限：
　重试窗口有限，
　超时整体放弃
每个调用点可按需覆写
（但覆写要留理由）

三、幂等前置（安全闸）
重试层使用规则：
目标操作必须幂等
· 有幂等键的操作 →
　放心重试
· 无幂等保证的操作 →
　重试层拒绝服务
　（宁可暴露问题，
　不可盲目重试）
本次盘点抓出：
两处非幂等操作
一直在被重试
（重复数据的隐患源）

四、统一实现与接入
重试层提供统一入口：
传入操作 + 策略标识
· 策略集中定义，
　调用点声明式选用
· 重试过程打日志：
　第几次、什么原因
· 最终失败抛出
　「重试耗尽」异常
　（带完整过程摘要）

五、观测配套
· 每次重试计数：
　重试率是下游健康度
　的晴雨表
· 重试耗尽告警
· 按调用点分类统计
　（哪个调用点总失败）
重试从「隐藏行为」
变成「可见指标」

六、结果
七处收敛为一处；
两处非幂等重试隐患
被清除；
重试策略有了
统一的裁判。`,
  stats2: [['收敛', '7→1 处'], ['隐患', '非幂等×2'], ['观测', '重试可见']],
  qa: [
    ['「对不可重试的错误重试」为什么是常见又危险的错误？','因为它把重试用在了错误的对象上。重试的意义是「对抗瞬时性故障」——网络抖动、短暂过载，这类错误随时间可能自愈。而参数错误、权限拒绝是确定性错误：重试一万次，错参数还是错参数。对确定性错误的重试不仅无用，还有害：它延长了失败路径、消耗了调用方的等待时间、并给下游送去无意义的流量。重试的第一道设计关卡是错误分类，而不是次数配置。'],
    ['幂等为什么是重试的「前置安全闸」？','因为重试的物理含义是「同一操作执行多次」。如果操作本身不幂等（没有去重机制），重试就是在制造重复：重复扣款、重复下单、重复发送。重试层拒绝服务非幂等操作，是把「重试的代价」显式化：你想享受重试的可用性红利，就必须先支付幂等的设计成本。这道闸口拦住的不是重试请求，是未来的重复数据事故。'],
    ['重试率为什么值得当观测指标？','因为重试率是「表面成功率」之下的真实健康度。一个接口成功率 99.9%，看起来很好——但如果这 99.9% 里有一半靠重试才成功，说明真实的单次成功率只有约 99.8% 不到，系统在靠重试续命。重试率上升通常是下游劣化的早期信号（比重试耗尽告警早得多）。观测重试，是在观测系统为了维持体面付出了多少隐形成本。'],
  ],
  fbs: [
    ['开发', '统一重试层上线后，「重试把重复支付打出来」的隐患被一次性清掉了'],
    ['运维', '重试率看板成了下游健康的早期预警'],
    ['评审者', '「重试必须过重试层」进了代码规范'],
  ],
  risks: ['每处自己实现的重试逻辑，等于每个调用点都埋了一个「重试策略靠手感」的盲盒', '确定性错误不重试', '非幂等操作禁止重试', '重试率要进观测'],
}),
({
  t: '时间抽象 · 时钟接口', sub: '8 分钟写完', d: '重构时间依赖：时钟接口抽象',
  ly: 'doc', pal: 'ink', bg: 'grid',
  facts: ['输入：时间硬依赖', '输出：抽象方案', '接缝 1 处'],
  inp: '粘贴时间依赖', out: ['结构：时钟接口注入', '关键：时间可控', '配套：时区纪律'],
  stats: [['接缝', '时钟注入'], ['边界', '可测试'], ['时区', '纪律化']],
  quote: '时间是最容易被忽略的硬依赖：它安静、无形、无处不在——直到你要测试「月底最后一分钟」的逻辑',
  fb: ['测试', '时钟接口注入后，跨月跨年的边界测试从碰运气变成点菜'], risk: '直接读系统时间的业务逻辑，等于把测试的时钟交给了墙上——而墙上的钟永远不配合你',
  ctx: '计费模块大量直接读系统时间，月末结算逻辑根本没法测（总不能等到月末再跑测试）。重构：抽象时钟接口（获取当前时刻、时区），业务代码全部经由时钟获取时间，生产注入系统时钟，测试注入可控时钟。边界测试随点随测，顺带把散落的时区处理收进时钟层。',
  raw: `时间硬依赖现状：
计费模块 23 处
直接读取系统时间
· 月末结算逻辑
  无法测试
  （难道等真的月末？）
· 跨天边界行为
  全靠线上验证
· 时区处理散落各处
  （有的用本地时区，
  有的用协调时）

重构方案：
时钟接口：
· 获取当前时刻
· 获取时区
生产注入系统时钟
测试注入可控时钟`,
  steps: [
    ['找散点', '时间读取点清点', '2 分钟'],
    ['定接口', '时钟能力定义', '2 分钟'],
    ['改注入', '业务代码切换', '3 分钟'],
    ['收时区', '时区处理集中', '1 分钟'],
    ['写测试', '边界时刻随点随测', '1 分钟'],
    ['验行为', '生产行为不变', '1 分钟'],
  ],
  full: `【重构 · 时间抽象（时钟接口）】

一、时间依赖的隐蔽性
时间是最特殊的依赖：
· 它不需要创建对象，
　一个系统调用就完成，
　所以没人把它当「依赖」
· 但它实实在在地
　决定了行为：
　同一份代码在上午跑
　和在月末跑，
　结果可能不同
凡是让行为随时间变化的输入，
都是依赖——
时间只是最不起眼的那个

二、时钟接口设计
接口两个能力：
· 获取当前时刻
　（统一精度与语义）
· 获取时区
　（业务时区从配置来，
　不从机器环境来）
两个实现：
· 系统时钟：
　生产用，读真实时间
· 可控时钟：
　测试用，
　可设定任意时刻、
　可快进
业务代码只认识接口，
不认识墙上挂的钟

三、散点迁移
23 处时间读取逐个替换：
系统调用 → 时钟接口
· 机械替换，
　行为不变
· 替换完整后，
　全库搜索确认
　无遗漏散点
　（漏一处，
　测试就还有一处
　不受控）

四、时区纪律（顺带治理）
借重构收拢时区处理：
· 存储与传输：
　统一用带时区的时间表示
· 展示：
　按用户时区转换
· 业务规则
　（如「按天结算」）：
　明确按哪个时区切天
　——写进规格
散落的「本地时间」假设
是跨时区事故的温床

五、测试红利
可控时钟让时间成为参数：
· 把时钟拨到月末 23:59 →
　测结算边界
· 快进一个月 →
　测账期逻辑
· 夏令时切换日 →
　测时区规则
以前「没法测」的逻辑，
现在「点菜测」

六、适用推广
时钟抽象值得推广到
所有时间敏感模块：
· 定时任务逻辑
· 有效期判断
· 时长计算
成本极低
（一个接口两个实现），
收益是全部时间逻辑
的可测性

七、结果
月末边界测试随点随测；
时区处理收进时钟层；
时间从「环境变量」
变成「可控输入」。`,
  stats2: [['散点', '23 处收敛'], ['测试', '边界随点'], ['时区', '集中治理']],
  qa: [
    ['为什么时间值得被当作「依赖」对待，尽管它只是一个系统调用？','判断依赖的标准不是获取方式，而是影响方式。时间无声地参与业务决策：同一个函数在不同时刻调用返回不同结果，这正是依赖的定义——行为随外部输入变化。数据库连接大家会想到抽象，因为它「看起来像依赖」；时间被忽略，只因为它获取成本为零。但测试需要的不是依赖的可见性，是依赖的可控性——凡是不可控又影响行为的输入，都配一个接缝。'],
    ['「业务时区从配置来，不从机器环境来」防的是什么？','防的是「代码行为随部署环境漂移」。机器的时区配置是运维层面的偶然事实：同一份代码部署在不同机房、容器重建时环境变量丢失，「本地时间」的含义就悄悄变了。按天切账的业务如果依赖机器时区，切账边界可能随部署位置漂移八小时。时区是业务规则（「按北京时间的自然日结算」），业务规则必须来自显式配置，不能寄生在环境里。'],
    ['可控时钟相比「测试时修改系统时间」好在哪？','好在隔离与并行。修改系统时间影响的是整台机器：同一时刻跑的其他测试、后台任务、日志时间戳全部被波及，测试之间互相污染，无法并行。可控时钟是注入到被测代码里的测试替身：这个测试把时钟拨到月末，隔壁测试的时钟还走现在，互不干扰。依赖注入的价值在这里体现得淋漓尽致：不是改变世界，而是给每个测试一个自己的世界。'],
  ],
  fbs: [
    ['测试', '时钟接口注入后，跨月跨年的边界测试从碰运气变成点菜'],
    ['开发', '时区规则集中后，跨时区需求不再翻车'],
    ['新人', '时钟接口是现成的时间处理教材'],
  ],
  risks: ['直接读系统时间的业务逻辑，等于把测试的时钟交给了墙上——而墙上的钟永远不配合你', '时间散点必须清点干净', '时区是业务规则要显式配置', '时钟抽象推广到所有时间敏感逻辑'],
}),
({
  t: '构建依赖 · 大扫除', sub: '8 分钟写完', d: '清理构建依赖：无用依赖识别与移除',
  ly: 'kanban', pal: 'green', bg: 'linen',
  facts: ['输入：依赖清单', '输出：清理方案', '移除 14 个'],
  inp: '粘贴依赖清单', out: ['结构：依赖审计流程', '关键：移除验证', '配套：准入纪律'],
  stats: [['依赖', '62→48'], ['体积', '降 31%'], ['安全面', '收缩']],
  quote: '每个依赖都是一份长期合同：引入时是一行声明，维护时是升级、漏洞、兼容的持续账单——大扫除就是重新审视每份合同',
  fb: ['安全', '依赖瘦身后，漏洞告警的来源少了一半'], risk: '只进不出的依赖清单，会让项目的攻击面、构建时间和升级负担按年复利增长',
  ctx: '项目 62 个直接依赖，一半是历史遗留：有的早就不用了，有的被更小的方案替代了，有的只是某次实验的残留。依赖大扫除：逐个审计（还用吗、可替代吗、值得吗）、安全移除流程（移除后全量构建加测试验证）、移除 14 个，安装体积降三成。同时立准入纪律：新依赖要过评审。',
  raw: `依赖现状：
62 个直接依赖
· 若干已无人使用
  （当年引入的功能下线了）
· 若干可被内置能力替代
  （框架自带了同类功能）
· 若干是实验残留
· 每次安全扫描
  一半告警来自
  不再使用的依赖

清理流程：
审计 → 分类 → 移除 →
验证 → 准入纪律`,
  steps: [
    ['盘清单', '全量依赖清点', '2 分钟'],
    ['做审计', '逐个三问判定', '3 分钟'],
    ['定移除', '可移除清单', '2 分钟'],
    ['走验证', '移除后构建测试', '2 分钟'],
    ['立准入', '新依赖评审制', '1 分钟'],
    ['设巡检', '定期复查机制', '1 分钟'],
  ],
  full: `【清理 · 构建依赖（大扫除）】

一、依赖的隐藏成本
每个依赖的真实账单：
· 安装体积与构建时间
· 升级义务
　（它的版本要跟，
　它的安全补丁要接）
· 攻击面
　（它的漏洞 = 你的漏洞）
· 兼容负担
　（它与别的依赖打架，
　你来劝）
引入一行声明，
签的是一份长期合同

二、逐个三问审计
对每个依赖：
问 1 还在用吗？
　代码里搜引用——
　零引用的直接标移除
问 2 有替代吗？
　语言/框架内置能力
　能否覆盖？
　更轻的依赖能否替代？
问 3 值得吗？
　只用了一个函数的依赖，
　值不值得背着整个包？
三问结论：
保留 / 替代 / 移除

三、本次战果
· 零引用：6 个
　（功能早已下线，
　依赖还在名单上）
· 被内置替代：5 个
　（框架升级后自带了）
· 实验残留：3 个
　（当年的尝试无人记得）
合计移除 14 个

四、安全移除流程
每个移除走验证：
1 移除依赖声明
2 全量构建
　（构建失败 =
　还有隐藏引用，
　顺藤摸瓜找出来）
3 全量测试
4 独立提交
　（一个依赖一个提交，
　出事好定位）
注意幽灵依赖：
有些依赖被间接引用
（别的依赖依赖它），
移除前查依赖树

五、准入纪律（防回潮）
新增依赖需回答：
· 为什么需要它
· 有没有更轻的选择
· 它的维护状态如何
　（三年没更新的依赖
　慎入）
· 许可是否兼容
评审通过才准入
（进来的门收紧，
　出去的清扫才有意义）

六、定期巡检
· 每季度跑一次
　依赖审计
　（零引用检测、
　体积排行、
　更新状态）
· 安全告警按
　「是否在用」分级
　（不用的依赖的漏洞：
　先移除依赖，
　而不是先打补丁）

七、结果
依赖 62 → 48；
安装体积降 31%；
安全告警来源减半。`,
  stats2: [['依赖', '62→48'], ['体积', '降 31%'], ['纪律', '准入评审']],
  qa: [
    ['「零引用依赖」为什么不能直接删，要先走构建验证？','因为代码引用不是依赖的唯一使用方式。构建工具可能隐式用到它（配置里声明了加载）、运行时可能反射加载、某些依赖的存在本身改变构建行为（全局垫片类依赖）。零引用搜索能抓出大多数，但「全量构建+测试」才是终审：移除后构建失败，说明还有隐藏的使用路径，这正是独立提交的价值——每个依赖单独验证，失败即知因果。'],
    ['「三年没更新的依赖慎入」这条准入标准在防什么？','防的是「接手一份无人维护的债务」。依赖停止更新意味着：安全漏洞无人修复、新版本的环境不兼容无人处理、出了问题无人可问——而这个依赖的漏洞在你的依赖树里，扫描器报的是你的名字。维护状态是依赖的生命体征：选择依赖不只是选择功能，是选择一个长期合作方。对方已经停业，你的合同签得再便宜也是风险。'],
    ['「不用的依赖有漏洞，先移除而不是先打补丁」的逻辑是什么？','因为打补丁是「继续持有」，移除是「解除合同」。一个已经没人使用的依赖，为它打补丁等于继续为一个不产生价值的资产支付维护成本，而且下个漏洞来了还要再付一次。移除则一劳永逸：攻击面直接消失，未来的告警与它无关。资产处置的原则同样适用于依赖：不产生价值的持有，止损优于维护。'],
  ],
  fbs: [
    ['安全', '依赖瘦身后，漏洞告警的来源少了一半'],
    ['开发', '安装时间和构建时间都肉眼可见地快了'],
    ['评审者', '准入评审拦下过两个「能用就行」的重型依赖'],
  ],
  risks: ['只进不出的依赖清单，会让项目的攻击面、构建时间和升级负担按年复利增长', '移除要走构建测试验证', '幽灵依赖先查依赖树', '准入评审防回潮'],
}),
({
  t: '分支模型 · 化繁为简', sub: '10 分钟写完', d: '简化分支模型：从复杂流程到主干开发',
  ly: 'chat', pal: 'orange', bg: 'dots',
  facts: ['输入：分支现状', '输出：简化方案', '分支 4 类→2 类'],
  inp: '粘贴分支现状', out: ['结构：主干开发简化', '关键：短命分支', '配套：特性开关'],
  stats: [['分支', '4 类→2 类'], ['合并', '冲突降七成'], ['发布', '主干即发']],
  quote: '分支模型的复杂度是协作成本的复利：长命分支上的每一次分叉，都在给未来的合并冲突存钱',
  fb: ['开发', '改主干开发后，合并冲突从每周大战变成偶发小事'], risk: '长期分支并行的团队，集成成本会随分支寿命指数增长——「合并日」就是还债日',
  ctx: '团队分支模型复杂：开发分支、集成分支、发布分支、修复分支四类并行，功能分支一活就是几周，合并冲突成了每周固定战役。简化为主干开发：直接向主干提交（或短命分支当天合并）、未完成的功能用特性开关隔离、发布从主干切出。合并冲突下降七成。',
  raw: `分支现状（四类并行）：
· 功能分支
  （平均寿命两周）
· 集成分支
  （定期从功能分支汇）
· 发布分支
  （从集成分出）
· 修复分支
  （从发布分支出）

代价：
· 合并冲突每周大战
· 「我的分支和主干
  差了多少」没人说得清
· 修复要挑到三四个分支
· 集成延迟：
  问题在合并日才暴露`,
  steps: [
    ['算成本', '分支复杂度盘点', '2 分钟'],
    ['定模型', '主干开发设计', '3 分钟'],
    ['配开关', '未完成功能隔离', '2 分钟'],
    ['改流程', '提交与评审调整', '2 分钟'],
    ['调发布', '主干切发布', '1 分钟'],
    ['验效果', '冲突频率对比', '1 分钟'],
  ],
  full: `【重构 · 分支模型（化繁为简）】

一、复杂分支的真实成本
分支越多、越长寿：
· 合并冲突越频繁
　（分叉时间越长，
　差异越大）
· 集成越延迟
　（问题攒到合并日
　才集中爆发）
· 修复越繁琐
　（一个补丁挑多个分支）
· 心智负担越重
　（「我该从哪个分支拉」）
复杂分支模型的
每一项「严谨」，
都在收取协作税

二、主干开发设计
简化后只有两类分支：
· 主干：
　唯一长期分支，
　始终可发布状态
· 短命分支：
　当天开当天合
　（超过一天的分支
　要说明理由）
核心变化：
集成的频率从「每周」
变成「每天多次」——
小步集成，
冲突永远是小冲突

三、特性开关配套
「功能没做完怎么合主干」：
用特性开关包裹未完成功能
· 代码合进主干，
　但功能默认关闭
· 完成后打开开关
· 开关的生命周期
　按开关治理管理
　（到期清理）
开关让「代码集成」
与「功能发布」解耦：
代码可以早集成，
功能可以晚亮相

四、质量门禁（主干保护）
主干必须始终健康：
· 合并前必过：
　构建 + 测试 + 评审
· 主干红了：
　修复优先于新功能
　（红色主干是全员事件）
门禁不是流程负担，
是主干开发的前提：
没有门禁的主干开发
是灾难开发

五、发布方式
· 发布 = 从主干切标签
　（或主干直接部署）
· 不再有发布分支的
　长期维护
· 紧急修复：
　主干修复后随下次发布
　（或热修复直接主干）
修复只需要去一个地方

六、迁移步骤
1 先缩短分支寿命
　（要求三天内合并）
2 引入特性开关
3 解散集成分支
　（功能直接合主干）
4 发布分支改为
　临时标签
5 复盘冲突数据，
　固化新模型

七、结果
分支类别 4 → 2；
合并冲突下降七成；
「合并日」从日历上消失。`,
  stats2: [['分支', '4 类→2 类'], ['冲突', '降七成'], ['集成', '日多次']],
  qa: [
    ['为什么「集成频率」是分支模型的核心变量？','因为冲突的规模与分叉时长成正比：两条线分开一天，差异可能只有几处；分开一个月，差异就是几十个文件的纠缠。高频集成把分叉时长压到天以内，每次合并的差异都小到可以轻松解决——冲突不是被消灭了，是被摊薄了。分支模型之争（主干开发还是长命分支）本质上是集成频率之争：是每天付一点小成本，还是每月付一次大成本。'],
    ['特性开关如何实现「代码集成」与「功能发布」的解耦？','它给了代码两张时间表。没有开关时，代码合入就等于功能上线——所以未完成的代码不敢合，只能留在分支上慢慢发酵（冲突随之累积）。有了开关，代码可以随时合入主干（享受早集成的红利），而功能通过开关状态独立控制（完成前关闭，发布时打开）。集成的节奏跟着工程走，发布的节奏跟着产品走，两者不再互相绑架。'],
    ['「主干红了优先修复」为什么必须是全员纪律？','因为主干是所有短命分支的共同基线。主干一旦带病，每个从它拉出的分支都继承了病灶，每个合并回去的分支都要和病灶共存——问题随分支数量复制扩散。红色主干的存在时间越长，污染面越大。所以主干健康是公共资源问题：修复红色主干的优先级高于一切新功能，这不是流程洁癖，是防止质量债务按分支数复利。'],
  ],
  fbs: [
    ['开发', '改主干开发后，合并冲突从每周大战变成偶发小事'],
    ['发布负责人', '修复只用去一个地方，不用再挑分支'],
    ['新同事', '两类分支一目了然，第一天就知道往哪提交'],
  ],
  risks: ['长期分支并行的团队，集成成本会随分支寿命指数增长——「合并日」就是还债日', '短命分支当天合并', '未完成功能用开关隔离', '主干红了修复优先'],
}),
];
