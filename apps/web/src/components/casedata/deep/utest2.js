/* 「写单元测试」深度版案例（6-10） */

export const DEEP_UTEST_2 = [
({
  t: 'API 层测试 · Mock 边界', sub: '12 分钟写完', d: '编写接口层测试：Mock 边界与契约断言',
  ly: 'mail', pal: 'red', bg: 'grid',
  facts: ['输入：接口定义', '输出：测试用例集', '用例 15 个'],
  inp: '粘贴接口定义', out: ['结构：Mock 下游+真实路由', '覆盖：参数校验×状态码', '边界：只 Mock 进程外依赖'],
  stats: [['用例', '15 个'], ['Mock', '仅进程外'], ['契约', '双向']],
  quote: 'Mock 的边界画在进程边上：自己的代码要真跑，别人的服务才替身',
  fb: ['后端负责人', 'Mock 边界纪律定了之后，接口测试抓到的真问题翻了一倍'], risk: 'Mock 掉自己的业务逻辑，测的是 Mock 的演技，不是代码的行为',
  ctx: '接口层测试最常见的失败是 Mock 过度：把自家的服务层也 Mock 掉，测试只验证了「路由接到了 Mock」，任何真实逻辑改动都测不出来。正确边界：HTTP 层真实运行（路由、参数校验、错误映射），只 Mock 进程外依赖（数据库、第三方）。15 个用例覆盖参数校验矩阵与状态码契约。',
  raw: `被测接口：
创建订单接口（参数校验、
库存检查、下单、异步通知）
依赖：
· 订单服务（自家代码）
· 库存服务（自家代码）
· 支付网关（外部）
· 消息队列（外部）

反面教材：
Mock 掉订单服务与库存服务，
测试只剩「参数进、Mock 出」
——自家逻辑零覆盖

边界原则：
进程内 = 真跑
进程外 = Mock`,
  steps: [
    ['画边界', '进程内外依赖分类', '2 分钟'],
    ['搭环境', '真实路由+外部替身', '3 分钟'],
    ['设参数矩阵', '校验规则的组合', '3 分钟'],
    ['定契约', '状态码与响应结构', '3 分钟'],
    ['测副作用', '外部调用的验证', '2 分钟'],
    ['测错误映射', '异常到响应的转换', '2 分钟'],
  ],
  full: `【测试设计 · API 层（Mock 边界）】

一、Mock 边界（先定纪律）
真实运行：
· 路由与中间件（参数解析、
　鉴权拦截）
· 参数校验逻辑
· 自家服务层与领域逻辑
· 错误到响应的映射
Mock 替身：
· 支付网关（外部，
　模拟成功/失败/超时）
· 消息队列（外部，
　记录发出的消息）
· 数据库可用内存实例
　（不 Mock，用真引擎轻实例）
判断标准：改这段代码时，
测试应该红吗？
应该红 → 真跑；
不该红 → 可 Mock。

二、参数校验矩阵（7 用例）
1 合法全参数 → 201 + 订单号
2 缺必填 → 400 + 字段级错误
　（断言错误里指明哪个字段）
3 数量超限 → 400 + 明确上限
4 非法枚举 → 400
5 超长字符串 → 400
6 类型错误（数字传字符串）→ 400
7 空请求体 → 400（不是 500！）

三、业务状态码契约（5 用例）
8 库存不足 → 422 + 库存错误码
9 重复提交（同幂等键）→
　返回首次结果（200，非新创建）
10 支付网关失败 → 502 +
　「支付暂不可用」（不透传内部细节）
11 网关超时 → 504
12 限流触发 → 429 + 重试指引

四、副作用验证（2 用例）
13 下单成功 → 断言消息队列
　收到一条正确结构的通知
　（Mock 的调用记录即断言对象）
14 下单失败 → 断言通知未发出

五、错误映射（1 用例）
15 内部未知异常 → 500 +
　统一错误结构（不泄露堆栈，
　堆栈进日志带请求 ID）

六、断言规范
· 响应断言三件套：
　状态码 + 错误码 + 关键字段
· 不透传断言：响应里
　不允许出现内部类名/堆栈
· Mock 交互断言只验证
　「进程外」的调用，
　不验证内部实现细节
　（防测试与实现耦合）

七、结果
15 用例，真实逻辑全覆盖；
后续一次参数校验回归
被用例 5 抓出。`,
  stats2: [['用例', '15 个'], ['真跑', '进程内全量'], ['抓缺', '校验回归']],
  qa: [
    ['「改这段代码测试应该红吗」为什么是 Mock 的判据？', '测试的价值在于充当变更的哨兵：代码变了，相关测试必须有反应。如果服务层被 Mock 掉，那么服务层的任何改动都不会让测试变红——哨兵对最重要的防区失明了。按「变更敏感性」划 Mock 边界，本质是在配置哨兵的站位：自家阵地真岗，外部边境替身。'],
    ['为什么数据库推荐内存实例而不是 Mock？', 'Mock 数据库意味着你在测「我以为数据库会怎么表现」：SQL 语法、约束、事务行为全是想象。内存实例是真实引擎，约束违反、事务隔离、查询行为都是真的——接口层测试经常死在「Mock 说可以，真库说不行」的落差上。能用轻实例就不做替身，是数据库测试的黄金法则。'],
    ['错误响应为什么要断言「不泄露内部细节」？', '两个理由：安全上，堆栈与内部类名是给攻击者的侦察报告；契约上，客户端按错误码编程，内部细节的泄露意味着响应结构随实现漂移，客户端迟早解析到垃圾。断言「500 响应只含统一错误结构」把错误格式也变成了契约——错误处理不是实现的附属品，它是接口的一部分。'],
  ],
  fbs: [
    ['后端负责人', 'Mock 边界纪律定了之后，接口测试抓到的真问题翻了一倍'],
    ['前端同学', '错误码契约稳定，错误处理逻辑终于不用猜'],
    ['新同事', '「改动会不会红」这个判据，一分钟学会'],
  ],
  risks: ['Mock 掉自己的业务逻辑，测的是 Mock 的演技，不是代码的行为', '数据库优先内存实例而非 Mock', '错误响应结构也是契约，要断言', 'Mock 交互断言不绑实现细节'],
}),
({
  t: '数据库层测试 · 事务回滚', sub: '12 分钟写完', d: '编写数据层测试：事务隔离与数据构造',
  ly: 'sheet', pal: 'ink', bg: 'linen',
  facts: ['输入：数据操作代码', '输出：测试用例集', '用例 13 个'],
  inp: '粘贴数据层代码', out: ['结构：每用例事务回滚', '覆盖：CRUD+约束+事务', '数据：构造器不共享'],
  stats: [['用例', '13 个'], ['隔离', '事务回滚'], ['构造', '每例独立']],
  quote: '数据层测试的洁净法则：每个用例自带数据、用完回滚——用例之间互不知情',
  fb: ['后端负责人', '事务回滚隔离后，测试顺序敏感这种玄学消失了'], risk: '用例之间共享数据是数据层测试的原罪：今天绿明天红，全看执行顺序',
  ctx: '数据层测试的经典灾难：用例 A 插入的数据被用例 B 依赖，单独跑都绿、一起跑看心情。改造两件事：每个用例用构造器自建所需数据（不依赖任何前置用例），每个用例结束事务整体回滚（数据不残留）。13 个用例覆盖 CRUD、约束、事务边界，任意顺序、任意子集运行，结果恒定。',
  raw: `被测对象：
订单数据访问层
（创建、查询、状态更新、
批量操作、唯一约束）

旧测试的问题：
· 用例 3 依赖用例 1 插入的订单
  （顺序敏感）
· 数据残留互相污染，
  重跑必挂
· 并发跑直接死锁

改造原则：
1 每个用例自建数据
2 每个用例事务回滚
3 数据库用测试专用实例`,
  steps: [
    ['立隔离', '事务回滚机制搭建', '3 分钟'],
    ['建构造器', '数据工厂与最小数据集', '3 分钟'],
    ['设 CRUD', '基本操作断言', '2 分钟'],
    ['设约束', '唯一键与外键用例', '2 分钟'],
    ['设事务', '提交与回滚边界', '2 分钟'],
    ['验乱序', '随机顺序跑三遍', '1 分钟'],
  ],
  full: `【测试设计 · 数据层（事务回滚）】

一、隔离机制（基础设施）
1 事务包裹：
　每个用例在事务中执行，
　结束时整体回滚——
　数据「借而不留」
2 独立构造：
　每个用例用数据构造器
　现造所需记录
　（要订单就造订单，
　不捡别人的）
3 测试专用实例：
　与开发/生产物理隔离，
　测试前自动建表
验证标准：
全部用例随机顺序跑三遍 +
任选子集跑，结果一致
（顺序敏感从此消失）

二、用例设计（13 个）
CRUD（5）：
1 创建 → 读回全字段一致
2 更新单字段 → 其余字段不动
3 删除 → 查询不可见
4 条件查询 → 结果集精确匹配
　（构造 3 条，只应命中 2 条）
5 批量插入 → 条数与内容双断言
约束类（4）：
6 唯一键冲突 → 明确约束错误
　（断言错误类型，
　不是笼统的 500）
7 外键引用不存在 → 拒绝
8 必填字段为空 → 拒绝
9 字段超长 → 拒绝
事务类（3）：
10 批量操作中途失败 →
　整体回滚（断言无部分写入）
11 并发更新同一行 →
　最终一致（版本号机制验证）
12 事务内读自己的写 →
　可见（隔离级别语义）
边界类（1）：
13 空结果查询 → 空集合
　（不是 null、不抛错）

三、数据构造器规范
· 构造器带合理默认值，
　用例只覆写关心的字段
　（「造一个已支付订单，
　金额改为 999」）
· 每个构造动作即最小权限：
　用例不造不需要的数据
　（多余数据是隐性耦合）
· 构造器本身有测试
　（构造器坏了全部用例背锅）

四、断言规范
· 写后读回断言，
　不信任返回值「看起来对」
· 条数断言用精确值
　（「至少一条」是漏洞温床）
· 错误断言到约束名/错误码

五、结果
13 用例，任意顺序稳定绿；
并发用例抓出过真实锁问题。`,
  stats2: [['用例', '13 个'], ['顺序', '三遍随机绿'], ['抓缺', '并发锁']],
  qa: [
    ['用例间共享数据为什么是「原罪」？', '因为它把独立验证单元变成了隐式链条：用例 B 的正确性依赖用例 A 的副作用，单独运行、乱序运行、并行运行都会断裂，而且断裂方式诡异（今天绿明天红）。测试的可信度建立在「每个用例自证」上——自带数据、自带断言、互不知情，才能任意组合运行。'],
    ['事务回滚隔离有什么局限？', '两点要知道：一是自增主键不回滚，依赖「第一条记录 ID=1」的断言会翻车（该用查询而非假设）；二是测试代码里显式提交的事务无法被外层回滚包住（被测代码自己开事务的场景需要清理策略）。回滚是主力隔离手段，但不是万能的——知道它的边界，才能在该用清理时用清理。'],
    ['「至少一条」这种断言为什么危险？', '它放过了两类错误：多查（查出不该查的行）和漏查之外的重复。精确条数断言把查询的结果集钉死：构造 3 条数据、条件应命中 2 条，那就断言恰好 2 条且就是那 2 条。模糊断言是测试里的宽松法律——能糊弄过去的检查，迟早被缺陷糊弄过去。'],
  ],
  fbs: [
    ['后端负责人', '事务回滚隔离后，测试顺序敏感这种玄学消失了'],
    ['开发同学', '构造器默认值真香，用例三行就能起步'],
    ['流水线', '随机顺序跑三遍进了固定流程'],
  ],
  risks: ['用例之间共享数据是数据层测试的原罪：今天绿明天红，全看执行顺序', '断言条数用精确值，禁「至少一条」', '构造器自身要有测试', '注意自增主键不回滚的坑'],
}),
({
  t: '鉴权测试 · 权限矩阵', sub: '15 分钟写完', d: '编写鉴权测试：角色×资源×操作矩阵',
  ly: 'kanban', pal: 'blue', bg: 'mesh',
  facts: ['输入：权限规则', '输出：测试用例集', '用例 36 个'],
  inp: '粘贴权限规则', out: ['结构：权限矩阵全覆盖', '关键：拒绝与允许同权测试', '越权：水平与垂直双向'],
  stats: [['矩阵', '4 角色×6 资源'], ['用例', '36 个'], ['越权', '双向']],
  quote: '鉴权测试的铁律：每个「允许」要有用例，每个「拒绝」更要有用例——漏测的拒绝等于开放',
  fb: ['安全测试', '权限矩阵用例是安全回归的地基，每次改动必跑'], risk: '只测「管理员能做什么」的鉴权测试，测了个寂寞：漏洞都在「谁能做了不该做的」里',
  ctx: '权限系统重构，测试方案只列了「管理员可以管理用户」「用户可以看自己订单」这类允许用例——拒绝场景一条没有。按权限矩阵重写：4 角色 × 6 资源 × 允许/拒绝双断言，36 个用例，再加水平越权（用户 A 访问用户 B）与垂直越权（普通用户调管理接口）专项。矩阵跑完，发现两处「未定义即放行」的漏洞。',
  raw: `权限规则素材：
角色：游客、普通用户、
运营、管理员
资源：自己订单、他人订单、
商品管理、用户管理、
系统配置、公开内容
规则要点：
· 默认拒绝（白名单制）
· 运营可管理商品，不可管理用户
· 用户只能访问自己的订单

反面教材（原测试方案）：
只测了 8 条「允许」路径
拒绝路径：0 条
越权测试：0 条

风险敞口：
「规则没写的地方」
代码是怎么处理的？`,
  steps: [
    ['建矩阵', '角色×资源×操作表', '4 分钟'],
    ['填期望', '每格允许或拒绝', '3 分钟'],
    ['生用例', '矩阵逐格转用例', '3 分钟'],
    ['测越权', '水平与垂直专项', '3 分钟'],
    ['测默认', '未定义行为的处理', '1 分钟'],
    ['固回归', '矩阵用例进流水线', '1 分钟'],
  ],
  full: `【测试设计 · 鉴权（权限矩阵）】

一、权限矩阵（规格可视化）
| 资源＼角色 | 游客 | 用户 | 运营 | 管理员 |
| 公开内容 | 读✓ | 读✓ | 读✓ | 读✓ |
| 自己订单 | ✗ | 读✓ | 读✓ | 读✓ |
| 他人订单 | ✗ | ✗ | 读✓ | 读✓ |
| 商品管理 | ✗ | ✗ | 写✓ | 写✓ |
| 用户管理 | ✗ | ✗ | ✗ | 写✓ |
| 系统配置 | ✗ | ✗ | ✗ | 写✓ |
填表过程即规格审查：
每一格都必须有明确答案，
「没想过」的格子就是漏洞候选。

二、矩阵用例（36 个）
每格一个用例，双向断言：
· 允许格：操作成功 +
　返回预期数据
· 拒绝格：操作被拒 +
　正确的拒绝形态
　（状态码 403 + 不泄露资源存在性）
拒绝用例的断言细节：
· 不能是 500（拒绝也要优雅）
· 不能暴露「资源存在但无权」
　与「资源不存在」的区别
　（枚举防护）

三、越权专项（4 个）
水平越权：
37 用户 A 用自己的令牌
　请求用户 B 的订单 → 403
38 用户 A 改请求里的用户 ID
　参数 → 403
　（永远不信参数里的身份，
　只信封号里的身份）
垂直越权：
39 普通用户直接调用
　管理接口 → 403
40 运营调用用户管理接口 → 403

四、默认行为（2 个）
41 新角色访问任意资源 →
　拒绝（默认拒绝验证）
42 新资源被旧角色访问 →
　拒绝（白名单制验证）

五、断言规范
· 拒绝响应的形态统一断言
　（防「这个接口拒绝时返回 200」
　的漏网形态）
· 鉴权失败日志断言
　（谁、何时、想访问什么，
　安全审计需要）
· 矩阵用例标记统一标签，
　流水线每次必跑全量

六、结果
矩阵首跑抓出 2 处
「未定义即放行」漏洞
（新资源忘了配默认拒绝）。
权限矩阵从此与代码同库维护。`,
  stats2: [['用例', '36+6 个'], ['漏洞', '2 处放行'], ['回归', '每次必跑']],
  qa: [
    ['为什么「拒绝」用例比「允许」用例更重要？', '允许路径出错，通常是某个用户用不了功能（显性、会被投诉）；拒绝路径出错，是某个不该有权限的人拿到了权限（隐性、没人投诉，直到出事）。而且「默认拒绝」的白名单制里，拒绝是常态行为，不测常态等于没测系统。安全测试的重心永远在边界上：谁能被挡在门外。'],
    ['「不泄露资源存在性」是什么防护？', '如果无权访问时返回「该订单属于他人」，攻击者就知道这个订单号存在——换个号继续试，可以枚举整个系统的订单。正确形态是让「无权」与「不存在」的响应无法区分。这类细节单看每个接口都「能用」，只有在矩阵化的安全视角下才显形——鉴权测试断言的不只是结果，还有响应的信息量。'],
    ['权限矩阵为什么要「与代码同库维护」？', '因为权限规则会漂移：加了新接口忘了配权限、角色调整漏了某资源。矩阵放在文档里会过期，放在测试代码里则每次都在运行——过期的矩阵只是错误的安慰，活的矩阵才是防线。「新角色/新资源默认拒绝」两个用例，就是防漂移的哨兵。'],
  ],
  fbs: [
    ['安全测试', '权限矩阵用例是安全回归的地基，每次改动必跑'],
    ['后端同学', '填矩阵时自己发现了两处没想过的格子'],
    ['审计同事', '鉴权失败日志断言，审计取证快多了'],
  ],
  risks: ['只测「管理员能做什么」的鉴权测试，测了个寂寞：漏洞都在「谁能做了不该做的」里', '拒绝响应形态也要断言', '身份只信令牌，不信参数', '矩阵与代码同库，防权限漂移'],
}),
({
  t: '状态机测试 · 转移表', sub: '12 分钟写完', d: '编写状态机测试：转移表驱动用例',
  ly: 'mind', pal: 'green', bg: 'dots',
  facts: ['输入：状态机定义', '输出：测试用例集', '用例 19 个'],
  inp: '粘贴状态机定义', out: ['结构：转移表逐行用例', '覆盖：合法+非法+终态', '方法：表驱动生成'],
  stats: [['转移', '12 条'], ['用例', '19 个'], ['方法', '表驱动']],
  quote: '状态机测试就是测转移表：每条合法的走过去，每条非法的撞回来',
  fb: ['后端负责人', '转移表用例让状态机变更再也没出过回归'], risk: '只测「快乐路径」的状态机测试，等于只测了没下雨的天气预报',
  ctx: '订单状态机 5 个状态、12 条转移。测试如果只顺着「创建→支付→发货→完成」测一遍，非法转移（已取消的订单能再支付吗？已完成的能再发货吗？）全在裸奔。用转移表驱动：12 条合法转移逐一验证，7 条关键非法转移断言拒绝，终态断言不可再动。表驱动让用例与状态机定义一一对应，状态机改了，用例差异一目了然。',
  raw: `状态机定义：
状态：待支付、已支付、已发货、
已完成、已取消
合法转移（12 条）：
待支付→已支付（支付成功）
待支付→已取消（超时/用户取消）
已支付→已发货（发货）
已支付→已取消（退款取消）
已发货→已完成（确认收货）
已发货→已支付（退货回退）
…等
问题清单：
· 非法转移没有用例
  （已取消→已支付？）
· 终态行为没有用例
· 并发转移没有用例`,
  steps: [
    ['建转移表', '状态×事件×目标三元组', '3 分钟'],
    ['生合法用例', '每条转移一个用例', '3 分钟'],
    ['生非法用例', '关键非法转移断言拒绝', '3 分钟'],
    ['测终态', '终态不可再转移', '1 分钟'],
    ['测并发', '同时转移的竞争', '1 分钟'],
    ['表驱动化', '数据与断言分离', '2 分钟'],
  ],
  full: `【测试设计 · 订单状态机（转移表）】

一、转移表（用例的源代码）
每条转移 =（当前状态，事件）→
目标状态 + 副作用
表驱动：用例循环读取转移表生成，
加状态机 = 加一行表 = 加一个用例

二、合法转移用例（12 个）
每条转移一个用例，断言：
· 状态变更正确
· 副作用发生（如「已支付」
　触发支付成功通知）
· 变更时间与操作人记录
示例：
（待支付，支付成功）→ 已支付
　断言状态 + 通知已发 +
　时间戳已记

三、非法转移用例（7 个）
选最危险的非法组合：
13 已取消 + 支付成功 →
　拒绝，状态不变
14 已完成 + 发货 → 拒绝
15 待支付 + 确认收货 → 拒绝
16 已取消 + 再次取消 →
　拒绝（幂等或报错，按规格）
17 已发货 + 支付成功 → 拒绝
18 任意状态 + 未知事件 →
　拒绝（不是崩溃）
19 已完成 + 任意事件 →
　终态全拒绝（抽查）
拒绝断言三要素：
状态未变、无副作用、
明确错误码

四、终态专项
· 已完成与已取消是终态：
　枚举所有事件，
　逐一断言不可转移
· 终态订单的写操作
　（修改金额等）全部拒绝

五、并发转移（进阶）
· 两个事件同时到达
　（支付与取消并发）→
　只有一个成功，
　另一个收到状态冲突
· 断言最终状态符合
　「先到先得」的规格语义
　（或按业务规则的优先级）

六、维护性设计
· 转移表与状态机定义
　同源维护（或从定义生成）
· 状态机变更 → 转移表变更 →
　用例自动跟随，
　评审时看表即知影响面

七、结果
19 用例，状态机变更
零回归逃逸；
非法转移用例上线前
拦截过一次「已取消订单
被重复支付」的缺陷。`,
  stats2: [['转移', '12+7 条'], ['终态', '枚举拒绝'], ['拦截', '重复支付']],
  qa: [
    ['非法转移用例为什么比合法转移更容易抓出真缺陷？', '因为合法路径是开发时反复走过的路，通常已经结实；而非法路径是「没人走过的路」——代码对它的处理往往是随手写的（或者根本没写）。已取消订单收到支付回调、已完成订单被重新发货，这些正是生产事故的高频剧本。测试的资源要向缺陷密度高的地方倾斜，非法转移就是那块富矿。'],
    ['表驱动相比手写用例的优势是什么？', '同构性与可审计性。状态机用例的结构完全一致（前置状态+事件→断言），手写 19 个是重复劳动，表驱动一行数据一个用例。更重要的是审计：转移表就是状态机的规格镜像，评审时一眼看出「哪条转移没测」——用例集与规格的差距变得可视化，而不是藏在几十段代码里。'],
    ['并发转移测试在防什么？', '状态机的单线程用例验证的是规则正确性，并发用例验证的是规则的唯一性：支付与取消同时到达，如果两个都成功，订单就进入了「既支付又取消」的矛盾态。真实系统的状态变更几乎都在并发环境里发生，锁或乐观并发的机制必须有测试背书——「先到先得」不能只是文档里的一句话。'],
  ],
  fbs: [
    ['后端负责人', '转移表用例让状态机变更再也没出过回归'],
    ['开发同学', '加新状态就是加一行表，测试自动跟上'],
    ['评审者', '看转移表就知道测试覆盖到哪了'],
  ],
  risks: ['只测「快乐路径」的状态机测试，等于只测了没下雨的天气预报', '非法转移与终态必须覆盖', '并发转移要有唯一性断言', '转移表与状态机定义同源维护'],
}),
({
  t: '重试逻辑测试 · 虚拟时钟', sub: '10 分钟写完', d: '编写重试逻辑测试：退避与终止条件验证',
  ly: 'timeline', pal: 'gold', bg: 'grid',
  facts: ['输入：重试规则', '输出：测试用例集', '用例 12 个'],
  inp: '粘贴重试规则', out: ['结构：失败序列驱动', '工具：虚拟时钟+假依赖', '覆盖：退避序列与终止'],
  stats: [['用例', '12 个'], ['时钟', '虚拟'], ['序列', '注入失败']],
  quote: '重试测试的诀窍：控制失败剧本（第几次成功）+ 拨快时钟，退避策略一秒验完',
  fb: ['中间件同学', '虚拟时钟把三天的重试逻辑测试压缩到三分钟'], risk: '用真实 sleep 测退避，测的是你的耐心，不是代码的策略',
  ctx: '重试组件：最多重试 5 次、指数退避（1/2/4/8/16 秒）、特定错误不重试。真实时钟下测完要等 31 秒，而且每次都是煎熬。用虚拟时钟+可编程失败依赖：注入「第 3 次成功」的剧本，时钟快进验证每次重试的间隔精确符合退避公式。12 个用例覆盖成功序列、重试耗尽、不重试错误、抖动范围——三分钟跑完，永远稳定。',
  raw: `被测组件：
远程调用重试器
规则：
· 最多重试 5 次
· 退避：1、2、4、8、16 秒
  + 0-20% 随机抖动
· 超时与 500 类错误重试
· 400 类错误不重试（直接失败）
· 成功即停

原始测试（反面教材）：
真依赖 + 真等待：
跑一遍全退避要 31 秒，
抖动导致断言只能写范围，
流水线里没人想跑它`,
  steps: [
    ['造假依赖', '可编程失败剧本', '3 分钟'],
    ['造虚拟时钟', '时间推进控制', '2 分钟'],
    ['设成功序列', '第 N 次成功的剧本', '2 分钟'],
    ['设终止用例', '耗尽与不重试', '2 分钟'],
    ['验抖动', '随机范围统计断言', '2 分钟'],
    ['固边界', '次数与时长上限', '1 分钟'],
  ],
  full: `【测试设计 · 重试逻辑（虚拟时钟）】

一、测试设施（两件）
1 可编程依赖：
　按剧本返回失败/成功——
　「前 2 次超时，第 3 次成功」
　「永远 500」「第 1 次就 400」
2 虚拟时钟：
　退避等待不真等，
　测试手动推进时间，
　并记录「下一次重试被安排
　在什么时刻」

二、成功序列用例（4 个）
1 首次即成功 → 不产生重试，
　总耗时 0
2 第 3 次成功 → 重试 2 次，
　断言两次重试的间隔分别
　落在 [1, 1.2] 与 [2, 2.4] 秒
　（退避基数 + 抖动上限）
3 第 5 次成功 → 重试 4 次，
　间隔序列逐个断言
4 成功后的行为 →
　立即返回，不再等待
　（「成功即停」验证）

三、终止条件用例（4 个）
5 永远失败 → 重试恰好 5 次后
　放弃，返回最后一次错误
　（断言调用次数 = 6
　= 首次 + 5 次重试）
6 400 错误 → 一次不重试，
　直接失败
　（不重试错误的清单验证）
7 超时错误 → 进入重试
8 混合剧本：先 500 后 400 →
　遇到 400 立即停止

四、退避数学用例（3 个）
9 间隔序列：
　1、2、4、8、16 逐一断言
　（指数退避的公式验证）
10 抖动范围：
　重复 100 次生成，
　统计全部落在
　[基数, 基数×1.2] 内
11 抖动随机性：
　100 次里至少出现 2 种
　不同间隔（防抖动退化为常数）

五、上限用例（1 个）
12 总时长上限：
　即使退避未走完，
　总时长超过配置上限
　（如 60 秒）也终止

六、断言规范
· 调用次数精确断言
　（重试次数是契约）
· 每次重试的时刻断言
　（虚拟时钟让时刻可断言——
　这是真实时钟做不到的）
· 错误传递断言：
　耗尽后返回的是
　「最后一次」的错误

七、结果
12 用例 3 分钟跑完
（原方案 31 秒/轮且不稳）；
一次退避公式回归
（有人把 ×2 改成 ×3）
被用例 9 抓出。`,
  stats2: [['用例', '12 个'], ['耗时', '31 秒→秒级'], ['抓缺', '退避回归']],
  qa: [
    ['虚拟时钟为什么让重试测试「从煎熬变享受」？', '重试逻辑的本质是「时间上的决策序列」，真实时钟强迫你陪代码一起等——31 秒测一遍，抖动还让断言只能写模糊范围。虚拟时钟把时间变成测试的自变量：想验证 16 秒后的重试？拨过去就是了。凡是逻辑依赖时间流逝的代码（重试、超时、过期），虚拟时钟都是把测试从「忍耐」变成「控制」的开关。'],
    ['「第 3 次成功」这类剧本化依赖的价值？', '它把「依赖的行为」变成了测试参数。真实依赖的失败是偶遇的，你无法指定「这次请恰好失败两次」；剧本化依赖让每个用例精确控制失败的次数与类型，重试逻辑的每条分支（成功即停、耗尽放弃、遇错即止）都能被定向触发。可控的失败，是重试测试的全部基础。'],
    ['抖动的「统计断言」为什么比单次断言更可靠？', '抖动是随机的：单次间隔落在范围内，可能只是运气。重复 100 次并断言「全部在范围内 + 至少出现两种值」，同时验证了边界（不越界）与活性（真随机而不是常数）——统计断言是把「随机行为的正确性」变成可判定命题的标准手法。单次抽查是赌博，分布验证才是测试。'],
  ],
  fbs: [
    ['中间件同学', '虚拟时钟把三天的重试逻辑测试压缩到三分钟'],
    ['开发同学', '退避公式改错被秒抓，重构有底气了'],
    ['流水线', '重试测试从「能跳就跳」变成必跑项'],
  ],
  risks: ['用真实 sleep 测退避，测的是你的耐心，不是代码的策略', '重试次数与时刻要精确断言', '抖动要统计验证，不靠单次抽查', '不重试错误清单逐条覆盖'],
}),
];
