/* 「SQL 写与优化」深度版案例（21-25） */

export const DEEP_SQL_5 = [
({
  t: 'OR 条件 · 拆分合击', sub: '8 分钟写完', d: '优化 OR 条件查询：拆分与索引合并',
  ly: 'code', pal: 'blue', bg: 'grid',
  facts: ['输入：OR 查询', '输出：优化方案', '提速 40 倍'],
  inp: '粘贴 OR 查询', out: ['结构：OR 拆分策略', '关键：索引利用恢复', '配套：等价验证'],
  stats: [['耗时', '1.6s→40ms'], ['策略', '拆联合'], ['索引', '各走各的']],
  quote: 'OR 是索引的天敌：一个 OR 可能让两边的索引同时失效——拆开它，让每个条件各走各的索引',
  fb: ['开发', '拆成两条查询合并后，两个索引都用上了，快了几十倍'], risk: '带 OR 的查询不检查执行计划就上线，等于埋了一个「索引集体罢工」的地雷',
  ctx: '查询条件是「手机号 = 某值 或 邮箱 = 某值」，两个字段各有索引，但执行计划显示全表扫描——OR 让优化器放弃了两个索引。拆分方案：改成两条查询分别走各自索引，结果合并去重；或者用数据库支持的索引合并能力。优化后 40 毫秒。',
  raw: `慢查询：
SELECT * FROM 用户
WHERE 手机号 = '某值'
OR 邮箱 = '某值'

现状：
· 手机号有索引
· 邮箱有索引
· 但执行计划：
  全表扫描

原因：
OR 连接两个不同列，
优化器难以同时
利用两个索引，
干脆全表扫`,
  steps: [
    ['看计划', '确认索引未被使用', '2 分钟'],
    ['析原因', 'OR 的索引障碍', '2 分钟'],
    ['选策略', '拆分或索引合并', '2 分钟'],
    ['写拆分', '两查询合并去重', '2 分钟'],
    ['验等价', '结果一致性', '1 分钟'],
    ['审频率', '高频场景固化', '1 分钟'],
  ],
  full: `【优化 · OR 条件（拆分合击）】

一、OR 的索引障碍
单列的 OR
（同一列多值）：
手机号 = 甲 或 手机号 = 乙
——可以走索引
　（多值探测）
跨列的 OR：
手机号 = 甲 或 邮箱 = 乙
——两个索引各管一段，
优化器常常选择放弃，
全表扫描
判别：
OR 两侧是不同列时，
优先怀疑索引失效

二、策略一 拆分合击（通用）
拆成两条独立查询：
查询 1：手机号 = 甲
　（走手机号索引）
查询 2：邮箱 = 乙
　（走邮箱索引）
应用层合并：
两边结果取并集，
按主键去重
每条查询都是
简单的索引探测，
各自几十毫秒

三、策略二 索引合并（看数据库）
部分数据库支持
索引合并优化：
两个索引各自探测，
结果在数据库内部合并
· 查执行计划确认
　是否支持并生效
· 生效则一条语句解决
· 不生效则回到策略一
策略选择：
能自动合并就用自动，
不能就手工拆

四、等价性验证
拆分改写必须验证：
· 结果集一致
　（含去重语义）
· 空值行为一致
· 排序与分页语义
　保持一致
　（合并后重新排序）
改写不是看起来像，
是比出来一样

五、高频场景固化
如果这是登录类查询
（手机号或邮箱登录）：
· 拆分逻辑封装在
　数据访问层
· 或业务层先判断
　输入形态
　（像手机号查手机号，
　像邮箱查邮箱）
　——从源头消灭 OR
最好的优化是
让 OR 不出现

六、结果
1.6 秒 → 40 毫秒；
「跨列 OR」进了
查询评审的注意清单。`,
  stats2: [['耗时', '1.6s→40ms'], ['索引', '双双复工'], ['根治', '源头判断']],
  qa: [
    ['跨列 OR 为什么会让索引失效？','因为每个索引只了解自己的列。执行「手机号=甲」可以查手机号索引，执行「邮箱=乙」可以查邮箱索引，但「满足其一」要求数据库把两条索引路径的结果合并——这需要额外的合并操作，而优化器估算后常认为「两个索引探测+合并」不如直接全表扫描划算（尤其表不大或选择性不高时），于是放弃索引。失效不是索引坏了，是优化器在 OR 面前算了一笔不划算的账。'],
    ['拆分后应用层合并要注意什么？','三件事：去重（同一行可能同时满足两个条件，两条查询都返回它，按主键去重）、排序（两条结果各自有序，合并后要统一重排，否则分页会乱）、分页（不能在两条查询上各取一页再拼——要先合并再切页，或改用其他分页策略）。拆分把数据库的工作搬到了应用层，搬过来的每件事都要接住，否则功能正确性会悄悄受损。'],
    ['「从源头消灭 OR」的思路适用于哪些场景？','适用于 OR 两侧代表「同一意图的不同载体」的场景。登录查询最典型：用户输入可能是手机号也可能是邮箱，但业务上就是「找这个账号」——与其让数据库处理「手机号或邮箱」，不如在入口先识别输入形态（数字串查手机号字段、含特定符号查邮箱字段），每次查询都是单列等值。当 OR 表达的是「输入的不确定性」而不是「条件的并集」时，把不确定性在业务层解决，比推给数据库更优雅。'],
  ],
  fbs: [
    ['开发', '拆成两条查询合并后，两个索引都用上了，快了几十倍'],
    ['产品', '登录入口识别输入形态，查询天然变简单'],
    ['评审者', '跨列 OR 进了评审注意清单'],
  ],
  risks: ['带 OR 的查询不检查执行计划就上线，等于埋了一个「索引集体罢工」的地雷', '跨列 OR 优先怀疑索引失效', '拆分合并要去重与重排', '能源头消灭 OR 就不要推给数据库'],
}),
({
  t: 'EXISTS 还是 IN · 看谁小', sub: '8 分钟写完', d: '选择子查询形式：按数据量决定写法',
  ly: 'chat', pal: 'green', bg: 'paper',
  facts: ['输入：包含查询', '输出：选型结论', '判据 1 条'],
  inp: '粘贴查询需求', out: ['结构：大小判据选型', '关键：内外表行数', '配套：计划验证'],
  stats: [['判据', '谁小用谁'], ['验证', '计划对比'], ['提速', '25 倍']],
  quote: '「EXISTS 还是 IN」没有标准答案，只有一条判据：小集合在内侧——先看行数，再选写法，最后用计划验证',
  fb: ['开发', '按判据改写后快了 25 倍，团队再不为这个选择题吵架了'], risk: '背「EXISTS 一定比 IN 好」的口诀写代码，和抛硬币做选型没有区别',
  ctx: '团队里「EXISTS 和 IN 哪个好」争论不休。其实判据只有一条：让小的数据集参与内层循环。外层大、子查询小用 IN（小集合建探测表）；外层小、子查询大用 EXISTS（外层逐行探测，命中即停）。两个真实案例分别验证，各提速一个量级，且都以执行计划为准。',
  raw: `争论话题：
「EXISTS 还是 IN？」

常见误区：
背口诀
（「EXISTS 永远更好」）

真相：
取决于两侧数据量

判据：
小集合在内侧
· 外层大 + 子查询小：
  子查询结果建探测表，
  外层逐行匹配
· 外层小 + 子查询大：
  外层逐行探测，
  命中即停

验证方式：
永远看执行计划`,
  steps: [
    ['数行数', '两侧数据量估算', '2 分钟'],
    ['套判据', '小在内侧定写法', '2 分钟'],
    ['写两版', '两种形式都写', '2 分钟'],
    ['跑计划', '对比实际成本', '2 分钟'],
    ['定结论', '以计划为准', '1 分钟'],
    ['留记录', '案例沉淀', '1 分钟'],
  ],
  full: `【选型 · EXISTS 还是 IN】

一、争论的终结方式
不是选边站，
是建立判据：
两种写法的成本都来自
「谁被循环、循环多少次」
让小的那侧
承担被构建/被探测的角色，
成本就低
口诀化的口诀：
小集合在内侧

二、场景一：外层大、子查询小
例：订单表（800 万）
筛选「属于活跃用户」
（活跃用户 3 万）
优选：
子查询先算出
3 万个活跃用户，
建探测结构，
800 万订单逐行匹配
——内层小而快
此形态下
「在……之中」写法
通常被优化成半连接

三、场景二：外层小、子查询大
例：当前用户的 20 条订单，
判断「是否存在异常记录」
（异常表 2000 万）
优选：
外层 20 条逐条探测，
每条在异常表索引里
命中即停
——外层小，
探测次数少
此形态下存在性写法
天然合适

四、执行计划验证（铁律）
判据给方向，
计划给事实：
· 两版都写出来
· 各自跑执行计划
· 比估算成本与实际行数
· 选计划更好的那版
现代优化器有时会把
两种写法优化成
同一种执行形态——
此时选可读性好的
判据是路标，
计划是裁判

五、注意空值差异
「在……之中」与空值：
子查询结果含空值时，
「在……之中」的行为
可能不符合直觉
（三值逻辑）
存在性写法对空值
更可控
涉及可空列时，
显式处理空值，
别依赖默认行为

六、沉淀
· 判据 + 两个真实案例
　入库
· 团队结论：
　不背口诀，
　先看行数，
　再信计划

七、结果
两个案例各提速
一个量级；
选题争论终结。`,
  stats2: [['判据', '小在内侧'], ['验证', '计划裁判'], ['提速', '量级']],
  qa: [
    ['「小集合在内侧」的成本逻辑是什么？','嵌套执行的成本约等于「外层行数 × 单次内层成本」。当子查询结果集很小时，把它先物化成探测结构（哈希表或索引），外层每行的匹配是近似常数成本——总成本由外层行数主导，内层几乎免费；反之如果外层很小，让它逐行去大表里做索引探测，探测次数等于外层行数，也很少。两种情况都是让小的一侧承担「被反复使用」的角色，大的一侧只过一遍。'],
    ['为什么现代数据库里「两种写法可能被优化成同一种形态」？','因为优化器看的是语义而不是语法：它发现两种写法表达的是同一个关系运算（半连接），就会选择它认为最优的物理实现，不管你用的是哪种关键字。这意味着写法之争在很多时候是「假问题」——真正影响性能的是统计信息准不准、索引有没有。所以流程上先跑计划：如果两版计划相同，直接选可读性好的；如果不同，按成本选。'],
    ['涉及可空列时两种写法的行为差异为什么重要？','因为三值逻辑对「在……之中」的影响是实打实的：如果子查询的结果集里包含空值，「某值在这个集合中吗」对任何外层值都可能返回「未知」，导致整行被过滤——结果集诡异地变空或变少。存在性写法用「存在匹配的行」来表达，空值语义更直观可控。这不是理论洁癖：生产里「子查询突然多了空值导致主查询结果消失」的事故真实发生过。涉及可空列，先想清楚空值去哪了。'],
  ],
  fbs: [
    ['开发', '按判据改写后快了 25 倍，团队再不为这个选择题吵架了'],
    ['评审者', '「先看行数再信计划」成了标准流程'],
    ['新同事', '两个案例比任何口诀都好记'],
  ],
  risks: ['背「EXISTS 一定比 IN 好」的口诀写代码，和抛硬币做选型没有区别', '选型先看两侧行数', '最终以执行计划为准', '可空列注意三值逻辑行为'],
}),
({
  t: 'UPDATE 关联 · 先查后改', sub: '10 分钟写完', d: '编写关联更新：先查后改与影响面控制',
  ly: 'doc', pal: 'orange', bg: 'dots',
  facts: ['输入：更新需求', '输出：安全方案', '步骤 4 步'],
  inp: '粘贴更新需求', out: ['结构：先查后改流程', '关键：影响面预览', '配套：事务与备份'],
  stats: [['流程', '4 步'], ['预览', '影响面'], ['回滚', '留档']],
  quote: '更新语句的正确打开方式：先用查询看清要动哪些行，再决定动不动——写操作永远比读操作多一道仪式',
  fb: ['开发', '先查后改的习惯拦下过一次「条件写错差点更新全表」的事故'], risk: '不看影响面直接执行更新，等于闭着眼睛挥刀——条件差一个字符，范围差一百万行',
  ctx: '一次条件写错的更新差点改全表，从此立规矩：关联更新必须「先查后改」——先用等价的查询语句看影响面（多少行、都是谁），确认无误再执行更新；大更新分批做；执行前留备份或确认回滚方案。四步流程固化后，更新类事故归零。',
  raw: `事故背景：
一次关联更新条件少写一项，
影响行数从预期的 3000
变成 300 万，
幸被慢查询告警拦下

先查后改流程：
1 把更新条件写成查询
2 看影响面：
  行数 + 抽样明细
3 确认后再执行更新
4 大更新分批 + 留回滚`,
  steps: [
    ['译查询', '更新条件转查询', '2 分钟'],
    ['看影响', '行数与抽样确认', '3 分钟'],
    ['定批次', '大更新分片策略', '2 分钟'],
    ['留后路', '备份或回滚方案', '2 分钟'],
    ['小事务', '每批独立提交', '1 分钟'],
    ['验结果', '更新后抽查', '1 分钟'],
  ],
  full: `【规范 · UPDATE 关联（先查后改）】

一、第一步：更新译查询
任何更新先写出
它的「影子查询」：
更新：
UPDATE 订单
SET 状态 = 新状态
WHERE 条件
影子查询：
SELECT 主键, 关键列
FROM 订单
WHERE 同条件
LIMIT 若干
两个语句的条件
必须一字不差
（复制粘贴，
不手打第二遍）

二、第二步：看影响面
影子查询回答三问：
1 多少行？
　（与预期量级对比：
　预期 3000 查出 300 万 =
　条件错了，停！）
2 都是谁？
　抽样看明细，
　确认命中的是
　该命中的行
3 有没有意外形态？
　（空值行、边界行）
三问都符合预期，
才进入执行

三、第三步：分批执行
大更新不一条打完：
· 按主键范围分批
　（每批几千行）
· 每批独立事务
　（锁持有时间短）
· 批间可暂停可观察
· 失败批记录重试
（参考分片提交策略：
　批量与事务的两全）

四、第四步：留后路
执行前确认回滚方式：
· 可逆更新：
　记下反向语句
· 不可逆更新：
　先备份目标行
　（影子查询的结果
　存档）
· 关键表更新：
　低峰期 + 有人值守
没有后路的更新
不执行

五、执行后验证
· 计数验证：
　实际更新行数 =
　影子查询行数
· 抽样验证：
　随机抽几行看结果
· 关联影响检查：
　依赖该字段的其他
　查询是否正常

六、流程的纪律本质
读操作错了可以重来，
写操作错了可能不可逆——
所以写操作多一道仪式：
先看清（查询），
再动手（更新），
留后路（回滚）
仪式不是繁琐，
是对不可逆性的尊重

七、结果
更新类事故归零；
「影子查询」成为
团队肌肉记忆。`,
  stats2: [['流程', '4 步固化'], ['拦截', '全表事故×1'], ['事故', '归零']],
  qa: [
    ['「影子查询」为什么必须和更新语句「一字不差」？','因为先查后改的全部价值在于：查询看到的集合就是更新将作用的集合。如果两个条件是分别手写的，哪怕只差一个空格位置或一个运算符，查询验证的就是另一个集合——仪式照走，保护失效。复制粘贴保证的是「同一性」：影子查询不是「差不多的查询」，是更新语句的读取形态。安全流程的有效性，取决于环节之间的严格一致。'],
    ['「预期 3000 查出 300 万」这个对比为什么能拦住事故？','因为它引入了独立参照：执行者心里对影响范围有一个业务预期（这次要改的就是那一批数据），影子查询给出的是条件实际命中的范围。两者量级对不上，说明「条件的语义」和「心里的意图」脱节了——条件少写一项、关联写错、类型隐式转换，都会造成这种脱节。量级对比是最便宜的语义校验：不需要理解条件怎么写的，只需要对比两个数字。'],
    ['大更新分批除了性能，还在保护什么？','保护可干预性。一条更新 300 万行的大事务，一旦执行就没有中途叫停的机会：锁拿着、日志写着、回滚比执行还慢。分批之后，每一批都是一个决策点：发现不对可以立刻停止，损失锁定在已执行的批次；每批独立提交也保证了锁的短暂持有，其他业务不被长时间阻塞。分批的本质是把「一次不可逆的大动作」变成「一串可随时停的小动作」。'],
  ],
  fbs: [
    ['开发', '先查后改的习惯拦下过一次「条件写错差点更新全表」的事故'],
    ['运维', '分批更新让线上写操作不再提心吊胆'],
    ['新同事', '影子查询的规矩第一天就学会了'],
  ],
  risks: ['不看影响面直接执行更新，等于闭着眼睛挥刀——条件差一个字符，范围差一百万行', '影子查询与更新条件必须一字不差', '大更新必须分批独立事务', '不可逆更新先备份'],
}),
({
  t: '区间重叠 · 边界公式', sub: '8 分钟写完', d: '编写区间查询：重叠判断与边界处理',
  ly: 'mind', pal: 'purple', bg: 'grid',
  facts: ['输入：区间需求', '输出：查询方案', '公式 1 条'],
  inp: '粘贴区间需求', out: ['结构：重叠公式推导', '关键：开闭边界定义', '配套：边界用例'],
  stats: [['公式', '1 条通用'], ['边界', '开闭定义'], ['用例', '5 组']],
  quote: '区间重叠判断只需要一条公式：「甲开始 < 乙结束 且 乙开始 < 甲结束」——但开闭边界不定义清楚，公式也会骗人',
  fb: ['开发', '会议室预订的重叠冲突检测上线后零漏判，全靠边界定义先行'], risk: '不定义开闭语义就写区间逻辑，「10:00-11:00 和 11:00-12:00 算不算冲突」就会成为永恒的争论',
  ctx: '会议室预订系统要判断时间段冲突。先定义边界语义：预订区间是左闭右开（10:00-11:00 与 11:00-12:00 不算冲突，可以背靠背）。再用重叠公式：新区间开始 < 已有结束 且 已有开始 < 新区间结束。配五组边界用例（相邻、包含、被包含、交叉、相离）验证，冲突检测零漏判。',
  raw: `需求：
会议室预订冲突检测
已有预订区间集合，
判断新区间是否冲突

第一步（最重要）：
边界语义定义
· 预订区间左闭右开：
  [10:00, 11:00)
· 背靠背允许：
  10-11 与 11-12 不冲突

第二步：
重叠公式
重叠 = 甲开始 < 乙结束
且 乙开始 < 甲结束

第三步：
五组边界用例验证`,
  steps: [
    ['定语义', '开闭边界约定', '2 分钟'],
    ['推公式', '重叠判断推导', '2 分钟'],
    ['写查询', '冲突检测语句', '2 分钟'],
    ['配用例', '五组边界验证', '2 分钟'],
    ['审索引', '区间列索引', '1 分钟'],
    ['固规格', '语义写入文档', '1 分钟'],
  ],
  full: `【设计 · 区间重叠（边界公式）】

一、边界语义（先于代码）
区间有两套世界：
· 左闭右开 [起, 止)：
　止点不属于区间——
　适合时间段类场景
　（背靠背无缝衔接）
· 双闭 [起, 止]：
　两端都属于——
　适合日期覆盖类场景
本例选左闭右开：
10:00-11:00 的会议室
11:00 就可以给下一场
语义先定，
公式才有意义

二、重叠公式推导
反向思考更简单：
什么时候「不重叠」？
甲完全在乙之前：
甲结束 ≤ 乙开始
或 甲完全在乙之后：
甲开始 ≥ 乙结束
取反即重叠：
甲开始 < 乙结束
且 乙开始 < 甲结束
（左闭右开语义下
　用严格小于；
　双闭语义则用小于等于——
　公式的比较符
　跟着边界定义走）

三、冲突检测查询
SELECT 已有预订
FROM 预订表
WHERE 会议室 = 目标
AND 已有开始 < 新结束
AND 新开始 < 已有结束
有结果 = 冲突
无结果 = 可预订
（注意：
　比较符与边界语义
　严格对应）

四、五组边界用例
1 相离：
　9-10 与 11-12 → 不冲突
2 相邻（背靠背）：
　10-11 与 11-12 → 不冲突
　（左闭右开的关键用例）
3 交叉：
　10-12 与 11-13 → 冲突
4 包含：
　9-15 与 10-11 → 冲突
5 被包含：
　10-11 与 9-15 → 冲突
每组断言明确，
尤其用例 2：
它是语义选择的
试金石

五、索引与性能
· 会议室 + 时间列
　联合索引
· 区间查询的范围扫描
　配合过滤
· 高频预订表可加
　时间范围分区

六、语义入文档
把「左闭右开 +
　背靠背允许」
写进功能规格——
未来任何争议，
以文档为准
（边界语义是产品决策，
　不是实现细节）

七、结果
冲突检测零漏判；
「背靠背算不算冲突」
的争论在文档里终结。`,
  stats2: [['公式', '1 条通用'], ['用例', '5 组边界'], ['漏判', '零']],
  qa: [
    ['重叠公式为什么用「反向取反」推导更不容易错？','因为「重叠」的形状有很多种（交叉、包含、被包含），逐一枚举容易漏；而「不重叠」只有两种形状（完全在左、完全在右），穷尽它们只要两个条件。取反之后得到的重叠条件天然完备——这是区间逻辑的经典推导路径：数「不」比数「是」简单。凡是需要枚举多种形态的判断，都值得先问：它的反面是不是更简单？'],
    ['比较符为什么要跟着边界语义走？','因为公式里的每一个小于或小于等于，都是边界归属的声明。左闭右开下「甲结束 ≤ 乙开始」表示甲的止点（不属于甲）与乙的起点（属于乙）重合时不冲突——背靠背成立；如果误写成严格小于，背靠背就会被误判冲突。双闭语义下一切反过来。公式不是死记的字符串，是语义的翻译件——语义改了，比较符必须跟着改，否则公式就在说谎。'],
    ['「背靠背用例」为什么是试金石？','因为它恰好站在两种语义的分界线上：10-11 与 11-12，在左闭右开世界里不冲突，在双闭世界里冲突。这个用例的通过与否直接暴露系统实际采用（或错误混用）了哪种语义。边界用例的价值就在于此：它们不测常规路径，专测定义分歧点——用例 2 通过，说明公式与语义对齐；用例 2 失败，说明某处比较符用错了。一条用例，检验整个体系的一致性。'],
  ],
  fbs: [
    ['开发', '会议室预订的重叠冲突检测上线后零漏判，全靠边界定义先行'],
    ['产品', '背靠背语义写进规格，争议终结'],
    ['测试', '五组边界用例直接成了回归集'],
  ],
  risks: ['不定义开闭语义就写区间逻辑，「10:00-11:00 和 11:00-12:00 算不算冲突」就会成为永恒的争论', '比较符必须与边界语义对应', '背靠背用例是必测项', '边界语义是产品决策要入文档'],
}),
({
  t: '递归查询 · 组织树展开', sub: '10 分钟写完', d: '编写递归查询：层级展开与深度保护',
  ly: 'terminal', pal: 'cyan', bg: 'dark',
  facts: ['输入：树形数据', '输出：递归方案', '特性 4 项'],
  inp: '粘贴树形需求', out: ['结构：递归公用表', '关键：终止与深度保护', '配套：环路防护'],
  stats: [['层级', '任意深度'], ['保护', '深度上限'], ['环路', '防护']],
  quote: '递归查询的两件保命事：必须有终止条件，必须有深度上限——树可以有无限深，查询不能',
  fb: ['开发', '深度上限救过一次脏数据：一条记录把自己设成了自己的父节点，差点无限递归'], risk: '没有深度保护的递归查询，遇到一条脏数据就能把数据库拖进无限循环',
  ctx: '组织架构要按层级展开（给定部门，列出它的所有下级）。用递归公用表表达式：锚点查询（起始部门）+ 递归部分（逐层向下关联），配两件保护：深度上限（超过 10 层停止并告警）与环路防护（已访问节点不重复进入）。一条「自己是自己父节点」的脏数据被保护机制拦下。',
  raw: `需求：
组织架构树
给定一个部门，
展开所有下级
（任意层级）

数据形态：
部门表（编号, 名称, 父编号）
树形自关联

递归方案：
递归公用表表达式
锚点：起始部门
递归：子部门逐层展开

保护机制：
1 深度上限
2 环路防护
（脏数据防御）`,
  steps: [
    ['理结构', '树形数据形态', '2 分钟'],
    ['写锚点', '起始层查询', '2 分钟'],
    ['写递归', '逐层展开关联', '3 分钟'],
    ['配保护', '深度与环路防护', '2 分钟'],
    ['测脏数据', '环路用例', '1 分钟'],
    ['审性能', '大树的展开成本', '1 分钟'],
  ],
  full: `【设计 · 递归查询（组织树展开）】

一、树形数据的递归结构
部门表自关联：
每个部门指向父部门
向下展开 =
沿着「父编号」
一层层走
递归公用表表达式
两段式：
锚点（从哪开始）+
递归（如何向下）

二、锚点查询
从起始部门出发：
SELECT 编号, 名称,
1 作为 层级
FROM 部门
WHERE 编号 = 起始部门
产出第一层结果，
带层级标记

三、递归部分
用上一层的结果
找下一层：
SELECT 子部门信息,
上一层级 + 1
FROM 部门
JOIN 上一层结果
ON 部门.父编号
= 上一层.编号
WHERE 层级 < 上限
逐层生长，
直到没有新的子部门
（自然终止）
或达到上限（强制终止）

四、保护一：深度上限
· 层级超过 10 停止
　（按业务树的实际
　深度设定）
· 触顶记录告警：
　「组织树深度异常」
理由：
正常的组织树
不会深不见底——
无限深度只可能来自
脏数据或环路
上限是哨兵，
不是限制

五、保护二：环路防护
· 展开时记录
　已访问节点集合
· 子节点已在集合中 →
　不再进入
· 或依赖数据库的
　环路检测能力
防御对象：
A 的父是 B，
B 的父是 A
（或自己指向自己）

六、脏数据演练
构造环路数据：
某部门的父编号
指向自己
无保护：无限递归
有保护：
触顶停止 + 告警
演练确认保护有效
（保护机制要演练，
　不能只写在代码里）

七、性能与边界
· 大树展开行数可能很多：
　先过滤再展开
　（只展开需要的分支）
· 层级列可用于
　按层筛选
· 高频查询可考虑
　路径冗余化
　（每行存完整祖先路径，
　空间换时间）

八、结果
任意深度展开可用；
自引用脏数据被拦下；
递归查询有了
安全护栏。`,
  stats2: [['展开', '任意深度'], ['保护', '双保险'], ['拦下', '自引用脏数据']],
  qa: [
    ['递归查询为什么必须有「自然终止」之外的「强制终止」？','自然终止依赖数据的正确性：树结构完好时，走到叶子节点就没有下一层了。但数据可能不正确——环路（A→B→A）会让「下一层」永远存在，自然终止条件永远不满足。强制终止（深度上限）不信任数据的完好性：无论结构如何，层数到了就停。这是防御式设计的通则：凡依赖外部数据终止的循环，都要配一个不依赖数据的终止阀。'],
    ['「自己是自己的父节点」这类脏数据是怎么产生的？','通常来自缺乏约束的更新操作：修改父节点时没有校验「新父节点不能是自己或自己的后代」，一次误操作或一个接口缺陷就造出了环路。这类数据平时无害（不查那条路径就发现不了），一旦递归查询经过就引爆。所以两手都要硬：写入侧加校验（禁止成环的父子关系），查询侧加防护（深度与环路兜底）——数据质量的防线从来都是双向的。'],
    ['路径冗余化（存完整祖先路径）适合什么场景？','适合「查祖先/查后代」远多于「改结构」的场景。递归展开每次都要逐层关联，树大时成本可观；如果每行直接存「祖先路径」（如「总部/事业部/部门」），查某节点的所有后代变成一个前缀匹配——查询从递归降维成普通过滤。代价是结构变更时要同步更新子树的路径。读多写少的树，用空间换时间；频繁调整的树，保持邻接表+递归。'],
  ],
  fbs: [
    ['开发', '深度上限救过一次脏数据：一条记录把自己设成了自己的父节点，差点无限递归'],
    ['运维', '触顶告警让脏数据当天就被修正'],
    ['产品', '按层筛选和路径展示都很顺'],
  ],
  risks: ['没有深度保护的递归查询，遇到一条脏数据就能把数据库拖进无限循环', '深度上限与环路防护缺一不可', '写入侧要校验禁止成环', '保护机制要演练验证'],
}),
];
