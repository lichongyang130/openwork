/* 「Bug 诊断」深度版案例（1-5） */

export const DEEP_BUG_1 = [
({
  t: '空指针故障 · 链路还原', sub: '25 分钟定位', d: '诊断空指针崩溃：调用链还原与根因',
  ly: 'terminal', pal: 'red', bg: 'dark',
  facts: ['输入：崩溃堆栈', '输出：根因报告', '定位 25 分钟'],
  inp: '粘贴崩溃堆栈', out: ['根因：上游返回 null 未约定', '传播链 4 层定位', '修复+防御双层方案'],
  stats: [['链路', '4 层'], ['根因', '契约缺失'], ['耗时', '25 分']],
  quote: '空指针不是崩在报错那行：它崩在四层之前某个没说清楚的约定',
  fb: ['后端负责人', '链路还原法比盯着报错行快十倍'], risk: '只修报错行是贴创可贴：下次换个地方崩',
  ctx: '晚高峰订单服务批量报空指针崩溃。报错行是个无害的字段读取，第一反应是加判空——但诊断没有停在那里：沿调用链向上还原四层，发现真正的根因是用户服务在特定时段返回 null（而不是约定的空对象），而调用方从未防过这种返回。修报错行是止血，修契约才是治病。',
  raw: `故障现象：
19:40 起订单服务空指针异常激增（每分钟 300+）
报错堆栈（顶层）：
  OrderBuilder.buildDiscount(OrderBuilder.java:88)
  UserService.getProfile() 返回值的字段读取

初步信息：
· 报错行：profile.getLevel()，
  profile 来自 userService.getProfile(uid)
· 19:30 用户服务刚发过版
· 受影响用户集中在「新注册未完善资料」群体
· 订单主流程本身无变更

迷惑点：
报错行代码半年没动过，为什么今天崩？`,
  steps: [
    ['读堆栈', '确定崩溃点与直接原因', '3 分钟'],
    ['查时序', '崩溃起点与变更对齐', '5 分钟'],
    ['复现样本', '抓一个崩溃用户的完整数据', '6 分钟'],
    ['还原链路', '逐层确认 null 的来源', '8 分钟'],
    ['定根因', '契约不一致的双方', '2 分钟'],
    ['出方案', '止血+根治+防回归', '6 分钟'],
  ],
  full: `【Bug 诊断报告 · 空指针故障】

一、现象与时间线
· 19:30 用户服务发版
· 19:40 订单服务空指针激增（300+/分）
· 时序强相关：发版后 10 分钟发作

二、链路还原（关键动作）
不修报错行，先还原 null 的旅程：
第 1 层：报错行 profile.getLevel()
　→ profile 为 null
第 2 层：调用 userService.getProfile(uid)
　→ 该接口对新用户返回 null
第 3 层：查用户服务本次发版 diff
　→ 重构了资料查询：查不到资料时
　　从「返回默认对象」改为「返回 null」
第 4 层：查接口文档与调用方代码
　→ 文档从未写明查不到时的行为；
　　订单服务按「必有对象」编写
根因：跨服务契约缺失——
　「查无此人返回什么」从未被定义，
　上游一改，下游就崩

三、修复方案（三层）
1 止血（10 分钟）：
　用户服务回滚，或热改返回默认对象
2 根治（上游）：
　明确契约：查无资料返回空对象
　（含默认等级），永不返回 null；
　契约写进接口文档
3 防御（下游）：
　getProfile 结果判空 + 降级默认等级；
　「别人承诺的」也要自己设防

四、防回归
· 契约测试：上游补「新用户返回空对象」
　用例，下游补「收到 null 不崩溃」用例
· 跨服务变更检查单：返回值语义变化
　= 破坏性变更，需通知所有调用方

五、复盘结论
空指针的病灶不在崩溃点，在约定缺失处。
修复成本 0.5 人日，
同类隐患排查（全库搜该接口调用方）2 小时。`,
  stats2: [['止血', '10 分钟'], ['调用方排查', '7 处'], ['契约测试', '已补']],
  qa: [
    ['为什么「报错行代码没动过」反而是重要线索？', '因为崩溃的必要条件是「输入变了」。代码不变而行为变，说明上游数据流发生了变化——这直接把排查方向从「这行有什么问题」转向「谁喂了它新东西」。时序对齐（崩溃起点与发版时间）再一确认，根因范围就从全系统缩到一次发版。'],
    ['「只加判空」为什么是创可贴？', '判空修的是症状：这次崩在 getLevel，下次同样的契约缺失会崩在别的字段、别的服务。更糟的是判空掩盖了真实问题——上游的错误返回继续存在，只是不再尖叫。根因修复永远要问「这个 null 凭什么存在」，而不是「怎么让它不报错」。'],
    ['跨服务契约为什么容易缺失？', '因为「查不到返回什么」这类边界行为，在双方都顺利的开发期从不被讨论：上游随手写了个 null，下游恰好总有数据，相安无事数月。契约的漏洞都藏在边界情况里，而边界情况只在文档和测试里显形——没有文档化契约，跨服务协作就是在赌运气。'],
  ],
  fbs: [
    ['后端负责人', '链路还原法比盯着报错行快十倍'],
    ['用户服务同学', '发版检查单加了一条：返回语义变更要广播'],
    ['值班同学', '止血回滚十分钟，没等到定位完才动手'],
  ],
  risks: ['只修报错行是贴创可贴：下次换个地方崩', '止血与根治分开执行，别混在一起', '返回值语义变更属于破坏性变更', '排查同类调用方，防别处复发'],
}),
({
  t: '越界崩溃 · 边界三问', sub: '20 分钟定位', d: '诊断数组越界：边界条件系统排查',
  ly: 'code', pal: 'blue', bg: 'grid',
  facts: ['输入：崩溃日志', '输出：根因报告', '定位 20 分钟'],
  inp: '粘贴崩溃日志', out: ['根因：空列表未防直接取下标', '边界三问排查法', '防御式改造'],
  stats: [['三问', '全过'], ['根因', '空集合'], ['耗时', '20 分']],
  quote: '越界排查三问：最小是多少、最大是多少、空了怎么办',
  fb: ['客户端负责人', '边界三问进审查清单后，越界崩溃基本绝迹'], risk: '「数据不可能为空」的假设，是越界崩溃的温床',
  ctx: '客户端启动崩溃率突增 0.8%，堆栈指向首页配置解析的下标访问。用「边界三问」排查：配置列表平时几十条、最大几百条——那空的时候呢？抓到崩溃用户的原始数据：配置接口返回了空列表（该用户的灰度分组无配置），代码直接取第 0 项。所有越界的故事，主角都是没被考虑过的边界。',
  raw: `故障现象：
版本发布后启动崩溃率 +0.8%，
堆栈固定：
  HomeConfigParser.parse(ConfigParser.kt:42)
  Index 0 out of bounds for length 0

关键线索：
· 报错行：configs.get(0)，
  取「主配置项」
· 历史：该行上线两年从未崩溃
· 崩溃用户画像：集中在某新灰度分组
· 配置接口对该分组返回空列表
  （运营还没配内容）

第一反应（错误示范）：
加个 isNotEmpty 判断 —— 治标`,
  steps: [
    ['读堆栈', '越界的方向（上界还是下界）', '2 分钟'],
    ['抓样本', '崩溃用户的真实输入', '5 分钟'],
    ['问三问', '最小/最大/空三个边界', '6 分钟'],
    ['找假设', '代码隐含了哪个假设', '4 分钟'],
    ['定修复', '防御+默认值策略', '2 分钟'],
    ['防回归', '空数据用例补进测试', '3 分钟'],
  ],
  full: `【Bug 诊断报告 · 启动越界崩溃】

一、现象
· 新版本启动崩溃率 +0.8%
· 堆栈 100% 聚集在配置解析第 42 行：
　configs.get(0) 越界（length 0）
· 崩溃用户集中在某新灰度分组

二、边界三问（系统排查法）
问 1：这个列表最小能是多少？
　→ 0。配置接口对「无配置的分组」
　　返回空列表——这是合法返回值，
　　只是从没人给它写过用例
问 2：最大能是多少？
　→ 几百条，代码用遍历处理，无风险
问 3：空了怎么办？
　→ 代码回答是：没想过。
　　直接取 get(0)，空即崩
三问结论：代码隐含假设
　「配置列表至少有一项」，
　该假设在新灰度分组下首次被打破

三、根因
灰度新分组 + 运营未配置 =
空配置列表成为真实输入；
两年「从未崩溃」只是因为
空列表从未发生过——
不是代码健壮，是运气好

四、修复（三层）
1 防御：解析入口统一判空，
　空列表返回默认配置对象
2 默认值策略：产品定义「无配置」
　的兜底体验（而不是崩溃或空白）
3 测试：空列表、单条、几百条
　三个边界用例进回归

五、防回归制度
· 「边界三问」进代码审查清单：
　任何集合/下标访问，问最小、最大、空
· 灰度发布检查单新增：
　新分组的数据边界（含空数据）
　必须有用例覆盖
· 配置后台加校验：灰度分组
　发布前强制要求至少一条配置

六、复盘
崩溃率 0.8% 当天归零。
真正的修复不是那行判空，
是把「没想过」变成「想过」。`,
  stats2: [['崩溃率', '0.8%→0'], ['边界用例', '3 个'], ['审查清单', '三问']],
  qa: [
    ['「上线两年没崩」为什么反而是危险信号？', '因为它制造了虚假的健壮感：代码没崩不是因为处理了所有输入，而是某些输入从未出现过。灰度分组、新用户类型、极端数据——每次业务变化都在生成「从未出现过的输入」。审查老代码时，「从来没出过问题」应该翻译成「它的边界从没人测过」。'],
    ['边界三问为什么比「加判空」高一个层次？', '加判空是战术动作，三问是战术背后的侦察：最小、最大、空，三个问题把集合类输入的全部边界画出来，每个边界都要求代码有明确答案。判空只是「空」这一问的答案——如果没问，你永远不知道自己漏的是哪一问。'],
    ['为什么修复要包含「产品定义的默认体验」？', '技术上判空之后有两种写法：返回空（用户看到空白页）或返回默认配置（用户看到基础体验）。这是产品决策不是代码决策——空配置组的用户也值得一个可用的首页。Bug 修复的质量上限，往往取决于「异常之后给用户什么」，而不只是「不崩溃」。'],
  ],
  fbs: [
    ['客户端负责人', '边界三问进审查清单后，越界崩溃基本绝迹'],
    ['运营同学', '后台加了配置必填校验，从源头堵住'],
    ['测试同学', '空数据用例补进去，回归踏实多了'],
  ],
  risks: ['「数据不可能为空」的假设，是越界崩溃的温床', '修复要带默认体验设计，别只防崩溃', '灰度新分组的数据边界必须测试', '老代码的「从未崩溃」不等于健壮'],
}),
({
  t: '时差错 8 小时 · 时区追凶', sub: '30 分钟定位', d: '诊断时间偏差：时区链路的逐段排查',
  ly: 'sheet', pal: 'gold', bg: 'linen',
  facts: ['输入：异常数据', '输出：根因报告', '定位 30 分钟'],
  inp: '粘贴异常现象', out: ['根因：容器时区与代码假设不一致', '时区链路四段排查', '全链路统一方案'],
  stats: [['链路', '4 段'], ['根因', '时区混用'], ['耗时', '30 分']],
  quote: '时间 Bug 的排查路线只有一条：从显示层一路问到存储层，每一段问「你是哪个时区」',
  fb: ['数据负责人', '时区链路图画出来后，这类工单再没出现过'], risk: '修时区问题最忌只修一段：链路不统一，修完还会错',
  ctx: '报表数据整体错 8 小时：昨天 16 点之后的订单今天查不到。排查像一场审讯——前端、接口、服务、数据库挨个问「你用的哪个时区」：前端按本地时区查询、接口收的是字符串、服务按服务器时区解析、数据库存的是无时区时间戳。四段链路三种时区假设，错 8 小时只是早晚的事。',
  raw: `故障现象：
每日报表 08:00 后数据「少一段」——
实际是昨天的订单被算成了前天的
（整体偏移 8 小时）

排查中收集的事实：
· 前端：日期查询按浏览器本地时区
  生成「2026-09-17 00:00」
· 接口：收到的是无时区字符串
· 服务：按服务器时区（UTC）解析
· 数据库：时间列无时区标识，
  历史数据混着两种时区写入
· 新线索：上个月机房迁移，
  服务器时区从 UTC+8 变成了 UTC

疑问：
为什么迁移前一直「正常」？`,
  steps: [
    ['量化偏差', '确定差多少、往哪偏', '3 分钟'],
    ['画链路', '时间从产生到显示的四段', '8 分钟'],
    ['逐段审讯', '每段的时区假设', '10 分钟'],
    ['找拐点', '哪次变更打破平衡', '5 分钟'],
    ['定方案', '全链路统一策略', '3 分钟'],
    ['写规范', '存储传输显示三层规则', '6 分钟'],
  ],
  full: `【Bug 诊断报告 · 报表错 8 小时】

一、现象量化
报表整体偏移 +8 小时：
昨天 16:00 后的订单落入「前天」分组。
偏差方向与时区差吻合——先假设时区问题，
再逐段验证。

二、时区链路审讯（四段）
第 1 段 前端：按用户浏览器时区（UTC+8）
　生成查询条件「2026-09-17 00:00」
第 2 段 接口：字符串透传，无时区信息——
　「00:00」到底是谁家的 00:00？无人知晓
第 3 段 服务：服务器时区解析——
　迁移前是 UTC+8，恰好与前端一致，
　「错误互相抵消」一直显得正常；
　迁移后变 UTC，偏差 8 小时显形
第 4 段 数据库：时间列无时区标识，
　存量数据混着两种时区写入
根因：链路从未统一时区，
　过去靠「服务器恰好在 UTC+8」掩盖

三、为什么这是经典结构
「一直正常」的真相是两处错误互相抵消；
任何一段变化（迁机房、换语言、
新同事按规范写了 UTC）都会引爆。
时区 Bug 不是被制造出来的，
是被暴露出来的。

四、修复方案（全链路）
1 存储：统一 UTC 写入
　（存量数据迁移：按写入源时区校正）
2 传输：接口一律带时区的时间格式
　或 UTC 时间戳，禁止裸字符串
3 转换：服务层用标准库时区组件，
　禁止手动加减小时
4 显示：前端拿 UTC 时间 +
　用户时区，本地渲染

五、验证与防回归
· 边界用例：夏令时切换日、跨天查询、
　不同时区用户各验一遍
· 监控：时区相关字段的写入格式校验，
　非法格式即告警
· 规范一页纸：「存储传输显示」三层规则
　进团队文档

六、复盘
修复 2 人日（含存量迁移评估）。
教训：靠巧合维持的正确，不是正确。`,
  stats2: [['链路', '4 段审讯'], ['存量迁移', '已评估'], ['规范', '一页纸']],
  qa: [
    ['为什么时区 Bug 常在被「修复」后才出现？', '因为很多系统的时区正确性建立在巧合上：前端和服务器恰好同一时区、两处错误恰好抵消。这类系统不是没有 Bug，是 Bug 处于休眠状态。机房迁移、多地域部署、国际化——任何变化都是唤醒剂。排查时间问题时先问「以前为什么对」，答案往往就是病灶。'],
    ['「裸字符串传输时间」为什么是原罪？', '「2026-09-17 00:00」这个字符串不含任何时区信息，每经手一方就要猜一次它的时区——猜测就是错误的种子。带时区的格式（或时间戳）让信息在源头完备，后续环节无需猜测。时间链路的第一原则：信息不要在传递中丢失。'],
    ['存量数据混时区怎么办？', '先止血后还债：新数据立即按统一标准写入，存量数据做一次考古式迁移——根据写入时间段的服务器时区配置推断原始时区，批量校正。校正必须有抽样人工核验，因为「推断」永远有例外。这笔债躲不掉，越晚还利息越高。'],
  ],
  fbs: [
    ['数据负责人', '时区链路图画出来后，这类工单再没出现过'],
    ['运维同学', '机房迁移检查单加了时区一致性核对'],
    ['前端同学', '拿到 UTC 自己转本地，逻辑反而简单了'],
  ],
  risks: ['修时区问题最忌只修一段：链路不统一，修完还会错', '裸字符串传时间是原罪', '存量数据迁移必须抽样核验', '「以前正常」要追到巧合为止'],
}),
({
  t: '内存泄漏 · 曲线会说话', sub: '40 分钟定位', d: '诊断内存泄漏：增长曲线与快照对比',
  ly: 'timeline', pal: 'green', bg: 'grid',
  facts: ['输入：监控曲线', '输出：根因报告', '定位 40 分钟'],
  inp: '粘贴监控截图描述', out: ['根因：监听器未解绑累积', '曲线形态判读法', '快照对比定位'],
  stats: [['曲线', '阶梯增长'], ['根因', '未解绑'], ['耗时', '40 分']],
  quote: '泄漏排查先看曲线形态：斜率、台阶、回落，三个特征锁定三类病因',
  fb: ['SRE 负责人', '曲线形态学练熟了，一半的泄漏看监控就能猜中'], risk: '重启能「解决」的内存问题只是延期：曲线还在那里',
  ctx: '服务内存每天涨 200M，运维的应对是「每周重启」。这次决定追到底：内存曲线呈阶梯状增长，每次流量高峰上一个台阶且从不回落——这个形态直接指向「随连接/事件累积的对象」。堆快照对比锁定根因：事件监听器只注册不卸载，每次热更新累积一批。曲线从不撒谎，撒谎的是我们没看它。',
  raw: `故障现象：
· 服务内存 7 天从 1.2G 涨到 2.6G，
  接近限额即触发重启
· 每周例行重启「解决」
· 无 OOM 崩溃，纯缓慢增长

曲线特征（关键线索）：
· 阶梯状增长：流量高峰时上台阶
· 低谷期不回落（不是正常的弹性波动）
· 台阶高度与「热更新次数」相关

环境：
· 服务支持配置热更新
  （每次热更新重新订阅消息总线）
· 监控只有内存总量，无对象级指标`,
  steps: [
    ['读曲线', '形态特征判读病因方向', '5 分钟'],
    ['找关联', '台阶与事件的时序对齐', '8 分钟'],
    ['抓快照', '两个时间点的堆快照', '10 分钟'],
    ['比差异', '增量最大的对象链', '10 分钟'],
    ['验假设', '复现热更新的累积', '5 分钟'],
    ['出方案', '修复+监控升级', '7 分钟'],
  ],
  full: `【Bug 诊断报告 · 内存缓慢泄漏】

一、曲线形态判读（先于一切）
三种典型形态对应三类病因：
· 线性匀速涨 → 稳态泄漏
　（每个请求漏一点）
· 阶梯涨、不回落 → 累积型泄漏
　（某类对象只增不减）← 本案
· 锯齿但底部抬升 → 缓存无淘汰
本案特征：阶梯 + 不回落，
且台阶与热更新时间点吻合——
假设锁定「热更新累积」。

二、堆快照对比（定位动作）
操作：热更新前取快照 A，
热更新 5 次后取快照 B，
对比增量对象
发现：
· 「消息总线监听器」实例数
　从 12 → 72（恰好 5 次 × 12）
· 每个监听器闭包引用着
　整份配置对象
根因：热更新时重新订阅消息总线，
但旧订阅从未取消——
监听器 + 闭包持有的配置对象
永久驻留，每次热更新累积一批

三、验证
实验环境连续热更新 20 次，
监听器数线性增长至 252，
内存曲线完全复现生产形态。
假设成立。

四、修复方案
1 根治：热更新流程改为
　「先取消旧订阅 → 再注册新订阅」，
　订阅生命周期与配置版本绑定
2 兜底：订阅管理器加重复注册检测，
　同一回调重复订阅即告警
3 监控升级：
　· 对象级指标（监听器数、
　　大对象统计）进看板
　· 内存 7 日斜率告警
　　（斜率超阈值 = 泄漏预警，
　　不等涨到限额）

五、复盘
「每周重启」是把泄漏当慢性病保守治疗，
本次根因修复后 14 天内存平稳。
教训：曲线形态是最便宜的诊断工具，
堆快照是最精确的手术刀。`,
  stats2: [['监听器', '12→72'], ['修复后', '14 天平稳'], ['斜率告警', '已上线']],
  qa: [
    ['「阶梯不回落」为什么能直接指向累积型泄漏？', '健康服务的内存是锯齿形：分配后随 GC/释放回落，底部稳定。阶梯不回落意味着有对象「注册了就再也不释放」——它们的数量与某种事件次数成正比。再问一句「什么事件发生后台阶就涨」，答案（本案是热更新）几乎自己浮出来。曲线形态是把排查空间从「整个程序」压缩到「一类行为」的过滤器。'],
    ['为什么监听器泄漏偏爱「热更新/重连」场景？', '因为这类场景的本质是「旧实例退场、新实例登场」，而退场流程最容易被漏写：注册订阅是功能代码（有人写、有人测），取消订阅是善后代码（没人看见它的价值）。所有「重新初始化」的路径都值得审一遍：上一次初始化留下的东西，谁来收拾？'],
    ['「每周重启」为什么必须停止？', '重启掩盖了曲线的存在：泄漏速率、触发条件、增长规律全被周期性清零抹掉，等于销毁了所有诊断证据。更实际的风险是：某天流量上涨让泄漏加速，重启间隔内触顶就是 OOM 事故。慢性病的正确处置是确诊，不是反复退烧。'],
  ],
  fbs: [
    ['SRE 负责人', '曲线形态学练熟了，一半的泄漏看监控就能猜中'],
    ['运维同学', '重启排班取消了，终于不用周末待命'],
    ['开发同学', '订阅管理器的重复检测，上周真报了一次'],
  ],
  risks: ['重启能「解决」的内存问题只是延期：曲线还在那里', '「重新初始化」路径必审退场清理', '监控要加对象级指标与斜率告警', '快照对比要在可控复现下做'],
}),
({
  t: '死锁排查 · 顺序即命运', sub: '35 分钟定位', d: '诊断死锁：锁顺序分析与等待图',
  ly: 'kanban', pal: 'purple', bg: 'mesh',
  facts: ['输入：卡死现象', '输出：根因报告', '定位 35 分钟'],
  inp: '粘贴卡死现象', out: ['根因：两把锁顺序相反', '等待图还原', '全局锁序方案'],
  stats: [['线程', '2 组'], ['锁', '2 把'], ['耗时', '35 分']],
  quote: '死锁的数学本质只有一个环：A 等 B，B 等 A——找到环，就找到了病',
  fb: ['并发专家', '锁顺序表贴在工位后，死锁再没来过'], risk: '超时重试救不了死锁：锁不会自己松手',
  ctx: '服务每晚低峰期随机卡死 30 秒，重启才好。线程转储抓到两组线程互相等待：转账线程先锁账户 A 再锁账户 B，对账线程先锁 B 再锁 A——经典的锁顺序相反。死锁不是运气差，是数学必然：只要两个方向都可能出现，环迟早形成。修复方案不是加超时，是统一全局锁序。',
  raw: `故障现象：
· 每晚 02:00-04:00（对账时段）
  服务间歇卡死 20-40 秒
· 卡死时无报错，纯无响应
· 重启立即恢复

线程转储（卡死瞬间抓取）：
线程 T1（转账）：
  持有 lockA，等待 lockB
线程 T2（对账）：
  持有 lockB，等待 lockA
（多组线程同模式）

代码事实：
· 转账：按「出账账户→入账账户」加锁
· 对账：按账户表顺序（恰好可能相反）加锁
· 两处代码上线两年，
  对账任务一个月前才改为并发执行`,
  steps: [
    ['抓现场', '卡死瞬间的线程转储', '8 分钟'],
    ['画等待图', '谁持有什么等待什么', '8 分钟'],
    ['找环', '确认循环等待链', '5 分钟'],
    ['查路径', '两条加锁路径的代码', '8 分钟'],
    ['定方案', '全局锁序设计', '3 分钟'],
    ['防回归', '锁序规约与检查', '5 分钟'],
  ],
  full: `【Bug 诊断报告 · 转账服务死锁】

一、现场证据
卡死瞬间线程转储：
· T1 转账：持有 lockA → 等待 lockB
· T2 对账：持有 lockB → 等待 lockA
等待图：A → B → A，环已形成。
死锁四要素（互斥、持有并等待、
不剥夺、循环等待）全部满足。

二、根因：锁顺序相反
路径 1（转账）：按业务方向加锁
　先出账账户（A）后入账账户（B）
路径 2（对账）：按表顺序加锁
　恰好可能先 B 后 A
两条路径同时执行 + 交叉持锁 = 环
为什么现在才发作：
对账任务一个月前从串行改并发——
「并发化」让两条路径首次真正相遇。
死锁概率 = 路径相遇概率，
相遇为零时它是隐形的。

三、修复方案
核心：全局锁序——
所有路径按同一规则加锁
（账户 ID 升序），方向无关
· 转账改造：拿到两个账户后，
　按 ID 排序再加锁
　（业务语义不变，只改加锁顺序）
· 对账改造：统一走同一锁序工具类
· 禁止散落的手工加锁：
　封装「按序多锁」原子工具

四、为什么不选「超时重试」
超时只能让卡死变短，不能消灭环；
且持锁方可能在超时瞬间刚好推进，
引入更隐蔽的时序问题。
锁顺序是数学解，超时是运气管理。

五、防回归
· 锁序规约写进团队文档：
　任何多锁场景必须说明排序键
· 代码审查检查点：
　新增加锁代码必问「全局锁序是什么」
· 压测科目：转账与对账并发对冲场景

六、复盘
修复 1 人日，卡死绝迹。
教训：并发化改造必须重审锁路径，
「以前串行没事」不是并发安全的证据。`,
  stats2: [['环', 'A→B→A'], ['锁序', 'ID 升序'], ['卡死', '绝迹']],
  qa: [
    ['为什么死锁「两年没事，突然发作」？', '死锁需要两条相反的锁路径在极窄的时间窗内交叉持锁——串行时代它们永远不相遇，概率为零。对账并发化那一刻，相遇概率从 0 跳到某个小值，于是「每晚低峰期偶发」。死锁没有潜伏期，只有触发条件；条件不满足时它是隐形的，条件一满足它是必然的。'],
    ['「按账户 ID 排序加锁」为什么能根治？', '环的形成要求两个线程以相反顺序申请锁。全局统一排序后，所有线程都先锁 ID 小的、再锁大的——相反顺序在物理上不存在了，环不可能闭合。这是用「消除环的存在条件」代替「处理环的后果」，属于从几何层面解题。'],
    ['为什么「超时」是死锁的错误药方？', '超时假设「等一等锁就会松手」，但死锁中的锁永远不会松手：持有者在等对方，谁也不会先放。超时的结果要么是无意义的等待后报错，要么是更糟的时序混乱（超时瞬间对方恰好推进）。死锁需要的是让环无法形成，而不是让等待更有耐心。'],
  ],
  fbs: [
    ['并发专家', '锁顺序表贴在工位后，死锁再没来过'],
    ['开发同学', '对账并发化那次改造，漏审了锁路径'],
    ['值班同学', '凌晨告警消失，终于能睡整觉'],
  ],
  risks: ['超时重试救不了死锁：锁不会自己松手', '并发化改造必须重审锁路径', '多锁场景必须声明全局排序键', '串行无死锁不等于并发无死锁'],
}),
];
