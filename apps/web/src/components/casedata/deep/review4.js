/* 「代码审查」深度版案例（16-20） */

export const DEEP_REVIEW_4 = [
({
  t: '接口版本审查 · 破坏性变更', sub: '8 分钟审完', d: '审查接口变更：兼容性与版本策略',
  ly: 'mail', pal: 'cyan', bg: 'paper',
  facts: ['输入：接口变更', '输出：审查报告', '问题 5 处'],
  inp: '粘贴变更说明', out: ['高危：删字段破坏旧客户端', '高危：枚举值语义变更', '建议：只增不改+废弃流程'],
  stats: [['高危', '2 处'], ['兼容', '3 检查'], ['建议', '4 条']],
  quote: '接口审查之问：已经发出去的老版本客户端，还能活吗',
  fb: ['平台负责人', '兼容性检查进审查清单后，线上白屏事故归零'], risk: '字段「改名」在接口上等于「删了旧的加了新的」',
  ctx: '一次「小重构」：订单接口字段改名、状态枚举合并了两个值——服务端测试全绿，发版后老版本 App 订单页白屏。接口变更审查的核心视角是「不可控的存量客户端」：你无法让用户升级，只能让接口兼容。这份报告建立「只增不改、废弃走流程」的纪律。',
  raw: `审查对象：订单接口变更（App 依赖）
变更内容：
1 字段 total_amount 改名 amount
2 状态枚举：合并 PAID 与 CONFIRMED 为 DONE
3 分页参数 page_size 上限 100 → 20
4 时间字段从毫秒时间戳改为日期字符串
5 新增 discount 字段（唯一安全的变更）

影响面：
线上 3 个 App 版本，最老版本
18 个月前发布，仍有 12% 用户
无接口版本机制`,
  steps: [
    ['列变更', '逐项标注变更类型', '2 分钟'],
    ['判破坏', '对存量客户端的影响', '3 分钟'],
    ['查枚举', '值的增删改语义', '2 分钟'],
    ['评策略', '版本机制与废弃流程', '2 分钟'],
    ['写报告', '兼容方案与迁移', '3 分钟'],
  ],
  full: `【代码审查报告 · 订单接口变更】

一、高危问题（2 处，存量客户端事故级）

H1 字段改名（破坏性）
　现状：total_amount → amount
　影响：12% 老版本用户读不到金额字段，
　　订单页白屏
　原则：接口字段「改名」=
　　删除旧字段 + 新增字段，两个动作
　整改：
　　· 保留 total_amount 至少两个大版本，
　　　同时返回 amount（双写过渡）
　　· 老字段标记废弃（文档 + 响应头），
　　　监控其调用量，归零后再删

H2 枚举值合并（语义破坏）
　现状：PAID、CONFIRMED 合并为 DONE——
　　老客户端收到未知枚举值，
　　显示逻辑落入「未知」分支
　整改：
　　· 枚举只增不删不改义
　　· 必须合并时：新增值 + 映射层，
　　　对老版本仍返回旧值
　　· 客户端规范：未知枚举值
　　　要有优雅降级（而非崩溃）

二、中危问题（2 处）
M1 分页上限收紧（20）：
　调用方按 100 请求将被拒绝或截断——
　放宽属于「行为变更」，需公告 + 过渡
M2 时间格式变更（时间戳→字符串）：
　解析逻辑完全不同，
　典型破坏性变更；
　新增字段用新格式，老字段保留

三、接口纪律（4 条建议）
· 只增不改不删：新增可选字段安全，
　其余动作默认破坏性
· 破坏性变更必须走废弃流程：
　公告 → 双写过渡 → 调用量监控 →
　下线审批
· 引入接口版本机制（URL 或请求头），
　新老并行
· 契约测试：老版本客户端的请求样本
　作为回归用例，每次发版跑

四、结论
本次变更 4 项中 3 项破坏性，打回。
按「新增字段 + 废弃流程」重新设计。`,
  stats2: [['兼容检查', '3 项'], ['双写过渡', '2 版本'], ['白屏', '归零']],
  qa: [
    ['为什么「服务端测试全绿」挡不住接口事故？', '因为测试测的是新客户端，而接口真正的用户是「不可控的存量」：18 个月前的 App 版本、第三方集成、爬虫，都按老契约写死了逻辑。接口变更的影响面不在你的代码库里，在你的调用方那里——契约审查必须站在最老调用方的视角。'],
    ['字段改名为什么要「双写过渡」？', '直接改名对调用方是断崖：要么全升级要么全挂。双写（新旧字段同时返回）把断崖变成缓坡：新版本用新字段，老版本继续读旧字段，等旧字段的调用量监控归零，说明存量都迁完了，再安全下线——过渡期的成本换的是零事故。'],
    ['枚举为什么是「最容易被低估」的破坏点？', '开发者改枚举时想的是「语义优化」，客户端看到的是「收到一个不认识的字符串」。大多数客户端对未知枚举的处理是未定义行为：崩溃、白屏、显示「未知」。枚举的兼容性纪律就一条：值可以加，永远不可以删、不可以改含义。'],
  ],
  fbs: [
    ['平台负责人', '兼容性检查进审查清单后，线上白屏事故归零'],
    ['App 开发', '老版本用户的订单页保住了'],
    ['QA', '契约回归用例，抓住了两次破坏性变更'],
  ],
  risks: ['字段「改名」在接口上等于「删了旧的加了新的」', '枚举值只增不删不改义', '破坏性变更必走废弃流程', '契约测试覆盖最老客户端'],
}),
({
  t: '配置代码审查 · 硬编码密钥', sub: '7 分钟审完', d: '审查配置代码：密钥管理与环境隔离',
  ly: 'terminal', pal: 'red', bg: 'dark',
  facts: ['输入：配置代码', '输出：审查报告', '问题 6 处'],
  inp: '粘贴配置代码', out: ['高危：密钥硬编码进代码', '高危：密钥已进 Git 历史', '建议：密钥管理服务+轮换'],
  stats: [['高危', '2 处'], ['中危', '2 处'], ['建议', '4 条']],
  quote: '配置审查的第一刀砍向常量：凡是长得像密钥的字符串，都审一遍',
  fb: ['安全负责人', '进了 Git 历史的密钥必须视为已泄露：轮换，不是删除'], risk: '密钥「从代码里删掉」不等于安全：历史里还在',
  ctx: '一次例行审查，在配置文件里发现了云厂商 AK/SK 明文硬编码——而且三个月前就已提交，Git 历史里清清楚楚。密钥审查的残酷常识是：删掉不等于安全，历史里的每一份拷贝都要视为已泄露。这份报告走完整套应急流程：立即轮换、接入密钥管理服务、历史清洗、全库清查。',
  raw: `审查对象：服务配置层
问题代码片段：

config = {
    'ak': 'LTAI5tQeD8vNxxxxxxxx',
    'sk': 'Hk9f2mQzXw7cVbxxxxxxxx',
    'db': 'mysql://root:pass123@prod-db/app',
    'env': 'production',       # 写死
}

问题点：
1 云密钥硬编码且已入 Git 历史
   （3 个月前提交，无法「删除了事」）
2 数据库密码在连接串里明文
3 环境写死在代码：
   测试/生产同一份代码无法隔离
4 配置无校验：缺项运行时才炸
5 敏感配置与业务配置混在一起
6 无密钥轮换机制`,
  steps: [
    ['扫密钥', '疑似密钥模式全库搜', '3 分钟'],
    ['查历史', '进过版本库的一律泄露', '2 分钟'],
    ['看环境', '配置与环境的分离', '2 分钟'],
    ['评校验', '配置加载的健壮性', '2 分钟'],
    ['定应急', '轮换与清洗流程', '3 分钟'],
    ['写报告', '密钥治理方案', '3 分钟'],
  ],
  full: `【代码审查报告 · 配置与密钥专项】

一、高危问题（2 处，立即应急）

H1 云密钥硬编码且已入历史
　定性：进入过 Git 历史的密钥 =
　　已泄露（任何有仓库读权限的人、
　　任何 CI 日志、任何备份都可能有拷贝）
　应急流程（按小时执行）：
　　1. 立即在云控制台轮换密钥
　　　（先立新后废旧，防中断）
　　2. 排查该密钥近期调用日志，
　　　确认无异常使用
　　3. 清洗 Git 历史（重写历史工具），
　　　并通知所有协作者重新克隆
　　4. 全库扫描同类密钥（模式搜索）
　注意：「从代码里删掉」不是补救，
　轮换才是

H2 数据库凭证明文
　整改同上：连接串中不出现密码，
　凭据走密钥管理服务或环境变量注入

二、中危问题（2 处）
M1 环境写死：
　环境由部署注入（环境变量/
　配置中心），代码零环境感知；
　生产与测试配置物理隔离
M2 配置无校验：
　启动时校验必填项与格式，
　缺配置「启动失败」优于
　「运行中诡异报错」

三、建议（4 条）
· 接入密钥管理服务：
　动态下发 + 自动轮换 + 访问审计
· 敏感配置独立域：与业务配置分离，
　最小权限访问
· CI 加密钥扫描（熵检测 + 规则库），
　提交即拦截
· 密钥台账：每个密钥的责任人、
　用途、轮换周期（如 90 天）

四、结论
密钥治理 = 应急（轮换）+ 基建（密钥服务）
+ 守门（CI 扫描）。
本次整改含应急 0.5 人日 + 基建 2 人日。`,
  stats2: [['轮换', '当日完成'], ['扫描', '进 CI'], ['台账', '90 天周期']],
  qa: [
    ['为什么「进了历史」就要视为已泄露？', 'Git 的提交是不可变快照：密钥在历史里躺着，任何一次克隆、任何有权限的人、任何 CI 缓存都可能拿到它。你无法知道谁看过——安全上对「无法排除的暴露」只有一个处理原则：假设最坏，立即轮换。删除代码只是止血，轮换密钥才是治病。'],
    ['密钥轮换为什么要「先立新后废旧」？', '直接作废旧密钥，正在使用它的所有服务瞬间全部鉴权失败——应急不能制造新事故。先创建新密钥、服务切换、验证通过后作废旧的，轮换过程零中断。密钥管理的所有操作都要问一句：这一步失败的代价是什么。'],
    ['「启动时校验配置」防的是什么？', '配置缺失的两种命运：启动时报错（明确、可修复），或运行到某个分支时炸（诡异、难定位、可能在高峰期）。前者是体检，后者是猝死。把配置校验前置到启动，本质是把故障时间从「最贵的时候」挪到「最便宜的时候」。'],
  ],
  fbs: [
    ['安全负责人', '进了 Git 历史的密钥必须视为已泄露：轮换，不是删除'],
    ['运维同学', '密钥台账一建，轮换不再靠记性'],
    ['开发同学', 'CI 扫描上周拦了一个手滑提交的 token'],
  ],
  risks: ['密钥「从代码里删掉」不等于安全：历史里还在', '轮换先立新后废旧，防服务中断', '敏感配置独立域+最小权限', '配置缺项要在启动时炸，不是运行时'],
}),
({
  t: '测试代码审查 · 断言空心', sub: '7 分钟审完', d: '审查测试代码：断言质量与覆盖有效性',
  ly: 'code', pal: 'orange', bg: 'grid',
  facts: ['输入：测试代码', '输出：审查报告', '问题 6 处'],
  inp: '粘贴测试代码', out: ['高危：断言只查不抛错', '高危：测试间状态串扰', '建议：断言规范+隔离'],
  stats: [['问题用例', '11 个'], ['空心断言', '8 处'], ['建议', '4 条']],
  quote: '测试审查的试金石：把被测代码改坏，这个测试会红吗',
  fb: ['测试架构师', '变异测试跑一次，就知道覆盖率是不是虚胖'], risk: '只测成功路径的测试，是「看起来有保险」的保险',
  ctx: '一个模块测试覆盖率 92%，审查却发现：大量测试只调用不断言、断言写在注释里、测试之间共享可变状态互相污染。高覆盖率给了团队虚假的安全感。测试审查的终极问题是「改坏代码测试会红吗」——这份报告用变异思路验证，92% 的覆盖率里只有 40% 真正有效。',
  raw: `审查对象：订单模块单元测试（覆盖率 92%）
问题代码片段：

def test_create_order():
    result = create_order(valid_req)
    # 应该返回成功        ← 断言在注释里

def test_pay():
    pay(order)               # 只调用，无断言

def test_refund():
    assert refund(order) is not None
                             # 空心断言：
                             # 任何返回都通过

全局测试客户端共享（测试间状态串扰）
问题点：
1 无断言用例：跑过即通过，什么都没验证
2 注释断言：期望写在注释里
3 空心断言：not None / True 这类恒真检查
4 共享状态：用例执行顺序影响结果
5 只测成功路径：异常分支零覆盖
6 测试依赖时间/随机数：偶发红`,
  steps: [
    ['数断言', '每个用例的有效断言', '3 分钟'],
    ['判空心', '恒真断言逐个标出', '2 分钟'],
    ['查隔离', '共享状态与执行顺序', '2 分钟'],
    ['看分支', '异常路径覆盖', '2 分钟'],
    ['做变异', '改坏代码测试会红吗', '3 分钟'],
    ['写报告', '断言规范与治理', '3 分钟'],
  ],
  full: `【代码审查报告 · 测试质量专项】

一、高危问题（测试体系失效级）

H1 空心断言与无断言（8+11 处）
　类型盘点：
　　· 无断言：只调用函数，跑过即通过
　　· 注释断言：期望写在注释里
　　· 恒真断言：not None、is True、
　　　无消息的 try-except
　危害：覆盖率 92% 是虚胖——
　　这些测试对任何实现都亮绿灯，
　　改坏代码照样通过
　试金石（变异思想）：
　　故意把业务逻辑改错，
　　测试必须红；不红的测试 = 无效
　整改：每个用例至少一条
　　「结果断言」（值、状态、副作用），
　　断言写具体期望值而非宽泛真值

H2 测试间状态串扰
　现状：全局共享客户端与数据，
　　用例顺序不同结果不同（偶发红根源）
　整改：
　　· 每个用例独立构造数据，
　　　不依赖他人副作用
　　· 随机顺序跑一遍测试，
　　　暴露隐藏依赖
　　· 共享 fixture 改为函数级

二、中危问题（2 处）
M1 只测成功路径：
　每个功能至少补三条失败用例
　（参数非法、依赖失败、边界值）
M2 依赖时间/随机数：
　注入时钟与随机源，测试可复现

三、建议（4 条）
· 断言规范进团队文档：
　禁止裸调用、禁止恒真断言
· 引入变异测试工具（定期抽样跑），
　用「杀死率」替代「覆盖率」考核
· 测试代码同等审查：
　测试也是生产代码
· 偶发红零容忍：
　红过的测试要么修好要么删除

四、结论
覆盖率是入场券，断言质量才是保险单。
整改 3 人日，先补高危路径断言，
再上变异测试验收。`,
  stats2: [['有效覆盖', '40%'], ['变异杀死率', '新指标'], ['偶发红', '归零']],
  qa: [
    ['为什么「覆盖率 92%」可能是虚胖？', '覆盖率只回答「这行代码被执行过吗」，不回答「执行的结果被验证过吗」。一个没有任何断言的测试可以刷满覆盖率，却对代码的正确性零贡献。覆盖率衡量的是测试的「足迹」，断言衡量的才是「目光」——没有目光的足迹是旅游，不是审查。'],
    ['变异测试的思想为什么是终极检验？', '它的逻辑近乎暴力：自动把代码改坏（加减号、改条件、删语句），看测试能不能抓住。改坏了测试还亮绿灯，说明测试从未真正验证过什么。这是唯一能客观回答「测试有效性」的方法——人会自我感觉良好，变异工具不会。'],
    ['测试为什么要「随机顺序跑」？', '隐藏依赖是偶发红的元凶：用例 A 恰好先跑留下了数据，用例 B 依赖它才通过；某天顺序变了，B 就红了。随机顺序执行把这类依赖强制曝光——测试套件的健壮性标准是「任何顺序都绿」，而不是「碰巧这个顺序绿」。'],
  ],
  fbs: [
    ['测试架构师', '变异测试跑一次，就知道覆盖率是不是虚胖'],
    ['开发同学', '补完失败路径用例，真抓到两个隐藏 bug'],
    ['团队', '随机顺序跑测试后，偶发红彻底消失'],
  ],
  risks: ['只测成功路径的测试，是「看起来有保险」的保险', '覆盖率之外必须看断言质量', '测试隔离：不共享可变状态', '偶发红零容忍'],
}),
({
  t: 'Dockerfile 审查 · 镜像安全', sub: '8 分钟审完', d: '审查 Dockerfile：镜像安全与构建质量',
  ly: 'terminal', pal: 'ink', bg: 'grid',
  facts: ['输入：Dockerfile', '输出：审查报告', '问题 6 处'],
  inp: '粘贴 Dockerfile', out: ['高危：root 运行应用', '高危：密钥进构建层', '建议：多阶段构建+最小基础镜像'],
  stats: [['高危', '2 处'], ['镜像', '1.2G→180M'], ['建议', '4 条']],
  quote: 'Dockerfile 审查三连：以谁的身份跑，带了什么不该带的，层里藏了什么',
  fb: ['SRE 负责人', '镜像瘦下来 1.2G，部署速度翻了三倍'], risk: '构建层的秘密永远留在镜像里：docker history 一查一个准',
  ctx: '一个生产镜像：以 root 跑应用、把 .env 文件 COPY 进镜像、单层缓存全部失效每次全量构建、基础镜像带着完整操作系统（1.2G）。Dockerfile 审查的三个视角——权限最小化、秘密零残留、层缓存效率——一次审透，镜像从 1.2G 瘦到 180M。',
  raw: `审查对象：生产服务 Dockerfile
问题代码片段：

FROM ubuntu:22.04
COPY . /app                  # 全量拷贝
RUN apt-get update &&
    apt-get install -y ...
RUN pip install -r
    /app/requirements.txt
ENV API_KEY=sk-live-xxxx     # 密钥进镜像
CMD ["python", "/app/main.py"]
                             # root 运行

问题点：
1 root 运行：容器逃逸即主机 root
2 密钥写进 ENV：
  镜像层永久留存，任何人拉镜像即得
3 COPY . 无排除：.env、.git
  全部进镜像
4 单层结构：代码改动导致
  依赖层缓存失效，每次全量装依赖
5 基础镜像过大（完整 Ubuntu）
6 无健康检查、无版本标签`,
  steps: [
    ['查身份', '运行用户与权限', '2 分钟'],
    ['查秘密', '层里的密钥残留', '2 分钟'],
    ['查范围', 'COPY 进镜像的内容', '2 分钟'],
    ['看分层', '缓存效率与顺序', '2 分钟'],
    ['评基础', '基础镜像最小化', '2 分钟'],
    ['写报告', '安全构建方案', '3 分钟'],
  ],
  full: `【代码审查报告 · Dockerfile】

一、高危问题（2 处）

H1 应用以 root 运行
　危害：容器内漏洞 + 逃逸 =
　　主机 root 权限，纵深防御归零
　整改：
　　· 构建期创建专用用户，
　　　USER 指令切换
　　· 应用目录属主与权限最小化
　　· 配合运行时参数：
　　　只读文件系统 + 无特权

H2 密钥进入镜像层
　现状：ENV API_KEY=...——
　　镜像层不可变，密钥永久固化；
　　任何能拉镜像的人
　　（docker history 一看便知）
　整改：
　　· 密钥不进镜像：运行时注入
　　　（环境变量由编排层下发 /
　　　　挂载密钥文件 / 密钥服务）
　　· 构建期需要的秘密用
　　　构建秘密机制（不留层）
　　· 已泄露的密钥立即轮换
　　　（同代码密钥处理流程）

二、中危问题（2 处）
M1 COPY . 无排除：
　.dockerignore 必备：排除 .env、
　.git、测试数据、构建产物
M2 分层反模式：
　依赖声明先拷先装（独立层，
　命中缓存），代码后拷——
　代码改动不再触发重装依赖；
　采用多阶段构建：
　构建层带工具链，运行层只留产物

三、建议（4 条）
· 基础镜像最小化：
　运行时用精简版（如 slim/distroless）
· 固定版本标签（禁 latest），
　依赖锁文件随镜像构建
· 加健康检查与优雅停机信号处理
· 镜像扫描进 CI：
　已知漏洞（CVE）阻断发布

四、整改效果
镜像 1.2G → 180M；
构建时间（代码改动场景）
从 8 分钟 → 40 秒；
密钥清零，非 root 运行。`,
  stats2: [['镜像', '1.2G→180M'], ['构建', '8m→40s'], ['CVE 扫描', '进 CI']],
  qa: [
    ['为什么「密钥进构建层」等同于公开发布？', '镜像的每一层都是只读文件系统，永久保存在镜像里：docker history 能看到所有 ENV，导出镜像文件系统能翻出所有 COPY 进去的文件。镜像是分发物，会到达注册表、CI、每个部署节点——把密钥放进去，等于给每个经手人发了一份。'],
    ['「依赖先装、代码后拷」的缓存逻辑是什么？', 'Docker 层缓存按内容失效：某层变了，它之后的所有层重建。依赖安装是最慢的一层，把变化频率低的（依赖声明）放前面、变化频繁的（源代码）放后面，代码改动只重建最后几层——分层顺序本质是按变化频率排列构建流水线。'],
    ['多阶段构建解决了什么？', '构建需要工具链（编译器、构建器），运行只需要产物。单阶段镜像把工具链也打包进生产镜像——体积大、攻击面大。多阶段构建让「构建环境」与「运行环境」分离：运行层只拷贝产物，干净、小、安全。'],
  ],
  fbs: [
    ['SRE 负责人', '镜像瘦下来 1.2G，部署速度翻了三倍'],
    ['安全同事', 'CVE 扫描进流水线，上周拦了一个高危'],
    ['开发同学', '缓存分层后，本地构建快得像换了机器'],
  ],
  risks: ['构建层的秘密永远留在镜像里：docker history 一查一个准', '应用绝不以 root 运行', '基础镜像与依赖版本固定', '镜像漏洞扫描阻断发布'],
}),
({
  t: 'CI 脚本审查 · 缓存污染', sub: '7 分钟审完', d: '审查 CI 流水线：缓存安全与制品可信',
  ly: 'sheet', pal: 'green', bg: 'dots',
  facts: ['输入：CI 配置', '输出：审查报告', '问题 6 处'],
  inp: '粘贴 CI 配置', out: ['高危：缓存无完整性校验', '高危：密钥暴露给任意分支', '建议：缓存锁定+权限分级'],
  stats: [['高危', '2 处'], ['中危', '2 处'], ['建议', '4 条']],
  quote: 'CI 审查的威胁模型：跑在流水线里的代码，值得信任吗',
  fb: ['DevOps 负责人', '缓存投毒是供应链攻击的新宠，这份审查把门焊死了'], risk: 'fork 分支的流水线若能读秘密，等于秘密公开下载',
  ctx: '一条「看起来没问题」的 CI 流水线：依赖缓存不做校验直接复用（可被投毒）、生产密钥对所有分支可见（fork 提个 PR 就能读）、构建产物无签名直接部署。CI 是现代软件的供应链咽喉——这份审查按供应链威胁模型逐项加固。',
  raw: `审查对象：主仓库 CI 流水线配置
问题代码片段：

- restore_cache: deps-          # 前缀匹配，
                                  # 无哈希绑定
- run: npm install
- run: npm run build
- deploy:
    env: PROD_KEY               # 所有分支可见

问题点：
1 缓存前缀匹配：攻击者构造同名
  缓存键即可投毒依赖
2 缓存恢复无完整性校验
3 生产密钥对全部流水线可见：
  fork 的 PR 触发流水线即可读秘密
4 构建产物无签名/校验，
  部署不验证来源
5 依赖安装无锁文件校验
6 流水线无审计日志`,
  steps: [
    ['画链路', '代码到部署的每步', '2 分钟'],
    ['查缓存', '键设计与完整性', '2 分钟'],
    ['查秘密', '可见范围与触发条件', '3 分钟'],
    ['查制品', '签名与部署验证', '2 分钟'],
    ['评权限', '流水线执行权限分级', '2 分钟'],
    ['写报告', '供应链加固', '3 分钟'],
  ],
  full: `【代码审查报告 · CI 流水线】

一、高危问题（2 处，供应链级）

H1 缓存投毒面
　现状：缓存键只用前缀匹配，
　　恢复时不校验内容——
　　任何能写缓存的作业都能
　　把恶意依赖塞给后续所有构建
　整改：
　　· 缓存键绑定锁文件哈希
　　　（内容变则缓存变）
　　· 恢复后校验：依赖树与
　　　锁文件一致性检查
　　· 缓存写入权限只给可信作业
　　　（如仅主干构建可写）

H2 秘密暴露面过大
　现状：生产密钥对所有分支的
　　流水线可见——外部 fork 提交
　　一个 PR（含恶意脚本）触发构建，
　　即可读取秘密
　整改：
　　· 秘密分级：生产密钥只对
　　　主干 + 受保护分支可见
　　· fork 的 PR 流水线运行在
　　　无秘密环境
　　· 秘密访问进审计日志，
　　　异常读取告警

二、中危问题（2 处）
M1 制品无签名：
　构建产物附带哈希与签名，
　部署前验证（防制品被调包）
M2 依赖无锁定校验：
　安装严格按锁文件，
　禁止「顺手升级」，
　依赖变更走独立 PR

三、建议（4 条）
· 流水线权限分级：
　构建 / 测试 / 部署逐级收窄
· 构建环境最小化：
　只装必需工具，禁网段按需开放
· 审计闭环：每次流水线的
　代码版本、依赖版本、
　制品哈希全留痕
· 定期演练：模拟缓存投毒与
　秘密读取，验证告警

四、结论
CI 是供应链的咽喉：
缓存可信、秘密最小、制品可验。
整改 2 人日 + 一次红队演练。`,
  stats2: [['缓存键', '绑哈希'], ['秘密', '分级可见'], ['制品', '签名验证']],
  qa: [
    ['缓存投毒是怎么发生的？', '依赖缓存的初衷是加速：把装好的依赖存起来复用。但「复用」意味着信任前人的产物——如果攻击者能写入同键的缓存（比如通过一个不受限的构建作业），他塞进去的恶意依赖就会被后续所有构建吃下。供应链攻击的精髓就是「让可信环节替你作恶」。'],
    ['为什么 fork 的 PR 绝不能见生产秘密？', 'fork 是公开的：任何人都能提 PR，PR 里的代码在流水线里执行。如果流水线环境里有生产密钥，攻击者只需要提交一行打印环境变量的代码——构建日志里就是密钥。这不是漏洞，是「把钥匙挂在公共门把手上」的设计错误。'],
    ['制品签名验证防的是什么？', '防「调包」：构建与部署之间如果存在可被篡改的间隙（共享存储、中间传输），部署的制品可能已不是当初构建的产物。签名让部署环节能验证「这正是那次构建的产物，一个字节没变」——制品的完整性必须有密码学背书，而不是信任路径。'],
  ],
  fbs: [
    ['DevOps 负责人', '缓存投毒是供应链攻击的新宠，这份审查把门焊死了'],
    ['安全同事', '秘密分级后，fork PR 环境干干净净'],
    ['发布负责人', '制品签名验证上线，部署多了一道铁闸'],
  ],
  risks: ['fork 分支的流水线若能读秘密，等于秘密公开下载', '缓存键必须绑定锁文件哈希', '部署前必须验证制品完整性', '秘密访问全审计'],
}),
];
