/* 「代码审查」深度版案例（6-10） */

export const DEEP_REVIEW_2 = [
({
  t: 'Shell 部署脚本审查 · 静默失败', sub: '7 分钟审完', d: '审查部署脚本：错误处理与原子性',
  ly: 'terminal', pal: 'orange', bg: 'dark',
  facts: ['输入：部署脚本', '输出：审查报告', '问题 6 处'],
  inp: '粘贴部署脚本', out: ['高危：失败继续执行', '高危：部署一半无回滚', '建议：set -euo pipefail+原子切换'],
  stats: [['高危', '2 处'], ['中危', '2 处'], ['建议', '3 条']],
  quote: 'Shell 脚本审查第一问：上一步失败了，脚本知道吗',
  fb: ['SRE 负责人', '静默失败是部署事故的温床，这单审查值一个季度'], risk: 'rm -rf 带变量的路径，变量为空就是灾难',
  ctx: '一个部署脚本，下载失败继续解压、解压失败继续重启服务——每一步都在失败上叠加，最终「部署成功」但服务是旧版本。Shell 脚本审查的核心就是打破这种「惯性前进」：set -euo pipefail 打底、关键步骤显式校验、发布做成原子切换。',
  raw: `审查对象：应用部署脚本（Bash）
问题代码片段：

cd /opt/app
wget http://pkg/releases/latest.tar.gz
tar xzf latest.tar.gz
rm -rf /opt/app/current
mv release /opt/app/current
systemctl restart app
echo "deploy success"

问题点：
1 无 set -e：wget 失败（网络抖动）后
  继续执行，解压的是旧包
2 rm -rf 在 mv 之前：删除与替换之间
  服务无代码可用（部署中断窗口）
3 无版本校验：下载完整性不验证
4 latest 语义：不可回滚到指定版本
5 重启失败无感知：脚本照样打印成功
6 无回滚动作：坏了只能重新部署`,
  steps: [
    ['走流程', '逐步模拟失败场景', '3 分钟'],
    ['查开关', 'set -euo pipefail', '1 分钟'],
    ['看原子', '切换窗口与回滚', '3 分钟'],
    ['验校验', '下载完整性检查', '2 分钟'],
    ['评可观测', '成功失败的判定', '2 分钟'],
    ['写报告', '分级与整改版本', '3 分钟'],
  ],
  full: `【代码审查报告 · 部署脚本】

一、高危问题（2 处）

H1 静默失败链（脚本惯性前进）
　现状：任何一步失败，下一步照常执行；
　　最终 echo "deploy success" 与事实无关
　整改：
　　· 脚本头三件套：
　　　set -euo pipefail
　　　（出错即停 / 未定义变量报错 /
　　　　管道中任何一段失败即失败）
　　· 关键步骤显式校验：
　　　wget 后检查文件存在与大小
　　　解压后校验版本号文件
　　· trap 捕获错误：失败时打印
　　　「第几行失败」并退出非零码

H2 删除先于替换（服务中断窗口）
　现状：rm -rf current 后才 mv 新版本，
　　两步之间服务裸奔；mv 失败则
　　彻底无代码可跑
　整改：符号链接原子切换——
　　· 版本目录带时间戳：
　　　releases/20260918-1430/
　　· ln -sfn 新版本 current.tmp，
　　　mv -T 原子替换软链
　　· 切换是原子的：没有「无代码」窗口

二、中危问题（2 处）
M1 latest 不可回滚：
　改为指定版本号部署，
　回滚 = 重新指向上一版本目录
M2 重启后不验证：
　重启后轮询健康检查接口 30 秒，
　不健康自动回滚软链

三、建议（3 条）
· 下载加 sha256 校验（发布流程产出校验和）
· rm -rf 安全纪律：路径带变量时
　先判空（防御变量为空的灾难）
· 部署日志落文件 + 失败通知值班群

四、结论
部署脚本的「成功输出」必须来自验证，
而不是 echo。整改 1 人日，
附演练：故意传坏包，脚本必须失败报警。`,
  stats2: [['原子切换', '软链方案'], ['健康检查', '30 秒轮询'], ['演练', '坏包注入']],
  qa: [
    ['为什么「echo deploy success」是审查红旗？', '因为它暴露了脚本的成功判定逻辑：成功靠打印，而不是靠验证。健壮的部署脚本，最后一行应该是「健康检查通过」的断言。审查时看到无条件打印成功，基本可以断定前面缺少错误处理——输出语是脚本质量的自供状。'],
    ['符号链接原子切换解决了什么？', '「删除旧版 → 放上新版」的两步操作之间，存在一个没有代码的时间窗，任何故障都会把它拉长成事故窗口。软链方案把「指向新版本」压缩成一次 mv -T（原子操作），要么指向旧版要么指向新版，永远有代码可跑——回滚也只是指回去。'],
    ['rm -rf 为什么要对变量路径特别警惕？', '变量为空时，路径退化：deploy_dir 为空，删除目标变成系统根路径附近的通配。这类事故的真实案例不胜枚举，代价是整台机器。判空检查不是啰嗦，是运维脚本的疫苗——一行判断换一次灾难豁免。'],
  ],
  fbs: [
    ['SRE 负责人', '静默失败是部署事故的温床，这单审查值一个季度'],
    ['运维同学', '原子切换上线后，部署窗口从分钟到秒'],
    ['开发同学', '坏包演练真失败了，脚本第一次诚实地报错'],
  ],
  risks: ['rm -rf 带变量的路径，变量为空就是灾难', '部署成功必须来自验证而非打印', '回滚方案与部署方案同时交付', '关键步骤显式校验，别信命令默认'],
}),
({
  t: 'TypeScript 审查 · any 泛滥', sub: '7 分钟审完', d: '审查 TS 代码：类型逃逸与类型质量',
  ly: 'code', pal: 'cyan', bg: 'grid',
  facts: ['输入：TS 代码', '输出：审查报告', '问题 6 处'],
  inp: '粘贴 TS 代码', out: ['高危：any 穿透核心链路', '中危：类型断言绕过检查', '建议：类型收口+泛型收敛'],
  stats: [['any', '14 处'], ['断言', '6 处'], ['建议', '4 条']],
  quote: 'TS 审查看的不是有没有类型：是类型在边界上站没站住',
  fb: ['前端架构师', 'any 清完那天，重构的胆子都大了三倍'], risk: 'as 断言是审查重点：它在告诉编译器「闭嘴」',
  ctx: '一个 TypeScript 项目，核心链路上 14 处 any、6 处 as 断言——类型系统名存实亡，等于花着 TS 的成本写着 JS。审查不是搞「any 狩猎」的数字游戏，而是分清要害：外部边界（接口响应、第三方）必须类型收口，内部 any 按风险分级清理。',
  raw: `审查对象：订单模块（TypeScript）
问题代码片段：

const res: any = await api.get(url)
const order = res.data as any
const total = (order as any).items
    .map((x: any) => x.price * x.count)

function fmt(v: any): any { return v }

问题点：
1 接口响应直接 any：后端字段变更
  编译期零感知（类型系统在此失效）
2 as any 断言链：一层断言污染整条链路，
  后续代码全部失去类型保护
3 map 参数 any：price/count 可能不存在，
  运行时才爆
4 fmt 函数 any 进 any 出：
  工具函数成了类型黑洞
5 无共享类型定义：前后端字段各写各的
6 枚举用字符串字面量散落各处，
  拼写错误编译不报`,
  steps: [
    ['画边界', '类型从哪进入系统', '2 分钟'],
    ['数 any', '按链路风险分级', '2 分钟'],
    ['查断言', '每处 as 的必要性', '2 分钟'],
    ['看共享', '前后端类型来源', '2 分钟'],
    ['评工具', '泛型能替代 any 吗', '2 分钟'],
    ['写报告', '收口方案与规则', '3 分钟'],
  ],
  full: `【代码审查报告 · 订单模块（TS）】

一、高危问题（类型边界失守）

H1 接口响应无类型收口
　现状：api.get 返回 any，字段拼写错误、
　　后端字段变更——编译期全部静默
　整改：类型收口三件套——
　　· 定义 OrderResponse 接口（与后端
　　　契约对齐，或从 OpenAPI 生成）
　　· 边界处运行时校验（如 zod）：
　　　外部数据「先验后用」
　　· api.get 泛型化：
　　　api.get<OrderResponse>(url)

H2 as any 断言链污染
　现状：一处 as any，整条调用链
　　退回无保护状态
　整改：
　　· 断言只能「收窄」不能「逃逸」：
　　　允许 res as OrderResponse（有依据），
　　　禁止 as any / as unknown as X
　　· lint 规则：no-explicit-any
　　　+ no-unsafe-assertion

二、中危问题（2 处）
M1 工具函数类型黑洞：
　fmt(v: any): any 改为泛型
　（保留类型信息进出）
M2 状态字符串散落：
　订单状态收敛为联合类型
　「待支付 | 已支付 | 已关闭」，
　拼写错误编译即报

三、建议（4 条）
· 建立共享类型包：前后端同源，
　后端变更自动同步
· any 分级清理：核心交易链路先清，
　纯展示层随后
· 第三方库无类型：补最小 declare，
　别用 any 绕过
· 新代码 any 门禁：
　CI 统计 any 数量只降不升

四、结论
TS 的价值 = 类型在边界的覆盖率。
本模块先收口接口层（1 人日），
再逐步清理内部（随迭代）。`,
  stats2: [['any', '14→0 计划'], ['边界校验', 'zod'], ['门禁', '只降不升']],
  qa: [
    ['为什么「外部边界」是类型收口的第一优先？', '类型系统的敌人是「不受控的数据」，而外部数据（接口、用户输入、第三方）是唯一真正不受控的来源。内部数据总归是自己写的，边界数据才是随时会变的。把类型防线修在国境线上，境内自然安定——收口一处边界，保护整条内部链路。'],
    ['as 断言为什么被称作「让编译器闭嘴」？', '类型断言的语义是「我比编译器更懂」：编译器明明看出类型不匹配，你用 as 强行盖章。它偶尔必要（解析 JSON 后的收窄），但多数场景是懒惰的遮羞布。审查时每处 as 都值得问一句：如果编译器是对的，这行代码会怎么死？'],
    ['「any 数量只降不升」门禁的意义？', '类型债务和利息一样复利：存量 any 可以慢慢还，但新增 any 是边还边借。CI 门禁把「不再变差」固化成制度——不要求一天还清，只保证方向单调向好。这是所有技术债治理的通用结构：先止血，再康复。'],
  ],
  fbs: [
    ['前端架构师', 'any 清完那天，重构的胆子都大了三倍'],
    ['后端同学', '共享类型包上线，字段变更自动同步'],
    ['新同事', '订单状态联合类型，拼错直接红线，爽'],
  ],
  risks: ['as 断言是审查重点：它在告诉编译器「闭嘴」', '外部数据必须校验后才能信任', 'any 门禁只降不升，防边还边借', '泛型优先于 any，保住类型信息'],
}),
({
  t: 'Node 中间件审查 · 错误穿透', sub: '8 分钟审完', d: '审查 Express 中间件：错误处理链',
  ly: 'terminal', pal: 'ink', bg: 'grid',
  facts: ['输入：中间件代码', '输出：审查报告', '问题 6 处'],
  inp: '粘贴中间件代码', out: ['高危：异步错误无人接', '高危：错误堆栈泄露到响应', '建议：统一错误出口+分类处理'],
  stats: [['高危', '2 处'], ['中危', '2 处'], ['建议', '3 条']],
  quote: '中间件审查沿错误走一遍：每个抛出的错，都有人接吗',
  fb: ['Node 负责人', '错误链路理顺后，500 错误从随机变成可控'], risk: 'next(err) 调用遗漏，是 Express 异步错误失踪的头号原因',
  ctx: '一个 Express 服务，异步路由里抛出的错误没人接——进程不崩、响应挂起、日志无痕；全局错误处理器又把堆栈原样吐给客户端。中间件审查的方法论是「沿错误路径走查」：每个可能抛错的地方，追问谁来捕获、怎么响应、记什么日志。',
  raw: `审查对象：API 网关中间件层（Express）
问题代码片段：

app.get('/api/order/:id', async (req, res) => {
    const order = await getOrder(req.params.id)
    res.json(order)
})
// getOrder 抛错时：无 try/catch，
// 无 next(err)，Express 4 不接异步错误 → 挂起

app.use((err, req, res, next) => {
    res.status(500).json({ error: err.stack })
})

问题点：
1 异步路由错误无人接（Express 4 特性）
2 全局错误处理器泄露堆栈到客户端
3 错误无分类：业务错误与系统错误
  混为一谈，全返回 500
4 无错误日志中间件：出错无痕
5 第三方中间件抛错未覆盖
6 无请求兜底：404 与未知路由混在一起`,
  steps: [
    ['走错误路', '每个抛错点的去向', '3 分钟'],
    ['查异步', 'async 错误的接法', '3 分钟'],
    ['看分类', '业务错与系统错', '2 分钟'],
    ['验响应', '客户端能看到什么', '2 分钟'],
    ['评日志', '错误的可追溯性', '2 分钟'],
    ['写报告', '错误体系设计', '3 分钟'],
  ],
  full: `【代码审查报告 · API 网关中间件】

一、高危问题（2 处）

H1 异步错误失踪（进程级隐患）
　现状：async 路由内抛错，
　　Express 4 不会传给错误中间件——
　　请求挂起、客户端超时、日志无痕
　整改（按版本二选一）：
　　· Express 5：原生支持异步错误传递
　　· Express 4：asyncHandler 包装
　　　（自动 catch 并 next(err)），
　　　或统一改造路由注册器
　验证：故意抛错的集成测试

H2 堆栈泄露到客户端
　现状：全局处理器把 err.stack 原样返回
　危害：文件路径、框架版本、内部结构
　　全部暴露给攻击者
　整改：
　　· 生产环境只返回错误码 + 消息
　　· 堆栈只进日志（带 trace_id）
　　· 开发环境可保留详细堆栈

二、中危问题（2 处）
M1 错误无分类：
　建立错误基类——
　　· BusinessError（带 HTTP 状态与
　　　错误码，如库存不足 422）
　　· 其余视为系统错误（500 + 告警）
　错误中间件按类型分流响应
M2 404 与异常混：
　未知路由显式注册 404 处理器，
　与错误中间件分开

三、建议（3 条）
· 错误日志中间件置于响应之前：
　先记后答，保证「必有日志」
· 统一错误码表，
　客户端按码做差异化提示
· 进程级兜底：未捕获异常记录后
　优雅重启（防僵尸进程）

四、结论
错误处理不是补丁，是体系：
抛出有包装 → 传递不断链 →
分类定响应 → 日志带上下文。
整改 2 人日，附错误注入测试清单。`,
  stats2: [['异步覆盖', '100%'], ['堆栈', '仅入日志'], ['错误码', '统一表']],
  qa: [
    ['Express 4 为什么不接异步错误？', 'Express 4 的错误传递依赖同步调用栈：中间件同步抛错能捕获，而 async 函数的错误发生在 Promise 里，调用栈已经返回了。这是框架的历史包袱，不是 bug——审查 Node 项目时，「异步错误有没有人接」永远是第一个问题，因为它的默认答案是「没有」。'],
    ['为什么堆栈泄露是「高危」而不是「建议」？', '堆栈是系统的解剖图：文件结构暴露代码组织，路径暴露部署环境，依赖版本暴露已知漏洞面。攻击者拿到堆栈等于拿到侦察报告。安全问题的分级看「资敌程度」——把内部结构送给所有人，当然值一个高危。'],
    ['「先记后答」的顺序为什么重要？', '错误中间件如果先响应后记日志，日志环节一旦出错（磁盘满、序列化失败），这次错误就彻底失踪了。先落日志再响应，保证最坏情况下也有案可查——错误处理系统的第一原则是：它自己不能静默失败。'],
  ],
  fbs: [
    ['Node 负责人', '错误链路理顺后，500 错误从随机变成可控'],
    ['前端同学', '统一错误码后，提示文案终于不乱了'],
    ['安全同事', '堆栈不外泄这条，安全扫描直接过'],
  ],
  risks: ['next(err) 调用遗漏，是 Express 异步错误失踪的头号原因', '堆栈与内部信息只进日志，不出响应', '业务错与系统错必须分类', '错误中间件自己不能静默失败'],
}),
({
  t: 'ORM 查询审查 · N+1 陷阱', sub: '8 分钟审完', d: '审查 ORM 代码：查询模式与性能陷阱',
  ly: 'sheet', pal: 'purple', bg: 'dots',
  facts: ['输入：ORM 代码', '输出：审查报告', '问题 5 处'],
  inp: '粘贴 ORM 代码', out: ['高危：列表页 N+1 查询', '中危：全字段加载大文本', '建议：预加载+字段投影'],
  stats: [['查询', '301 次→2 次'], ['问题', '5 处'], ['建议', '4 条']],
  quote: 'ORM 审查的听诊器是查询日志：一页列表发多少条 SQL，数数就知道',
  fb: ['后端负责人', 'N+1 修掉后，列表接口从 1.8 秒到 90 毫秒'], risk: 'ORM 的「方便」是糖，查询日志才是体检报告',
  ctx: '一个订单列表接口，页面打开 1.8 秒。打开查询日志：一页 30 条数据发了 301 条 SQL——列表 1 条，每条订单的详情各 1 条，每条的订单项又各 1 条。这就是教科书级的 N+1。审查给出预加载方案，SQL 降到 2 条，接口到 90 毫秒。',
  raw: `审查对象：订单列表接口（SQLAlchemy 风格）
问题代码片段：

orders = db.query(Order).limit(30).all()
result = []
for o in orders:
    result.append({
        'id': o.id,
        'user': o.user.name,        # 每行触发
                                     # 一次 user 查询
        'items': [i.name for i
            in o.items],             # 每行再触发
                                     # 一次 items 查询
        'detail': o.detail.text,    # 大文本
    })

查询日志实测：301 条 SQL，1.8 秒
问题点：
1 经典双层 N+1（user + items）
2 detail 大文本全量加载但前端不展示
3 无总数查询优化（COUNT 全表）
4 排序字段无索引
5 循环内查询无批量意识`,
  steps: [
    ['开日志', '数一页的 SQL 条数', '2 分钟'],
    ['找 N+1', '循环内的关联访问', '3 分钟'],
    ['查投影', '加载的字段是否都用', '2 分钟'],
    ['看索引', '过滤排序字段覆盖', '2 分钟'],
    ['验分页', '深翻页与总数优化', '2 分钟'],
    ['写报告', '整改与防回归', '3 分钟'],
  ],
  full: `【代码审查报告 · 订单列表接口】

一、高危问题（性能事故级）

H1 双层 N+1（301 条 SQL 的元凶）
　现状：循环内访问 o.user 与 o.items，
　　ORM 惰性加载逐行发查询
　数据：30 行 ×（1 user + 1 items）
　　+ 1 列表 = 301 条，1.8 秒
　整改：预加载一步到位——
　　· joinedload（JOIN 合并）或
　　　selectinload（IN 批量）
　　· 两层关联同时预加载：
　　　options(user, items)
　效果：301 条 → 2 条，1.8 秒 → 90 毫秒

二、中危问题（2 处）
M1 大文本全量加载：
　detail.text 平均 40KB，列表页不展示——
　用字段投影只取列表所需列，
　详情接口再加载全文
M2 COUNT 全表：
　分页总数查询无过滤条件复用，
　大表下 COUNT 成为慢查询；
　改为估算总数或仅首页精确计数

三、建议（4 条）
· 开查询计数门禁：测试断言
　「本页 SQL ≤ 5 条」，N+1 回归即红
· 排序字段补索引（联合索引覆盖
　常用过滤+排序组合）
· 深翻页预案：偏移翻页超过
　100 页后切换游标分页
· ORM 查询审查清单进团队规范：
　循环内访问关联 = 条件反射查预加载

四、结论
N+1 是 ORM 项目最普遍的性能税，
修复便宜、收益立竿见影。
整改 0.5 人日 + 查询门禁防回归。`,
  stats2: [['SQL', '301→2'], ['耗时', '1.8s→90ms'], ['门禁', 'SQL≤5/页']],
  qa: [
    ['为什么 N+1 偏爱「列表+详情」结构？', '惰性加载的设计初衷是「用到才查」，在单对象场景是优点；但列表页的本质是「批量用到」，于是优点变成灾难：30 行数据触发 60 次关联查询。N+1 不是 ORM 的 bug，是「单条思维写批量代码」的必然产物——审查时看到 for 循环里摸关联属性，警报就该响。'],
    ['JOIN 预加载和 IN 预加载怎么选？', 'JOIN 把关联表合并进主查询，一次往返但结果集膨胀（多对多时行数爆炸）；IN 预加载先发主查询、再用 ID 列表批量查关联，两次往返但无膨胀。经验法则：一对多用 IN，一对一用 JOIN——选择依据是关联基数，不是个人口味。'],
    ['「查询计数门禁」为什么比人肉审查可靠？', 'N+1 的隐蔽性在于代码完全正确：功能对、语法对、小数据量下也不慢，只在生产数据规模下现形。测试断言「本页 SQL 不超过 5 条」把性能契约变成自动检查——人会漏看的模式，机器每次都数得清。'],
  ],
  fbs: [
    ['后端负责人', 'N+1 修掉后，列表接口从 1.8 秒到 90 毫秒'],
    ['测试同学', '查询门禁拦到过两次新写的 N+1'],
    ['前端同学', '列表秒开了，加载动画都白做了'],
  ],
  risks: ['ORM 的「方便」是糖，查询日志才是体检报告', '循环内访问关联属性必须预加载', '大字段按需投影，别全量搬运', '查询计数写进测试，防回归'],
}),
({
  t: '缓存层审查 · 穿透雪崩', sub: '9 分钟审完', d: '审查缓存代码：失效策略与击穿防护',
  ly: 'kanban', pal: 'red', bg: 'linen',
  facts: ['输入：缓存代码', '输出：审查报告', '问题 6 处'],
  inp: '粘贴缓存代码', out: ['高危：缓存穿透无防护', '高危：过期时间全相同', '建议：空值缓存+随机抖动+互斥重建'],
  stats: [['高危', '2 处'], ['中危', '2 处'], ['建议', '4 条']],
  quote: '缓存审查三连问：查不到怎么办、同时过期怎么办、热点挂了怎么办',
  fb: ['架构师', '这三问答不好的缓存，是大促夜的定时炸弹'], risk: '缓存与数据库的双写一致性，必须有明确策略',
  ctx: '商品详情页缓存，审查时按「三连问」走查：查不到的 key 直接打穿数据库（穿透）、所有缓存同一秒过期（雪崩）、热点商品缓存失效瞬间千倍流量回源（击穿）。三个经典问题一条不落。报告给出空值缓存、过期抖动、互斥重建三套整改。',
  raw: `审查对象：商品详情缓存（Redis）
问题代码片段：

def get_product(pid):
    data = redis.get('p:' + pid)
    if data:
        return json.loads(data)
    p = db.query_product(pid)   # 查不到返回 None
    if p:
        redis.setex('p:' + pid,
            3600, json.dumps(p)) # 全部 3600 秒
    return p

问题点：
1 穿透：不存在的商品每次查库，
  恶意请求用随机 ID 可打垮数据库
2 雪崩：批量预热同秒写入，
  同秒过期，数据库瞬间承受全量
3 击穿：热点商品过期瞬间，
  并发请求全部回源重建
4 双写不一致：更新商品后
  缓存更新逻辑缺失（只靠过期）
5 大 value：详情 JSON 无上限，
  大 Key 拖慢 Redis
6 无缓存命中率监控`,
  steps: [
    ['问穿透', '查不到的 key 去哪', '2 分钟'],
    ['问雪崩', '过期时间分布', '2 分钟'],
    ['问击穿', '热点失效瞬间', '2 分钟'],
    ['查一致', '写库与写缓存顺序', '2 分钟'],
    ['看容量', '大 Key 与命中率', '2 分钟'],
    ['写报告', '三防护方案', '3 分钟'],
  ],
  full: `【代码审查报告 · 商品详情缓存】

一、高危问题（2 处，大促级隐患）

H1 缓存穿透（恶意流量放大器）
　现状：不存在的商品 → 缓存必不命中 →
　　每次都打数据库；攻击者用随机负数
　　ID 即可把数据库打穿
　整改二选一（可叠加）：
　　· 空值缓存：查询为空的 key 也缓存
　　　空标记（短过期，如 60 秒）
　　· 布隆过滤器：所有合法商品 ID
　　　前置过滤，非法请求不进库

H2 缓存雪崩（批量同秒过期）
　现状：预热脚本批量写入，全部 3600 秒，
　　一小时后集体失效，数据库瞬时
　　承受全部回源
　整改：
　　· 过期时间加随机抖动：
　　　3600 + random(0, 600) 秒
　　· 预热分批写入，错峰起步
　　· 多级缓存兜底（本地缓存 30 秒）

二、中危问题（2 处）
M1 热点击穿：
　互斥重建——热点 key 失效时，
　只允许一个请求回源（分布式锁），
　其余等待或返回旧值
M2 双写不一致：
　明确策略「先更新数据库，
　再删除缓存」（删除而非更新），
　失败重试入队列；对一致性要求高的
　场景走变更订阅

三、建议（4 条）
· 大 Key 治理：value 超过 10KB 报警，
　详情拆分为基础信息 + 描述两段
· 命中率监控：跌破 90% 告警
　（穿透与雪崩的先行指标）
· 缓存降级开关：Redis 故障时
　限流直连数据库，而非全量放行
· 压测科目：缓存集体失效场景

四、结论
缓存三防护（穿透/雪崩/击穿）是标配，
缺一即埋雷。整改 2 人日，
大促前完成压测验收。`,
  stats2: [['空值缓存', '60 秒'], ['抖动', '0-600 秒'], ['命中率', '监控告警']],
  qa: [
    ['为什么「空值缓存」要设短过期？', '空值缓存的目的是挡住穿透，不是提供数据：如果空值长期缓存，商品真实上架后用户依然看到「不存在」。短过期（60 秒）在「防穿透」与「数据及时性」之间取了务实的平衡——缓存设计永远是在几个不完美之间选最不疼的那个。'],
    ['「删除缓存」为什么优于「更新缓存」？', '更新缓存有两个坑：并发写时后写的可能先更新缓存（顺序颠倒），以及缓存值可能需要复杂计算（白白浪费）。删除是幂等的、无顺序问题的、零计算成本的——下次读时自然重建。缓存领域「删」比「写」安全，是经过无数事故验证的经验。'],
    ['命中率为什么是穿透雪崩的「先行指标」？', '雪崩发生前，批量缓存临近过期，命中率开始下滑；穿透攻击时，大量非法 key 拉低命中率。命中率曲线比数据库 CPU 更早暴露问题——把监控埋在原因层而不是症状层，告警才能跑在事故前面。'],
  ],
  fbs: [
    ['架构师', '这三问答不好的缓存，是大促夜的定时炸弹'],
    ['运维同学', '命中率告警上线，雪崩前兆看得见'],
    ['开发同学', '过期抖动五行代码，压测数据稳多了'],
  ],
  risks: ['缓存与数据库的双写一致性，必须有明确策略', '批量缓存过期必须加抖动', '热点 key 要互斥重建', '命中率跌破阈值要告警'],
}),
];
