/* 「SQL 写与优化」深度版案例（1-5） */

export const DEEP_SQL_1 = [
({
  t: '慢查询优化 · 索引重建', sub: '10 分钟写完', d: '优化慢查询：执行计划分析与索引设计',
  ly: 'code', pal: 'blue', bg: 'grid',
  facts: ['输入：慢查询语句', '输出：优化方案', '提速 400 倍'],
  inp: '粘贴慢查询', out: ['结构：执行计划解读', '关键：索引列序设计', '配套：验证对比'],
  stats: [['耗时', '2.4s→6ms'], ['扫描', '全表→索引'], ['索引', '1 个联合']],
  quote: '慢查询优化的正路：先读执行计划看病，再开索引这副药——不看计划乱加索引是往数据库身上贴膏药',
  fb: ['开发', '加了联合索引后列表接口从转圈变成秒开'], risk: '不看执行计划就加索引，等于不诊断就开药：可能无效，还可能拖慢写入',
  ctx: '订单列表接口 2.4 秒，运营天天抱怨。拿到慢查询先跑执行计划：全表扫描 800 万行。查询条件是「用户 + 状态 + 时间排序」，据此设计联合索引（等值列在前、排序列在后），并砍掉查询里一个多余的函数包裹（让索引列失效）。优化后 6 毫秒。',
  raw: `慢查询：
订单列表按用户查询
SELECT * FROM orders
WHERE user_id = ?
AND status = ?
AND create_time > ?
ORDER BY create_time DESC
LIMIT 20

现状：
· 平均 2.4 秒
· 表 800 万行
· 只有主键索引

执行计划诊断：
· type = 全表扫描
· rows = 800 万
· 排序走了临时文件`,
  steps: [
    ['跑计划', '执行计划采集', '2 分钟'],
    ['读病灶', '扫描与排序问题定位', '2 分钟'],
    ['审语句', '索引失效点排查', '2 分钟'],
    ['设索引', '联合索引列序设计', '2 分钟'],
    ['上线索', '预发验证执行计划', '1 分钟'],
    ['做对比', '前后耗时与扫描对比', '1 分钟'],
  ],
  full: `【优化 · 慢查询（索引重建）】

一、执行计划诊断（先看病）
跑执行计划看到三个病灶：
病灶 1 全表扫描：
　没有任何索引匹配
　查询条件
病灶 2 文件排序：
　排序无法借用索引，
　走临时文件
病灶 3 回表量大：
　选出大量行再过滤

二、语句体检（再查语句本身）
发现一处索引杀手：
　条件里对列做了函数包裹
　（对 create_time 做函数运算）
——列被函数包住，
索引就用不上
改写为纯列比较：
　create_time > 某时间点
纪律：
优化索引之前，
先让语句「配得上」索引

三、联合索引设计（开药）
查询形状：
　user_id 等值 +
　status 等值 +
　create_time 范围与排序
索引列序原则：
　等值列在前，
　范围/排序列在后
建成：
　(user_id, status, create_time)
这个顺序让索引同时
完成三件事：
　定位用户 → 过滤状态 →
　按时间有序输出
（排序直接读索引顺序，
　文件排序消失）

四、为什么不是其他顺序
· (create_time, user_id, …)：
　范围列在前，
　后面的列用不上
· 单列 user_id：
　过滤后还要排序与
　大量回表
列序就是索引的处方，
顺序错 = 药不对症

五、验证闭环
预发环境：
· 重建后执行计划：
　扫描行数 800 万 → 23 行
· 排序消失（索引有序）
· 耗时 2.4 秒 → 6 毫秒
· 写入影响评估：
　新增索引的写放大
　在可接受范围
（索引不是免费的：
　每个索引都收写入税，
　加之前要评估）

六、结果
列表接口秒开；
优化的全过程
（计划→语句→索引→验证）
沉淀为组内标准动作。`,
  stats2: [['耗时', '2.4s→6ms'], ['扫描', '800 万→23 行'], ['排序', '文件→索引']],
  qa: [
    ['联合索引「等值在前、范围在后」的原理是什么？','索引是排好序的结构，它的能力在「连续性」：等值条件把搜索锁定到一个连续区间，这个区间内索引依然有序，可以继续过滤下一个等值列、或支持排序。一旦遇到范围条件，区间被打散成多段，后面的列在整体上就不再有序——范围列之后的索引列基本失效。所以列序的本质是：把能保持连续性的条件放前面，把会打断连续性的放最后。'],
    ['「对列做函数运算导致索引失效」背后的机制？','索引存储的是列的原始值并按它排序。当查询条件是「函数(列) > 某值」时，数据库需要对每一行现场计算函数结果再比较——索引里存的是原值，派不上用场，只能逐行算。改写的方向是把运算从列侧移到值侧：让列保持裸露，把计算做到常量那一侧。记住口诀：索引列要「素面朝天」，任何化妆品（函数、运算、隐式转换）都会让它认不出。'],
    ['加索引前评估「写入税」为什么必要？','因为索引是双刃剑：每次写入（增删改）都要同步维护所有索引，索引越多写入越慢，还占存储。一个只为一条慢查询加的索引，可能让高频写入变慢——用读的收益换写的成本，这笔账必须算。成熟的做法是：加索引前问「这条查询多频繁、写入多频繁」，事后观察写入指标。索引优化不是「多多益善」，是精准投放。'],
  ],
  fbs: [
    ['开发', '加了联合索引后列表接口从转圈变成秒开'],
    ['运营', '终于不用趁人少才敢查列表了'],
    ['同事', '「先跑执行计划」成了组内优化的第一步'],
  ],
  risks: ['不看执行计划就加索引，等于不诊断就开药：可能无效，还可能拖慢写入', '索引列禁止函数包裹', '联合索引列序遵循等值在前', '新增索引要评估写入影响'],
}),
({
  t: '深分页优化 · 游标替代', sub: '8 分钟写完', d: '优化深分页：偏移分页到游标分页',
  ly: 'terminal', pal: 'green', bg: 'dark',
  facts: ['输入：分页查询', '输出：优化方案', '提速 300 倍'],
  inp: '粘贴分页查询', out: ['结构：游标分页改造', '关键：有序唯一游标', '配套：兼容策略'],
  stats: [['第 1000 页', '3.1s→10ms'], ['原理', '跳过扫描'], ['限制', '声明']],
  quote: '深分页的病根：偏移分页让数据库替你「翻过」前几万行——游标分页直接告诉它从哪一行开始',
  fb: ['开发', '改成游标分页后，最后一页和第 1000 页一样快'], risk: '偏移分页在浅页面无感，在深页面指数级变慢——用户翻到深处的那一刻，就是系统卡死的那一刻',
  ctx: '后台列表翻到第 1000 页要 3 秒：偏移分页的「跳过 2 万行」是数据库逐行扫出来的。改造为游标分页：用有序且唯一的列做游标（上一页最后一条的排序键），查询条件变成「排序键 < 上次游标」，索引直接定位起点。同时声明游标分页的交互限制（不能跳页），前端改为「加载更多」。',
  raw: `深分页查询：
SELECT * FROM orders
ORDER BY id DESC
LIMIT 20 OFFSET 20000

性能曲线：
第 1 页：几十毫秒
第 100 页：几百毫秒
第 1000 页：3.1 秒
——偏移越大越慢，
因为前 2 万行要
读出来再扔掉

业务场景：
后台导出与浏览，
用户会不断「下一页」`,
  steps: [
    ['析病因', '偏移扫描机制解读', '2 分钟'],
    ['选游标', '有序唯一列确定', '2 分钟'],
    ['改查询', '游标条件替换偏移', '2 分钟'],
    ['配索引', '游标列索引确认', '1 分钟'],
    ['理交互', '跳页限制与替代', '2 分钟'],
    ['做兼容', '新旧并存策略', '1 分钟'],
  ],
  full: `【优化 · 深分页（游标替代）】

一、病因：偏移的代价
「LIMIT 20 OFFSET 20000」
的真实执行：
数据库取出 20020 行，
扔掉前 20000 行，
返回 20 行
——翻得越深，
扔得越多
偏移分页的成本
与页深成正比，
这是机制病，
不是调参能治的

二、游标方案（换机制）
游标 = 上一页最后一条
的排序键
查询变为：
SELECT * FROM orders
WHERE id < 上次最小 id
ORDER BY id DESC
LIMIT 20
数据库从游标位置
直接开始取 20 行——
不再翻也不扔
第 1 页与第 1000 页
成本相同

三、游标的选择条件
· 有序：与排序方向一致
· 唯一：不允许并列歧义
　（并列值会导致
　跨页重复或遗漏）
· 稳定：翻页期间不变
本例 id 完美符合；
若按时间排序，
则用 (时间, id) 复合游标
（时间可能并列，
id 保证唯一）

四、索引配合
· 游标列需要索引
　（本例主键天然满足）
· 复合游标建对应
　联合索引
· 执行计划验证：
　范围扫描 + 有序输出，
　无排序无临时表

五、交互限制（诚实声明）
游标分页的取舍：
· 不能跳页
　（不知道第 500 页
　的游标是什么）
· 适合「下一页/加载更多」
　与无限滚动
· 不适合「跳到第 N 页」
应对：
· 后台场景改
　「加载更多」交互
· 确需跳页的功能
　走搜索+过滤
　（用条件缩小范围，
　而不是翻到深处）

六、兼容过渡
· 接口参数双轨：
　偏移与游标都接受，
　返回里带下一页游标
· 浅页面行为不变，
　深页面自动受益
· 旧参数保留一个
　过渡期后下线

七、结果
第 1000 页 3.1 秒 → 10 毫秒；
任意深度耗时恒定。`,
  stats2: [['深页', '3.1s→10ms'], ['页深', '成本恒定'], ['交互', '加载更多']],
  qa: [
    ['游标分页为什么能做到「任意深度同速」？','因为它把「第几页」这个相对概念替换成了「从哪一行开始」这个绝对坐标。偏移分页的每次查询都要从头数到目标位置——深度即成本；游标分页直接给出起点坐标，数据库用索引一次定位——成本与深度无关。这是坐标制对序数制的降维打击：不再问「往后数两万行」，而是说「从这行开始」。'],
    ['游标列为什么必须「有序且唯一」？','唯一性保证翻页的边界清晰：如果排序值并列（比如十条记录时间相同），「小于某时间」的条件要么漏掉并列的几条，要么重复返回——翻着翻着数据就丢了或重了。用复合游标（时间+id）把并列关系彻底排序化：时间相同就比 id，总有高下。有序性则保证「下一页」的方向一致。游标的本质是给数据集定义一条没有歧义的行进路线。'],
    ['「不能跳页」这个限制应该怎么向产品解释？','用成本对比来解释：跳页功能要求系统随时知道「第 500 页从哪行开始」，这在数据持续变化时本身就不精确（新数据进来，所有页码都在漂移），而且维持深翻页能力的代价就是用户已经抱怨的卡顿。更诚实的产品方案是：把「翻到指定页」的需求转化为「用条件筛选到目标集合」——用户想跳页，真实意图通常是找特定数据，搜索比翻页更直达。'],
  ],
  fbs: [
    ['开发', '改成游标分页后，最后一页和第 1000 页一样快'],
    ['产品', '「加载更多」交互反而更符合使用习惯'],
    ['运维', '深翻页导致的慢查询告警清零'],
  ],
  risks: ['偏移分页在浅页面无感，在深页面指数级变慢——用户翻到深处的那一刻，就是系统卡死的那一刻', '游标列必须有序且唯一', '并列排序用复合游标', '跳页需求用搜索替代而非硬翻'],
}),
({
  t: '统计报表 · 预聚合表', sub: '12 分钟写完', d: '优化统计查询：预聚合与增量更新',
  ly: 'kanban', pal: 'gold', bg: 'paper',
  facts: ['输入：统计需求', '输出：预聚合方案', '提速 2000 倍'],
  inp: '粘贴统计需求', out: ['结构：预聚合表设计', '关键：增量刷新', '配套：口径文档'],
  stats: [['耗时', '20s→10ms'], ['刷新', '增量'], ['口径', '文档化']],
  quote: '统计报表的性能出路：把「每次现算」变成「提前算好」——查询读结果，刷新算增量',
  fb: ['数据组', '报表从 20 秒到毫秒级，而且口径终于有了唯一出处'], risk: '每次打开都现算全量聚合的报表，等于让每个看报表的人重复跑一遍批处理',
  ctx: '经营日报表每次打开要 20 秒：实时对明细表做分组聚合。改造为预聚合：按天汇总表（每天一行，存各维度指标）、增量刷新（每天只算当天增量）、报表查询只读汇总表。同时把聚合口径写成文档——预聚合表顺带解决了「同一指标多处计算口径不一致」的老问题。',
  raw: `报表需求：
经营日报：
按天统计订单量、金额、
用户数，支持维度筛选

现状：
SELECT 日期, SUM(金额),
COUNT(DISTINCT 用户) …
FROM 订单明细
GROUP BY 日期
——每次打开现算
800 万行，20 秒

问题二合一：
· 慢：重复现算
· 乱：各报表各写各的
  聚合，口径打架`,
  steps: [
    ['定粒度', '聚合维度与粒度', '2 分钟'],
    ['建汇总表', '结构设计', '3 分钟'],
    ['写刷新', '增量更新逻辑', '3 分钟'],
    ['改查询', '报表改读汇总', '2 分钟'],
    ['对口径', '口径文档化', '2 分钟'],
    ['做兜底', '历史重算机制', '1 分钟'],
  ],
  full: `【优化 · 统计报表（预聚合）】

一、聚合粒度设计
汇总表粒度 = 报表查询
的最细粒度：
　每天 × 每渠道 × 每类目
一行记录存：
　订单数 / 金额 /
　用户数 / 退款数…
原则：
· 粒度宁细勿粗
　（粗了上卷容易，
　细了无法还原）
· 指标存「可加」的中间量
　（金额、次数可加；
　「用户数」这类不可加
　指标单独处理，
　见口径说明）

二、增量刷新（核心）
每日刷新只算增量：
· 计算当天的聚合
· 写入/更新当天行
　（幂等：重跑覆盖）
· 不碰历史行
刷新窗口：
凌晨低峰执行，
完成后更新
「数据截至」时间戳
报表页展示
「数据更新至某日」
（诚实标注时效）

三、报表查询改造
原查询（20 秒）：
明细表全量聚合
新查询（毫秒）：
SELECT FROM 汇总表
WHERE 日期 BETWEEN …
AND 渠道 = …
扫描行数：
800 万 → 几百行
（预聚合把计算
　从查询时移到刷新时，
　成本只付一次）

四、不可加指标的口径
「去重用户数」跨天不可加
（今天 100 人 + 昨天 100 人
　≠ 两天 200 人）
处理：
· 日粒度用户数可预聚合
· 跨天用户数要么
　按区间单独算
　（范围有限，可接受），
　要么明确标注
　「日活之和≠区间活跃」
口径写进文档，
报表按口径标注

五、口径收口（副产品红利）
预聚合表成为唯一口径源：
· 所有报表读同一张表
· 口径变更走评审 +
　文档更新
· 历史争议
　（「金额含不含退款」）
　在文档里一次性定案
（报表性能问题的一半，
　其实是口径问题）

六、历史重算兜底
· 口径调整或数据修正时，
　支持指定日期范围重算
· 重算幂等覆盖
· 重算记录留档

七、结果
报表 20 秒 → 毫秒级；
口径争议从每周吵架
变成查文档。`,
  stats2: [['耗时', '20s→毫秒'], ['刷新', '增量幂等'], ['口径', '唯一出处']],
  qa: [
    ['「粒度宁细勿粗」的设计逻辑是什么？','因为聚合是有损压缩：细粒度汇总可以向上卷成粗粒度（按天汇总可加出按月），但粗粒度无法还原出细粒度（按月汇总拆不回按天）。把汇总表建在报表需要的最细粒度，等于保留了最大的查询灵活性；反之建粗了，任何更细的需求都要回到明表现算，预聚合白做。存储便宜，重算昂贵——粒度设计要为未来的问题留余地。'],
    ['不可加指标为什么是预聚合的经典陷阱？','因为「可加性」决定了指标能不能在汇总表上继续聚合。金额、次数可以直接求和上卷；但去重用户数、转化率、均值这类指标跨维度合并时语义会变——把每天的「去重用户数」加起来会得到一个比真实值大的数（重复用户被计了多次）。如果汇总表不区分这两类，下游拿着「加出来的去重数」做决策，错误会顺着报表链条传播。口径文档的首要任务就是标注哪些能加、哪些不能。'],
    ['预聚合为什么能顺带解决口径打架？','因为口径打架的根源是「同一个指标有多处实现」：三张报表三个写法，谁也不服谁。预聚合表把口径物化成一张实体表：指标只在这张表里被定义一次，所有读者消费同一个结果。口径从「分散在各查询里的写法」变成「集中在一处的资产」——争议不再是「你的算法还是我的算法」，而是「修改这张表的定义」，有了唯一的修订对象。'],
  ],
  fbs: [
    ['数据组', '报表从 20 秒到毫秒级，而且口径终于有了唯一出处'],
    ['业务方', '「数据更新至某日」的标注让时效不再含糊'],
    ['新同事', '口径文档是现成的指标教材'],
  ],
  risks: ['每次打开都现算全量聚合的报表，等于让每个看报表的人重复跑一遍批处理', '不可加指标必须单独标注口径', '刷新要幂等可重跑', '数据时效要诚实标注'],
}),
({
  t: '隐式转换 · 引号救索引', sub: '8 分钟写完', d: '排查隐式转换：类型不匹配导致索引失效',
  ly: 'doc', pal: 'red', bg: 'dots',
  facts: ['输入：慢查询', '输出：排查结论', '提速 500 倍'],
  inp: '粘贴慢查询', out: ['结构：类型比对排查', '关键：隐式转换识别', '配套：规范预防'],
  stats: [['耗时', '1.8s→3ms'], ['病因', '隐式转换'], ['规范', '类型对齐']],
  quote: '隐式转换是索引的隐形杀手：查询看起来没毛病，索引悄悄失效——凶手就藏在类型不匹配里',
  fb: ['开发', '加了一对引号查询快了 500 倍，全组都来围观这个最便宜的优化'], risk: '类型不匹配的查询不报错、不警告，只是默默变慢一百倍——最安静的故障最危险',
  ctx: '按订单号查询突然变慢，执行计划显示全表扫描，但索引明明存在。逐列比对发现：订单号列是字符串类型，查询传的是数字——数据库对每一行做隐式转换后再比较，索引失效。修复只需给参数加引号；随后立规范：查询参数类型必须与列类型一致，代码评审检查。',
  raw: `慢查询：
SELECT * FROM orders
WHERE order_no = 88213927

疑点：
· order_no 上有索引
· 执行计划却是全表扫描
· 语句「看起来」完全正常

真相：
order_no 列是字符串类型
传入的 88413927 是数字
比较时对每行做
隐式转换 →
索引无法使用`,
  steps: [
    ['看现象', '有索引却全表扫', '1 分钟'],
    ['比类型', '列类型与参数类型', '2 分钟'],
    ['验假设', '加引号重跑计划', '2 分钟'],
    ['查扩散', '全库同类问题扫描', '3 分钟'],
    ['立规范', '类型对齐规则', '1 分钟'],
    ['进评审', '评审检查项', '1 分钟'],
  ],
  full: `【排查 · 隐式转换（引号救索引）】

一、现象与矛盾
· 慢查询有索引，
　但执行计划全表扫描
· 常规嫌疑（无索引、
　函数包裹）都排除
· 矛盾点：
　索引存在却不用 =
　数据库「认为」用不上

二、类型比对（破案）
对照两侧：
· 列：字符串类型
· 参数：数字
比较规则：
字符串列与数字比较时，
数据库把字符串逐行
转成数字再比——
转换发生在列上，
等于给每行包了函数，
索引失效
（索引存的是原字符串，
转换后的值查不到）

三、验证与修复
加引号：
WHERE order_no = '88413927'
重跑执行计划：
全表扫描 → 索引定位
耗时 1.8 秒 → 3 毫秒
修复成本：一对引号
（最便宜的优化，
最贵的教训）

四、扩散排查
同类问题扫描：
· 找出所有「字符串列
　被数字比较」的查询
· 常见重灾区：
　手机号、证件号、
　单号类字段
　（业务语义像数字，
　实际该用字符串存）
· 修复一批潜在慢查询

五、根治规范
1 类型对齐铁律：
　查询参数类型 =
　列类型，
　不允许隐式转换兜底
2 建模规范：
　「像数字的标识符」
　（单号、手机号）
　用字符串类型
　（它们不做算术，
　且有前导零等需求）
3 代码评审检查项：
　新增查询对照列类型
4 执行计划巡检：
　定期抽查高频查询的
　计划变化

六、为什么数据库不报错
隐式转换是「贴心」特性：
它保证查询有结果，
代价是性能而非正确性——
所以它从不报错，
只是悄悄变慢
这类「安静失效」
必须靠规范与巡检防御

七、结果
单点修复 + 全库扫描，
同类隐患一次清零；
类型对齐进评审清单。`,
  stats2: [['修复', '一对引号'], ['提速', '500 倍'], ['扩散', '全库清零']],
  qa: [
    ['为什么「列上发生转换」索引就失效，「值上发生转换」却没事？','因为索引是按列的原始值建立的路标。查询条件转换的是常量值（比如把字符串常量转成数字），转换一次即可，索引照常使用；但当比较规则要求转换每一行的列值时，数据库必须逐行计算转换结果——索引里没有存转换后的值，路标作废，只能逐行扫。记住方向：运算必须留在等号的常量一侧，列的一侧永远保持原样。'],
    ['单号、手机号为什么建议用字符串类型存？','三个理由：它们从不参与算术运算（「手机号加一」没有意义），却常需要精确保真（前导零、长度）；字符串类型天然避免隐式转换事故（传数字才会触发转换，字符串列配字符串参数永远对齐）；展示与拼接是它们的真实用途，字符串语义更贴切。「看起来像数字」不是选数字类型的理由——类型应该跟着运算语义走，而不是跟着长相走。'],
    ['隐式转换这类「安静失效」为什么必须靠规范防御？','因为它的故障模式避开了所有常规警报：不报错（结果正确）、不崩溃（只是慢）、甚至不总是慢（数据量小的时候无感）。等到它被察觉，通常已经在大表上拖了很久。对这类问题，事后检测永远滞后——只有前置规范（类型对齐铁律+评审检查）能把问题挡在写入之前。安静的杀手，要用制度去抓。'],
  ],
  fbs: [
    ['开发', '加了一对引号查询快了 500 倍，全组都来围观这个最便宜的优化'],
    ['评审者', '类型对齐进了评审清单，同类问题不再复发'],
    ['运维', '全库扫描顺手清掉一批潜在慢查询'],
  ],
  risks: ['类型不匹配的查询不报错、不警告，只是默默变慢一百倍——最安静的故障最危险', '标识符类字段用字符串类型', '查询参数与列类型严格对齐', '定期巡检高频查询的执行计划'],
}),
({
  t: 'JOIN 优化 · 小表驱动', sub: '10 分钟写完', d: '优化多表关联：驱动表选择与索引配合',
  ly: 'sheet', pal: 'purple', bg: 'linen',
  facts: ['输入：关联查询', '输出：优化方案', '提速 60 倍'],
  inp: '粘贴关联查询', out: ['结构：驱动表重排', '关键：被驱动表索引', '配套：过滤前置'],
  stats: [['耗时', '1.2s→20ms'], ['驱动', '小表先行'], ['索引', '被驱动列']],
  quote: 'JOIN 的成本公式：驱动表行数 × 每次探测成本——让小表开车，给被驱动表修好索引这条高速路',
  fb: ['开发', '调整驱动顺序加索引后，报表查询从秒级掉进毫秒级'], risk: '不看行数就写 JOIN，等于让百万大军当司机去挨家挨户敲门',
  ctx: '订单表关联用户表的查询 1.2 秒。分析：当前驱动表是订单表（800 万行），每行都要去用户表探测。优化三步：把过滤后更小的结果集作为驱动（条件过滤前置）、确认被驱动表的关联列有索引、必要时改写关联顺序。优化后 20 毫秒。',
  raw: `慢查询：
订单表 JOIN 用户表
按时间范围查订单
并带出用户信息

执行计划分析：
· 驱动表：订单表
  （范围内仍有 50 万行）
· 每行去用户表探测一次
· 用户表关联列无索引，
  每次探测都扫描

成本公式：
驱动行数 × 单次探测成本
= 50 万 × 全表扫描
= 灾难`,
  steps: [
    ['算成本', '驱动行数×探测成本', '2 分钟'],
    ['前置过滤', '条件尽早收窄', '3 分钟'],
    ['修索引', '被驱动关联列加索引', '2 分钟'],
    ['调顺序', '驱动表重排', '2 分钟'],
    ['验计划', '前后执行计划对比', '1 分钟'],
    ['审改写', '必要时重写语句', '1 分钟'],
  ],
  full: `【优化 · JOIN（小表驱动）】

一、成本模型（先算账）
嵌套关联的成本：
驱动表行数 ×
每次探测的成本
两个旋钮：
· 驱动表越小越好
　（少开车）
· 单次探测越快越好
　（路要好）
优化就是同时拧
这两个旋钮

二、过滤前置（收窄驱动）
原查询把时间过滤
放在关联之后：
先关联 800 万，
再过滤出 50 万
改写：
让过滤条件尽早生效，
驱动侧只保留
范围内的行
原则：
能先过滤的先过滤，
让参与关联的行数
最小化

三、被驱动表修路（关键）
被驱动表的关联列
必须有索引：
· 探测从「全表扫描」
　变成「索引定位」
· 单次探测成本
　从 O(表大小)
　降到 O(对数)
本例给用户表关联列
建索引后，
每次探测从扫描
变成一次索引查找

四、驱动顺序重排
索引修好后对比两种顺序：
· 订单驱动：
　50 万次索引探测
· 用户驱动（先筛活跃用户）：
　探测次数可能更多或更少
　——以执行计划的
　实际估算为准
结论不靠直觉，
靠计划对比：
让数据库给出
两种顺序的成本，
选低的

五、验证闭环
优化后执行计划：
· 驱动侧行数大幅下降
· 关联走索引探测
· 无临时表、无文件排序
耗时：1.2 秒 → 20 毫秒

六、JOIN 优化清单（沉淀）
1 关联前先过滤
2 被驱动表关联列必有索引
3 驱动顺序看计划不看感觉
4 关联列类型一致
　（防隐式转换毁索引）
5 超大关联考虑分批
　或预关联

七、结果
报表查询进毫秒级；
清单成为组内
JOIN 评审标准。`,
  stats2: [['耗时', '1.2s→20ms'], ['探测', '索引化'], ['顺序', '计划说了算']],
  qa: [
    ['「小表驱动」里的「小表」到底指什么？','指过滤之后的结果集大小，不是表的总行数。一张 800 万行的大表，如果查询条件筛完只剩几百行，它就是优秀的驱动表；一张 10 万行的小表，如果没有任何过滤条件，驱动它也要探测十万次。所以「小表驱动」的准确表述是「小结果集驱动」——优化者的工作是让过滤尽早发生，把驱动侧喂瘦。'],
    ['被驱动表的索引为什么比驱动表的索引更紧急？','因为成本公式是不对称的：驱动表的行数决定探测次数，被驱动表的索引决定每次探测的成本——而探测成本被乘以了全部驱动行数。被驱动表无索引，等于把「全表扫描」这个最贵的动作执行了 N 遍。给被驱动表关联列建索引，是把乘法里那个最大的因子按下去。资源要花在杠杆最长的地方。'],
    ['为什么驱动顺序要「看计划不看感觉」？','因为人的直觉只看得见表的大小，看不见过滤后的结果集与索引的实际效果。看起来的大表可能已被条件筛成小集，看起来的优化顺序可能触发更差的探测路径。执行计划是数据库基于统计信息给出的成本估算——它不完美，但比直觉靠谱得多。优化 JOIN 的科学流程是：提出顺序假设→各自看计划→用估算成本裁决，而不是拍脑袋重排。'],
  ],
  fbs: [
    ['开发', '调整驱动顺序加索引后，报表查询从秒级掉进毫秒级'],
    ['评审者', 'JOIN 清单五条，评审照着过一遍就行'],
    ['新同事', '成本公式一讲就懂，不用背口诀'],
  ],
  risks: ['不看行数就写 JOIN，等于让百万大军当司机去挨家挨户敲门', '过滤条件尽量前置', '被驱动表关联列必须有索引', '关联顺序以执行计划为准'],
}),
];
