/* 「代码审查」深度版案例（1-5） */

export const DEEP_REVIEW_1 = [
({
  t: 'Python 接口审查 · 异常吞没', sub: '8 分钟审完', d: '审查 Python 接口：异常处理与错误传播',
  ly: 'terminal', pal: 'purple', bg: 'dark',
  facts: ['输入：接口代码', '输出：审查报告', '问题 6 处'],
  inp: '粘贴接口代码', out: ['高危：裸 except 吞掉全部异常', '中危：错误信息直接回写客户端', '建议：分层捕获+结构化日志'],
  stats: [['高危', '2 处'], ['中危', '3 处'], ['建议', '4 条']],
  quote: '裸 except 是代码审查的头号目标：它把故障藏进了地毯',
  fb: ['后端负责人', '这次审查拦下的异常吞没，避免了三次线上抓瞎'], risk: '异常日志必须带 trace_id，否则查无可查',
  ctx: '一个下单接口的 Python 代码，用裸 except 把所有异常吃干净：数据库挂了返回「下单成功」，参数错了返回 200。这类代码上线后最可怕的不是报错，是「看起来一切正常」。审查挖出 6 处问题，核心整改是分层捕获异常、结构化日志带 trace_id。',
  raw: `审查对象：订单服务 create_order 接口（Python/Flask）
问题代码片段：

def create_order(req):
    try:
        user = get_user(req.user_id)
        items = check_inventory(req.items)
        order = Order.create(user, items)
        pay = call_payment(order)
        send_mq(order.id)
        return ok(order)
    except:
        log("error happened")
        return ok(None)

问题点：
1 裸 except：所有异常（含 KeyboardInterrupt）被吞
2 返回 ok：失败也返回成功码，客户端无法感知
3 log 无上下文：无 trace_id、无参数、无堆栈
4 事务缺失：Order.create 成功后支付失败，
  订单悬挂
5 MQ 同步发送：消息中间件抖动拖垮接口
6 无幂等键：重试会重复下单`,
  steps: [
    ['读契约', '先看接口的成功失败语义', '2 分钟'],
    ['查异常', '逐层检查捕获与传播', '3 分钟'],
    ['看事务', '多资源操作的原子性', '3 分钟'],
    ['查日志', '上下文与敏感信息', '2 分钟'],
    ['评幂等', '重试场景的正确性', '2 分钟'],
    ['写报告', '分级问题与整改建议', '3 分钟'],
  ],
  full: `【代码审查报告 · create_order 接口】

一、高危问题（2 处，上线阻断）

H1 裸 except 吞没异常
　位置：create_order 主体
　危害：数据库宕机、支付超时、库存服务
　　异常——全部被吞，客户端收到「成功」
　整改：按类型分层捕获
　　· 参数校验错误 → 400 + 错误码
　　· 业务冲突（库存不足）→ 422 + 明确原因
　　· 系统异常 → 500 + 统一错误结构，
　　　堆栈进日志不进响应
　　· 禁止捕获 BaseException

H2 失败路径返回成功码
　危害：故障不可观测，客服与监控同时失明
　整改：统一响应结构，
　　错误码表（如 ORD_1001 库存不足）

二、中危问题（3 处，本迭代修复）

M1 事务边界缺失
　现状：建单成功 + 支付失败 = 悬挂订单
　整改：支付失败回滚订单或转「待支付」
　　状态 + 定时关单兜底

M2 日志无上下文
　现状：log("error happened") 等于没写
　整改：结构化日志 = 时间 + trace_id +
　　用户 ID + 订单号 + 异常类型 + 堆栈

M3 MQ 同步发送
　危害：中间件抖动直接放大为接口超时
　整改：本地事件表 + 异步投递，
　　或至少加超时与降级

三、建议（4 条）
· 接口加幂等键（客户端生成 request_id）
· 对外依赖全部加超时（默认无超时 = 隐患）
· 补充接口级监控：成功率跌破 99% 告警
· 单元测试覆盖三条失败路径

四、结论
阻断上线，整改 H1/H2 后复审。
预计整改 1.5 人日。`,
  stats2: [['异常路径', '3 条补齐'], ['监控', '成功率'], ['复审', '阻断级']],
  qa: [
    ['为什么裸 except 是审查的头号目标？', '异常是系统向你报告真相的唯一通道。裸 except 等于把所有真相就地掩埋：故障发生了，但没人知道在哪、为什么。上线后表现为「偶发数据不一致」，排查成本是当场处理的十倍——审查时它永远值一个阻断。'],
    ['「失败也返回 200」比直接报错更危险吗？', '是的。报错至少会触发告警和客诉，是可发现的故障；返回假成功是静默腐坏：订单丢了但流程显示完成，用户付了钱但没单。静默错误的平均发现周期以天计，而显式错误以分钟计。'],
    ['异常日志为什么必须带 trace_id？', '分布式系统里一次请求穿过多个服务，没有 trace_id 的日志是散落一地的碎片：你知道出错了，但拼不出这次请求的完整路径。trace_id 是把碎片串回故事的线，结构化日志是索引——两者缺一，线上排障只能靠猜。'],
  ],
  fbs: [
    ['后端负责人', '这次审查拦下的异常吞没，避免了三次线上抓瞎'],
    ['测试同学', '补的三条失败路径用例，真抓出了回归'],
    ['值班运维', 'trace_id 上线后，告警定位从小时到分钟'],
  ],
  risks: ['异常日志必须带 trace_id，否则查无可查', '裸 except 一律打回，无例外', '失败路径必须有测试覆盖', '对外依赖超时必须显式设置'],
}),
({
  t: 'React 组件审查 · 状态竞态', sub: '8 分钟审完', d: '审查 React 组件：异步状态与竞态条件',
  ly: 'code', pal: 'blue', bg: 'grid',
  facts: ['输入：组件代码', '输出：审查报告', '问题 5 处'],
  inp: '粘贴组件代码', out: ['高危：请求竞态导致旧数据覆盖', '中危：闭包陷阱读到旧 state', '建议：竞态防护+副作用清理'],
  stats: [['高危', '1 处'], ['中危', '2 处'], ['建议', '3 条']],
  quote: '异步组件审查先问一句：用户快速切换时，回来的数据还是当前的吗',
  fb: ['前端负责人', '竞态防护加上后，「数据闪回」的投诉消失了'], risk: 'useEffect 依赖数组漏项，是闭包陷阱的温床',
  ctx: '一个用户详情页组件，用户快速切换不同用户时，页面偶尔显示上一个用户的数据——典型的异步竞态：旧请求晚返回，覆盖了新数据。审查挖出竞态、闭包陷阱、副作用泄漏三类问题，核心整改是「请求守卫 + 清理函数 + 依赖数组体检」。',
  raw: `审查对象：UserDetail 组件（React/Hooks）
问题代码片段：

function UserDetail(props):
    state = useState(null)

    useEffect(() => {
        fetch('/api/user/' + props.userId)
            .then(res => setState(res.data))
    }, [])

    function toggleFollow():
        setFollowed(!followed)
        api.post('/follow', { id: props.userId,
            followed: followed })

    return render(user)

问题点：
1 依赖数组为空：props.userId 变化不重新请求
2 竞态：快速切换用户时，旧请求后返回
  覆盖新数据（无请求守卫）
3 闭包陷阱：toggleFollow 读到旧 followed 值，
  上报状态与实际相反
4 组件卸载后 setState：内存泄漏警告
5 无加载/错误态：请求中页面空白`,
  steps: [
    ['画数据流', '状态从哪来到哪去', '2 分钟'],
    ['查竞态', '异步返回与当前意图对齐', '3 分钟'],
    ['看闭包', '事件处理器读的 state', '2 分钟'],
    ['查清理', '副作用的卸载处理', '2 分钟'],
    ['评体验', '加载错误态完整性', '1 分钟'],
    ['写报告', '分级与整改建议', '3 分钟'],
  ],
  full: `【代码审查报告 · UserDetail 组件】

一、高危问题（1 处）

H1 请求竞态：旧数据覆盖新数据
　复现：快速从用户 A 切到用户 B，
　　A 的请求晚 200ms 返回 → 页面显示 A
　整改：请求守卫三选一——
　　· 清理函数标记废弃（推荐）：
　　　useEffect 返回 cleanup，
　　　响应回来时检查「是否已过期」
　　· AbortController 真正取消请求
　　· 请求序号比对（seq 递增，
　　　只接受最新 seq 的响应）

二、中危问题（2 处）

M1 依赖数组漏项（闭包陷阱温床）
　现状：useEffect(fn, []) 缺 props.userId
　整改：补齐依赖；用 lint 规则
　　（exhaustive-deps）守门

M2 toggleFollow 闭包陷阱
　现状：上报的 followed 是本次渲染的旧值
　整改：用函数式更新 + 上报用最终值
　　或乐观更新失败回滚模式

三、体验问题（2 处）
· 卸载后 setState：清理函数中
　置「已卸载」标记
· 无加载/错误态：补 skeleton 与
　重试入口

四、建议（3 条）
· 数据获取逻辑抽成 useUser hook，
　竞态防护内聚在 hook 内
· 竞态写进组件测试：
　模拟慢响应断言最终数据
· 考虑 SWR/React Query：
　竞态、缓存、重试开箱即用

五、结论
可上线但需本迭代修复 H1（真实客诉级），
其余随版本带走。`,
  stats2: [['竞态防护', '守卫模式'], ['测试', '慢响应断言'], ['客诉', '归零']],
  qa: [
    ['为什么「依赖数组为空」必须打回？', '依赖数组是 React 的「何时重跑」契约。漏写依赖意味着组件对 props 变化失明——用户切了人，组件还在显示旧人的数据请求。更糟的是它教会团队坏习惯：依赖数组成了随手一写的摆设。lint 规则 exhaustive-deps 是这类问题的自动审查员。'],
    ['闭包陷阱的本质是什么？', '事件处理器持有的是「创建时那次渲染」的 state 快照。toggleFollow 里读到的 followed 是渲染时的值，点击时可能早已过期。函数式更新（setX(prev => ...)）绕开了快照依赖——审查时凡是「读 state 再上报/计算」的代码，都值得多看一眼。'],
    ['竞态防护为什么推荐「守卫」而不是「取消」？', 'AbortController 语义最正确，但需要一路传递 signal、处理 abort 异常，改造面大。守卫模式（标记过期、丢弃响应）改动最小、效果等价，适合存量代码整改——审查建议要考虑落地成本，最优解是团队能坚持执行的解。'],
  ],
  fbs: [
    ['前端负责人', '竞态防护加上后，「数据闪回」的投诉消失了'],
    ['测试同学', '慢响应竞态用例，回归时真抓到过'],
    ['新同事', '依赖数组体检这条，成了我们组 lint 规则'],
  ],
  risks: ['useEffect 依赖数组漏项，是闭包陷阱的温床', '异步返回必须校验「还是当前的吗」', '卸载后 setState 要有清理标记', '竞态要有测试用例，不能只靠自觉'],
}),
({
  t: 'Go 微服务审查 · 并发安全', sub: '9 分钟审完', d: '审查 Go 服务：goroutine 泄漏与共享状态',
  ly: 'terminal', pal: 'green', bg: 'dark',
  facts: ['输入：服务代码', '输出：审查报告', '问题 6 处'],
  inp: '粘贴服务代码', out: ['高危：goroutine 无退出路径', '高危：共享 map 并发写无锁', '建议：context 贯穿+等待组'],
  stats: [['高危', '2 处'], ['中危', '2 处'], ['建议', '3 条']],
  quote: 'Go 代码审查的两把尺子：goroutine 怎么退出，共享状态谁在写',
  fb: ['架构师', '这两类问题抓到一次，就省一次凌晨重启'], risk: 'range channel 的消费协程，必须有退出信号',
  ctx: '一个 Go 写的事件消费服务，上线两周内存缓慢上涨——goroutine 泄漏；高峰期偶发崩溃——共享 map 并发写。这两个是 Go 审查的经典组合。审查报告给出 context 贯穿、退出路径、sync 保护三套整改，并附上 pprof 验证方法。',
  raw: `审查对象：事件消费服务（Go）
问题代码片段：

func StartConsumer(ch chan Event):
    for i := 0; i < 10; i++:
        go func():
            for ev := range ch:
                process(ev)
                stats[ev.Type]++
        ()

func process(ev Event):
    go notifyDownstream(ev)

var stats = make(map[string]int)

问题点：
1 消费协程 range ch：channel 永不关闭，
  协程永不退出；重连时新起 10 个，
  泄漏累积（内存上涨元凶）
2 stats 共享 map：10 个协程并发写，
  无锁，fatal: concurrent map writes
3 notifyDownstream 裸 go：
  无错误处理无重试，失败静默
4 无 context：服务关闭时无法优雅停止
5 process 错误被丢弃
6 无限流：下游抖动时无背压`,
  steps: [
    ['数协程', '每个 go 语句的退出路径', '3 分钟'],
    ['查共享', '可变状态的读写双方', '3 分钟'],
    ['看 context', '生命周期与取消传播', '2 分钟'],
    ['评错误', '被丢弃的 error 逐个找', '2 分钟'],
    ['验工具', 'pprof 与 race 检测建议', '2 分钟'],
    ['写报告', '分级整改与验证方法', '3 分钟'],
  ],
  full: `【代码审查报告 · 事件消费服务（Go）】

一、高危问题（2 处，线上事故级）

H1 goroutine 泄漏（内存上涨元凶）
　现状：消费协程 range ch，channel 永不
　　关闭；每次重连新起 10 个协程，
　　旧的永远阻塞在 range
　整改：
　　· context 贯穿：for 循环同时
　　　select ctx.Done() 与 channel
　　· 消费协程用 sync.WaitGroup 管理，
　　　服务关闭时 Wait 全部退出
　　· 重连先取消旧 context 再起新协程
　验证：pprof 看 goroutine 数量曲线，
　　重连 100 次后数量恒定

H2 共享 map 并发写（崩溃元凶）
　现状：10 协程并发 stats[ev.Type]++，
　　map 并发写在 Go 中直接 fatal
　整改三选一：
　　· sync.Mutex 保护（最简单）
　　· sync.Map（读多写少场景）
　　· 统计收敛到单协程：
　　　各协程发统计事件到专用 channel
　推荐第三种：消除共享，而非加锁

二、中危问题（2 处）
M1 裸 go notifyDownstream：
　错误无人处理，失败无重试
　整改：改为工作池 + 重试队列，
　至少带退避重试 3 次
M2 无优雅停机：
　SIGTERM 时 in-flight 事件丢失
　整改：信号监听 + context 取消 +
　WaitGroup 等待 + 未处理事件回写队列

三、建议（3 条）
· 加背压：下游慢时消费降速
　（令牌桶或自适应并发数）
· CI 跑 go test -race，
　并发写问题在流水线就拦住
· 关键路径 error 禁止下划线丢弃，
　用 lint（errcheck）守门

四、结论
H1/H2 任一都足以打回。
整改 2 人日，附压测验收：
10 万次重连 + 并发峰值 30 分钟。`,
  stats2: [['泄漏验证', '重连 100 次'], ['race 检测', '进 CI'], ['背压', '已加']],
  qa: [
    ['goroutine 泄漏为什么偏爱「range channel」？', 'range 是阻塞式的忠诚等待：只要 channel 不关闭、没有数据，协程就永远停在那里，没有超时、没有退出。这是 Go 并发的经典陷阱——审查时看到 range 消费协程，立刻问三件事：谁关闭它？有没有 ctx 退出？重连时旧的怎么办？'],
    ['「消除共享优于加锁」怎么理解？', '锁保护的是共享状态的访问，但共享状态本身是并发错误的土壤：锁会忘、会错序、会死锁。把统计收敛到单协程（通过 channel 传递），共享就不存在了，竞态也不存在了——最好的并发安全是没有并发竞争。'],
    ['为什么 -race 检测必须进 CI 而不是靠人工审查？', '数据竞态是概率性故障：代码审查能抓明显的（如本例的并发写），但复杂的时序交错只有工具能发现。go test -race 用影子内存检测每一次读写的同步关系，是零误报的守门员——机器能做的事别占用人眼。'],
  ],
  fbs: [
    ['架构师', '这两类问题抓到一次，就省一次凌晨重启'],
    ['Go 开发', '消除共享那条思路，打开了新世界'],
    ['SRE', 'pprof 验证法写进了我们的上线检查单'],
  ],
  risks: ['range channel 的消费协程，必须有退出信号', '共享可变状态必须有同步方案或消除', '服务停机路径要有测试', 'errcheck 与 -race 进 CI'],
}),
({
  t: '支付模块审查 · 金额精度', sub: '9 分钟审完', d: '审查支付代码：金额表示与计算精度',
  ly: 'code', pal: 'red', bg: 'grid',
  facts: ['输入：支付代码', '输出：审查报告', '问题 7 处'],
  inp: '粘贴支付代码', out: ['高危：浮点数参与金额计算', '高危：分转元四舍五入错误', '建议：最小货币单位+Decimal'],
  stats: [['高危', '2 处'], ['中危', '3 处'], ['建议', '4 条']],
  quote: '支付代码审查第一问：金额在整条链路上是什么类型',
  fb: ['支付架构师', '精度问题在支付域是零容忍：一分钱错账能查三天'], risk: '金额类型不统一的系统，对账就是无底洞',
  ctx: '一个促销支付模块，浮点数算折扣、字符串拼接金额、分元转换随手四舍五入——集齐了支付精度问题的全家桶。这类代码的错误不是「可能出错」，是「必然在某些金额上出错」。审查强制统一为最小货币单位（分）整数运算，Decimal 兜底除法，并给出对账验证方案。',
  raw: `审查对象：促销支付模块
问题代码片段：

price = 19.9
discount = 0.85
final = price * discount          # 16.915
charge = round(final, 2)          # 浮点链路

fen = int(float(amount_str) * 100) # "16.915" → 1691（截断）

split = total / 3                  # 无限小数分账

问题点：
1 浮点数参与金额计算：
  0.1 + 0.2 ≠ 0.3 的家族问题
2 round 四舍五入与银行家舍入不统一
3 字符串转分：float 中转导致截断
4 分账除法产生无限小数，
  三份之和 ≠ 总额（1 分钱黑洞）
5 金额字段在接口中混用元/分两种单位
6 无对账锚点：每笔订单的
  应收实收无记录
7 退款计算未复用正向逻辑，
  两套舍入`,
  steps: [
    ['追类型', '金额在链路上的类型流', '3 分钟'],
    ['查运算', '每个算术操作的精度', '3 分钟'],
    ['看转换', '元分转换与舍入规则', '2 分钟'],
    ['验分账', '多份之和与总额核对', '2 分钟'],
    ['评对账', '应收实收锚点设计', '2 分钟'],
    ['写报告', '统一方案与验证', '3 分钟'],
  ],
  full: `【代码审查报告 · 促销支付模块】

一、高危问题（2 处，资金安全级）

H1 浮点数参与金额计算
　现状：19.9 × 0.85 在二进制浮点里
　　无法精确表示，误差随运算累积
　整改：金额全链路整数化——
　　· 内部统一用「最小货币单位」（分）
　　　的 int64 表示
　　· 接口层显式标注单位字段
　　· 需要小数运算（折扣率）时用
　　　Decimal/定点，禁止 float

H2 分账产生金额黑洞
　现状：100 元分 3 份 = 33.33×3 = 99.99
　　1 分钱凭空消失
　整改：余数分配法——
　　· 均分取整：3333 分 × 3 = 9999
　　· 余数 1 分给第一份（或按规则）
　　· 校验断言：所有份之和 === 总额

二、中危问题（3 处）
M1 舍入规则不统一：
　全系统一种舍入（建议银行家舍入），
　写进金额工具库，禁止散落各处的 round
M2 元转分用 float 中转：
　"16.915" → 1691（截断）；
　改为字符串解析或 Decimal 转换
M3 退款两套逻辑：
　退款必须引用正向订单的实收金额，
　禁止重新计算

三、建议（4 条）
· 金额工具库：加减乘、分转元、
　舍入、分账，单一入口
· 每笔订单落「对账锚点」：
　应收 = 明细求和、实收 = 支付回调，
　两者不等即告警
· 单元测试用「分」边界值：
　0、1、99、100、19999999
· 上线后跑「万元随机订单」
　对账脚本一周

四、结论
资金模块，H 级问题零容忍。
整改 3 人日 + 对账验收。`,
  stats2: [['金额单位', '统一为分'], ['分账校验', '断言守恒'], ['对账', '每日跑']],
  qa: [
    ['为什么浮点金额是「必然出错」而不是「可能出错」？', '0.1 在二进制里是无限循环小数，这不是实现缺陷而是表示体系的先天限制。只要计算链路上存在无法精确表示的十进制小数，误差就一定在某些输入上显现——支付审查里这类问题不需要「触发条件」，它的存在本身就是事故。'],
    ['分账的「余数分配法」为什么是标准答案？', '货币不可无限分割：分是最小单位，100 元除以 3 在数学上无限，在货币上必须有人多拿 1 分或平台兜底。余数分配法把「除不尽」变成确定性的规则——关键是守恒断言：无论怎么分，份数之和必须等于总额，这条断言是所有分账逻辑的安全带。'],
    ['「退款禁止重新计算」的道理是什么？', '退款的正确金额只有一个：当初实际收了多少。重新计算等于用今天的规则重放历史，舍入、汇率、促销任何一个变量变了，退款金额就和实收对不上。引用正向实收是「原路返回」原则——资金链路可追溯的基础。'],
  ],
  fbs: [
    ['支付架构师', '精度问题在支付域是零容忍：一分钱错账能查三天'],
    ['财务同学', '对账锚点上线后，月度对账从两天变半小时'],
    ['开发同学', '金额工具库真香，再也不用自己写转换'],
  ],
  risks: ['金额类型不统一的系统，对账就是无底洞', '分账必须有守恒断言', '舍入规则全局唯一，禁止散落的 round', '退款引用实收，禁止重算'],
}),
({
  t: 'SQL 脚本审查 · 全表扫描', sub: '8 分钟审完', d: '审查 SQL 变更脚本：执行计划与锁风险',
  ly: 'sheet', pal: 'gold', bg: 'linen',
  facts: ['输入：SQL 脚本', '输出：审查报告', '问题 5 处'],
  inp: '粘贴 SQL 脚本', out: ['高危：无 WHERE 的 UPDATE', '高危：大表加索引未评估锁', '建议：分批执行+执行计划先行'],
  stats: [['高危', '2 处'], ['中危', '2 处'], ['建议', '3 条']],
  quote: 'SQL 审查先看两样：执行计划长什么样，锁会咬到谁',
  fb: ['DBA', '这两条高危拦下来，就是拦下了一次全站慢查询'], risk: '线上 DDL 必须评估锁范围，大表用 Online DDL 工具',
  ctx: '一个数据订正脚本：无 WHERE 的 UPDATE、大表直接加索引、事务包着长查询——每一条单拎出来都能造成线上事故。SQL 脚本审查的核心是「执行前想象执行后」：看执行计划、估锁范围、算回滚代价。这份报告把三条高危脚本改成分批低峰执行的安全版本。',
  raw: `审查对象：数据订正脚本（订单一表，4800 万行）

脚本 1：
UPDATE orders SET status = 9 WHERE user_id = 88;
（user_id 无索引 → 全表扫描锁行）

脚本 2：
UPDATE orders SET remark = concat(remark, x);
（漏写 WHERE → 全表更新）

脚本 3：
ALTER TABLE orders ADD INDEX idx_create (created_at);
（4800 万行，高峰期执行）

问题点：
1 脚本 2 无 WHERE：全表 4800 万行被更新
2 脚本 1 走全表扫描：扫全表锁命中行，
  且锁范围不可控
3 脚本 3 高峰期加索引：
  大表 DDL 锁竞争拖垮在线写入
4 三脚本包在一个事务里：
  大事务日志膨胀 + 锁持有时间长
5 无回滚方案：改错了怎么退回来`,
  steps: [
    ['读意图', '脚本想做什么改什么', '2 分钟'],
    ['看计划', 'EXPLAIN 每条语句', '3 分钟'],
    ['估锁', '锁范围与在线业务冲突', '3 分钟'],
    ['算回滚', '失败后如何恢复', '2 分钟'],
    ['改写法', '分批低峰可重试', '3 分钟'],
    ['写报告', '分级与执行窗口', '2 分钟'],
  ],
  full: `【代码审查报告 · 数据订正脚本】

一、高危问题（2 处，执行阻断）

H1 脚本 2 漏写 WHERE（全表更新）
　后果：4800 万行全量改写，主从延迟爆表、
　　binlog 撑爆、业务锁等待雪崩
　审查动作：任何 UPDATE/DELETE 先问
　　「WHERE 呢」，无 WHERE 一律打回
　整改：补 WHERE，且先跑同条件 SELECT
　　COUNT 确认影响行数符合预期

H2 大表高峰加索引（锁竞争）
　后果：4800 万行 DDL 与在线写入抢锁，
　　高峰期执行 = 写入排队事故
　整改：
　　· 用 Online DDL 工具
　　　（gh-ost / pt-osc）无锁变更
　　· 执行窗口：业务低峰（凌晨 3-5 点）
　　· 先在从库或影子表演练

二、中危问题（2 处）
M1 脚本 1 全表扫描订正：
　user_id 无索引 → 先补索引再订正，
　或按主键范围分批（每批 1 万行，
　批间 sleep 100ms）
M2 大事务：
　拆小事务分批提交，单事务 ≤1 万行，
　控制锁持有时间与日志膨胀

三、回滚方案（必须随脚本提交）
· 订正前导出目标行快照（备份表）
· 准备好反向 UPDATE 脚本并演练
· 明确「执行失败」判据与中止时机

四、建议（3 条）
· 订正脚本模板化：影响行数预估 →
　备份 → 分批 → 校验 → 报告
· 每批执行后校验业务指标
　（订单成功率、慢查询数）
· DDL 进变更平台，
　禁止手工直连执行

五、结论
三脚本全部重写为分批版本，
低峰窗口执行，全程双人复核。`,
  stats2: [['分批', '1 万行/批'], ['备份', '目标行快照'], ['窗口', '凌晨低峰']],
  qa: [
    ['无 WHERE 的 UPDATE 为什么值得「一律打回」的强硬？', '因为它的代价与概率严重不对称：写脚本的人 99% 是漏写而非故意，但一旦执行，全表更新的恢复成本是小时级甚至数据丢失级。这类「手滑即灾难」的语句，审查的强硬就是最便宜的保险——没有例外条款，才有真正的安全。'],
    ['大表订正为什么要分批加 sleep？', '数据库的写入资源是有限的：一次更新 100 万行，意味着长事务、大锁范围、binlog 洪峰，主从同步瞬间延迟。分批 1 万行加批间休眠，是把洪峰摊平成细流——在线系统的数据订正，永远要问「业务还喘得过气吗」。'],
    ['「先跑 COUNT 再执行」的习惯价值在哪？', '影响行数是 SQL 的预检报告：预期改 300 行，COUNT 出来 300 万行，说明条件写错了——这个差异在 SELECT 时看到是虚惊，在 UPDATE 后看到就是事故。COUNT 先行是把「惊讶」从执行后移到执行前的零成本动作。'],
  ],
  fbs: [
    ['DBA', '这两条高危拦下来，就是拦下了一次全站慢查询'],
    ['开发同学', '分批模板照着改，脚本一次过审'],
    ['值班运维', '低峰执行加双人复核，睡得着觉了'],
  ],
  risks: ['线上 DDL 必须评估锁范围，大表用 Online DDL 工具', '无 WHERE 的 UPDATE/DELETE 一律打回', '订正必须带备份与回滚脚本', '执行前先 COUNT 核对影响行数'],
}),
];
